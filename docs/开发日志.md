# MATR 开发日志

> 每天的更新写在最后面（追加在文件末尾），不插入到顶部。

## v0.5.5 正式版：GUI、陆联、王点撤退全面完善（2026-07-04）

### 王点前撤退
- 时代二 6 个节点全部从 ColorMatch 转为 TemplateMatch，消除跨图误匹配
- 时代三~八共 21 张地图所有 ROI 经实机验证重新校准
- 新增 2-3 第三 ROI、5-4 第二 ROI、7-4（此前完全缺失）
- 新增六图与八图道中撤退节点
- E7/E8 节点添加 green_mask

### 陆联
- 行动选择延迟优化：`LR_IsActionSelect` / `LR_ClickContinueBattle` 500→100ms，每轮省 800ms
- 特殊掉落检测 expected 改为正则 `完成|撤退`
- 新增 `LR_CheckCaptainDamage` 重伤检测 + `CaptainDamageAction` 自定义动作
- 新增 `LR_IsSupplyPopup` 补给弹窗检测
- 难度选择双重验证（`LR_VerifyDifficulty`）
- `LR_AfterCaptain` pre/post_delay 500ms，`LR_ClickReplenishConfirm` pre_delay 500ms
- 选项重排：换队长→部队交替→选择部队→队伍序列→选择难度

### GUI
- 设置-启动设置：「模拟器路径」→「游戏路径」
- 默认窗口大小 1028×650
- 合战场任务说明更新（删除自动行军提速、补充 7-4 限制、道中撤退说明）
- 陆联任务说明更新（令牌弹窗警告）
- 隐藏地下城、江户城任务
- README 移除月度 commit 计数

### 修复
- 补回 `MaaProcessor.cs` 中 `TryRunTasksAsync` 的任务完成/失败日志输出
- 资源包全量更新支持 `resource/` 子目录查找 `interface.json`
- 各种 stash/squash 导致的文件丢失问题

### Verify 兜底机制
- 所有 pipeline 中 Verify 节点的 `next` 列表追加对应 `DetectWhereAmI` 兜底，验证失败立刻跳回检测循环，不再空等 20 秒超时
- 涉及 19 个节点、7 个 pipeline 文件：
  - **Sortie**：`S_IsRegionSelect`、`S_VerifyMenuClosed`、`S_VerifyHomeMenuClosed`、`S_VerifyAfterRepairMenuClosed`
  - **LRentaisen**：`LR_VerifyDifficulty`、`LR_VerifyEventPage`、`LR_VerifyMenuClosed`、`LR_VerifyHomeMenuClosed`
  - **Underground**：`U_VerifyEventPage`、`U_VerifyMenuClosed`、`U_VerifyAfterRepairMenuClosed`、`U_VerifyHomeMenuClosed`
  - **FlowerBrush**：`FB_VerifyLeftMenuSortie`、`FB_VerifySortieMenuClosed`
  - **EdoCastle**：`EC_VerifyLeftMenuSortie`、`EC_VerifySortieMenuClosed`
  - **GoHome**：`G_VerifyMenuDir`
- 已有正确兜底的节点无需修改（`M_VerifyMenuFromMix`、`D_DisassembleVerifyMenu` 等）
- 修复流程专用兜底保留原样（`U_VerifyMenuOpened`、`S_VerifyMenuAfterRepair` 等）
- 问题来源：`S_IsRegionSelect` 在非地域选择界面反复误命中（score 0.996），`S_VerifyRegionSelect` 验证失败后 next 列表耗尽，原地重试直到超时触发 onerror
- **on_error 兜底大规模补完**：29 个节点 + 4 个新增节点，覆盖 7 个 pipeline 文件
  - **导航节点**：`ClickMenuForSortie` ×2、`ClickMenuForRepair` ×2、`ClickMenu` ×3、`DirAfterRepair` ×2、`SortieAfterRepair` ×2、`ClickUndergroundInMenu` ×2 → `DetectWhereAmI`
  - **修刀节点**：`ClickRepairInMenu` ×2 → `DetectWhereAmI`；`SelectDamagedSword` ×2 → `CheckRepairScreen`（新增）；`ConfirmSelectSlot` ×2 → `LogNoDamagedSword`（新增）；`ConfirmCancelPreDamage2` ×2、`ConfirmCancelMarchingDamage2` ×2、`ConfirmReturnHome` ×2、`IsMarchingDamage2` ×2 → `DetectWhereAmI`
  - **江户城/陆联**：`EC_ClickRestore3`、`LR_ClickRestore3` → 自循环重试
  - 新增节点：`CheckRepairScreen`（OCR 确认修复界面）、`LogNoDamagedSword`（日志"未检测到可修复刀剑"）
  - 确认无需处理：`HandleNoTicket` ×2、`ReplenishTicket` ×2（位于分支枢纽列表，不命中自动走下一候选）

### 文档
- 新增 `docs/合战场任务设计.md`、`docs/陆联任务设计.md`、`docs/复用节点清单.md`
- 合战场设计文档：所有节点（闪退恢复 24 节点、重伤处理完整链路、刀装补充、团队选择、出阵确认、菜单验证等）全部展开为具体 JSON 代码块
- 复用节点清单：15 个全任务通用节点 + 本丸/菜单/重伤/修刀/邮箱等高频复用节点 + 任务专属矩阵 + 新增任务检查清单
- 新增 `docs/王点前撤退_ROI验证.md`
- 陆联设计文档：Verify 节点兜底机制说明

## 2026-07-05

### Pipeline 兜底修复
- **Sortie**：`S_ClickRetreat` 的 `next` 追加 `S_DetectWhereAmI`，点击撤退按钮后若 OCR 未识别到"确认返回本丸"弹窗，走 `S_DetectWhereAmI` → 行军循环 → boss 检测回路，替代原来死等 20s 超时
- 问题来源：`S_ConfirmRetreat` OCR 成功后画面未及时切换，`S_Boss_E4_R3` 再度命中触发第二轮撤退，第二次点击后弹窗已消失，OCR 连续 17 次空结果直到超时

### Issue 模板更新
- 任务选项同步当前流水线：远征、合战场、陆联、刷花、刀解、习合（移除已隐藏的江户城、回到本丸、地下城）

### MuMu 12 更新后截图性能降级
- **现象**：MuMu 12 更新后截图耗时从个位数 ms 飙升到 ~130ms/帧
- **原因**：更新导致 MaaFramework 自动检测链路断裂，设备被识别为通用 ADB `emulator-5554` 而非 MuMu 设备，EmulatorExtras 快速截图失效，退化为 `adb exec-out screencap -p`
- **手动修复方案**：
  - Serial 改为 `127.0.0.1:16384`（从 MuMuManager 获取）
  - 截图方式选 `EmulatorExtras`
  - 额外参数填 `{"extras":{"mumu":{"enable":true,"path":"<MuMu 安装目录>","index":0}}}`
- 属于 MaaFramework 上游兼容性问题（`AdbDeviceWin32Finder.cpp` 中 `MumuInfo` 解析新格式失败）

### 全量资源更新修复
- 修复「重新下载全量资源包」后资源加载失败：全量更新删除旧文件时递归遍历 `resource/`，误删了刚解压在 `resource/temp/temp_res/` 中的更新临时文件，仅 `Changelog.md` 和 `interface.json` 被显式跳过幸存，其余 pipeline/image/model 等全部丢失
- 修复方案：删除循环中增加 `temp/` 目录跳过逻辑，避免删除刚解压的更新文件
- `ValidateExtractedResourcePackage` 增加递归搜索兜底：候选路径未找到 `interface.json` 时，全目录递归搜索，并自动推断 `resourceDirPath` 和 `originPath`，兼容任意 ZIP 目录结构

### 版本更新
- 版本号升至 v0.6.0-alpha
- Changelog.md 更新为 v0.6.0 内测版内容

### Pipeline 兜底修复（on_error / next / timeout）
- **Sortie**：`S_ConfirmRetreat` 添加 `on_error`，OCR 识别"确认返回本丸"超时后跳回 `S_DetectWhereAmI`
- **LRentaisen**：
  - `LR_ClickDifficulty`：`next` 追加 `LR_DetectWhereAmI` + `on_error`，`LR_VerifyDifficulty` 复核失败后即时兜底
  - `LR_ActionSelectHub`：`next` 追加 `LR_DetectWhereAmI`，OCR "重伤"/"战斗" 全失败时兜底
  - `LR_IsEventPage`：`next` 追加 `LR_DetectWhereAmI`，TemplateMatch 失败时兜底
  - `LR_CheckSwordDropAfterSpecial`：新增 `pre_delay=1000`、`timeout=5000`、`on_error`，移除 DirectHit 抢断，给 `LR_WaitForEventPage` 留 5s 匹配窗口
- 所有修复均为同类问题：识别/验证失败导致上级节点默认 20s 超时后崩溃，补兜底路径跳回检测枢纽
- 修复过程中发现 5 处隐藏的超时陷阱，均在崩溃前拦截

### 文档
- 更新 `docs/合战场任务设计.md`：同步 `S_ConfirmRetreat` 的 `on_error` 变更
- 更新 `docs/陆联任务设计.md`：同步所有 LRentaisen 节点兜底变更，补全缺失的 `LR_CheckSwordDropAfterSpecial` 和 `LR_IsSwordDropAfter` 节点文档

## v0.6.0 正式版 / v1.0.0-alpha 项目结构标准化（2026-07-07）

### 故障排查
- 排查 Sortie 任务 14:57:25 超时失败：`S_CheckHomeBrightness` 的 `S_IsHome` 模板匹配（本丸.png）连续 40 次返回空结果，20s 后超时
- 原因：`S_IsHome` 缺少 `on_error` 兜底，死循环直至超时；游戏画面可能已离开本丸界面
- 修复：`S_IsHome` 添加 `on_error` → `S_DetectWhereAmI`，`S_CheckHomeBrightness` 的 `next` 追加 `S_DetectWhereAmI` 作为备选

### 项目结构标准化
- **目标**：将 MATR 目录布局对齐 MFAAvalonia 上游原生标准，减少 fork 维护成本
- **AppPaths.cs**：路径全部回到上游原生版本
  - `interface.json`、`config/`、`agent/`、`temp/`、`backup/` 从 `resource/` 内提升到根目录
  - `LogsDirectory` 保持 `debug/logs/`
- **interface.json**：从 `resource/interface.json` 移出到根目录
- **resource/**：仅保留 `base/`（流水线）、`logo/`、`announcement/`
- **pack.ps1**：适配标准布局，`interface.json` 在根目录，`DependencySetup_*.bat` 置于 zip 根目录（与 MATR 文件夹同级）
- **Desktop.csproj**：添加 publish 后清理目标，自动移除 NuGet 包默认的 `resource/interface.json` 等污染文件
- **.gitignore**：新增 `/config/`、`/temp/`、`/backup/`、`/agent/`、`/appsettings.json`、`/libs/`、`/plugins/`、`/runtimes/win-x64/`

### v0.6.0 过渡版
- 在旧布局 `AppPaths.cs` 中添加启动时自动迁移逻辑：将 `resource/config/`、`resource/backup/`、`resource/agent/` 中的用户数据复制到根目录标准位置
- 检测条件：根目录 `config/instances/` 不存在时执行，避免重复迁移
- 目的：用户从 v0.5.5 更新到 v0.6.0 时自动保护数据，为 v1.0.0 全量删除 `resource/config/` 做准备

### 公告
- 新增 v0.6.0 更新公告（稳定性修复：任务超时兜底、全量资源加载修复）
- 更新长期公告：交流反馈添加 GitHub Issues 链接

### 文档
- 新增 `docs/标准化.md`：详细记录布局变更前后对比和所有调整项

## v0.6.1-alpha（2026-07-08 ~ 07-11）

### MuMu EmulatorExtras 自动修复
**问题背景**
- MuMu 12 更新后 MaaFramework 上游 `AdbDeviceWin32Finder.cpp` 中 `MumuInfo` 解析新格式失败，设备被识别为通用 `emulator-5554` 而非 MuMu 设备，`EmulatorExtras` 快速截图失效，退化到 `adb exec-out screencap -p`（~130ms/帧）
- 此前已在开发日志中记录手动修复方案（Serial 改 `127.0.0.1:16384`、截图方式选 `EmulatorExtras`、注入 extras JSON），但普通用户无法完成此操作

**实现方案**
- 在 `EmulatorHelper.cs` 中提取 `TryGetMuMuInstallInfo(adbSerial)` 公开方法，从 `KillEmulatorMuMuEmulator12` 中复用安装路径推导和模拟器索引计算逻辑
- 在 `MaaProcessor.cs` 的 `ConfigureAdbScreenCapTypes()` 和 `ConfigureAdbInputTypes()` 中，当设备名含 "MuMu" 且 MaaFramework 返回 `Default` 时，自动返回 `EmulatorExtras`
- 在 `TaskQueueViewModel.cs` 的 `ChangedDevice` 中选择 MuMu 设备后，自动调用 `EmulatorHelper.TryGetMuMuInstallInfo` 获取安装目录和索引，将 serial 从 `emulator-XXXX` 切换为 `127.0.0.1:PORT` 直连，注入 extras JSON 配置
- 全程用户无感知，无需手动配置

### 出击流程
- `Sortie.json`：`S_LogPreDamage1` 的上级节点点击动作从默认识别区中心点击改为固定坐标点击 `(788, 465)`，避免识别兜底时点偏位置

### 打包与命名
- `pack.ps1` 修复嵌套问题：`Compress-Archive` 从 `$TempBase\*` 改为 `$TempDir\*`，消除 zip 解压后的多余 `MATR/` 外层目录
- `pack.ps1` 去掉 `DependencySetup_依赖库安装_win` 打包
- `pack.ps1` 默认打包版本号更新为 `v0.6.1-alpha`
- `DependencySetup_依赖库安装_win.bat` 重命名为 `DependencySetup_win.bat`，去除文件名中的中文
- `.gitignore` 新增忽略打包产物 `MATR-*.zip` 与临时目录 `_temp_zip/`

### 版本
- `interface.json` 版本号升至 `0.6.1-alpha`

### 文档
- 新增 `docs/刮刮乐识别设计.md`

## 项目更名：AATL → MATR（2026-07-12）

项目由 **AATL** 正式更名为 **MATR**（Maa Assistant TokenRanbu），与 MaaFramework 生态命名风格保持一致。涉及变更：

- 程序集名称、输出文件名、窗口标题
- 项目配置 `interface.json` 及所有文档
- Logo 资源文件
- 打包脚本 `pack.ps1`
- 本地脚本目录 `aatl/` → `matr/`
- GitHub 仓库 `NotZoruak/AATL` → `NotZoruak/MATR`

## GUI 交互优化与子选项齿轮导航（2026-07-17 ~ 2026-07-18）

### 复选框改造
- 选项复选框从 `ToggleButton` 改为 `CheckBox`，样式与任务列表勾选框共用 SukiUI GlassCard 模板
- 多 case 复选框布局从 `UniformGrid { Columns = 2 }` 改为 `WrapPanel` 自适应换行

### 下拉框搜索移除
- ComboBox 搜索功能关闭（`SetCanSearch = false`）

### 全选按钮合并
- 「全选」和「取消全选」合并为单按钮，通过 `ToggleSelectAllCommand` 切换
- Tooltip 显示「全选 / 取消全选」

### 任务队列视图布局调整
- 12 列网格中任务列表从 4 列缩为 3 列，任务设置和任务说明从 4 列扩为 5 列
- 对应更新 `resource/mfa_layout.json` 默认布局文件

### 子选项齿轮导航
- 含子选项的控件标签右侧显示齿轮图标（`FluentIcon.Settings`，16×16）
- 点击齿轮进入子页面：返回按钮 + 选项名称标题 + 子选项控件
- 子页面与主选项视图互斥显示，由 `TaskQueueViewModel` 的 `IsSubPageOpen` / `SubPageTitle` / `SubPageContent` 驱动
- 有齿轮入口的选项，主页面不再内联渲染子选项（`HasSubOptions` 判断跳过 `UpdateSubOptions`）

### 远征 pipeline
- `E_SelectTeam1~5`（使用 `本丸远征.png` 的节点）移除 `target_offset`

### 刷花 pipeline
- `FB_FilterByHorse` 更名为 `FB_FilterByTroop`，target 改为 `[226, 514, 109, 26]`

### 陆联 pipeline
- `LR_ClickDifficulty` 默认 target 改为 `[1110, 319, 18, 18]`，移除 `target_offset`
- `LR_选择难度` 四个难度 ROI 全部更新（易/普/难/超难）

### 选项重组
- `LR_换队配置`（队伍序列）改为 `LR_部队交替` 的子选项，不再直接显示在陆联任务设置中
- `LR_部队交替` 的 case 增加 `"option": ["LR_换队配置"]`

### 窗口标题
- `custom_title` 改为 `MATR v0.6.1-alpha | 刀剑乱舞小助手`

### 测试任务
- 新增 `gui设计测试` 任务（`entry: GuiTest`），用于 GUI 控件效果验证（已于 2026-07-20 移除）
- 新增 `测试下拉（带子选项）` 和 `测试子选项` 示例选项定义（随 GuiTest 一并移除）

### 文档
- 新增 `docs/任务选项配置说明.md`
- 新增 `docs/远征任务设计.md`
- 新增 `docs/刷花任务设计.md`
- 新增 `docs/后续优化规划.md`
- 同步更新 `docs/习合任务设计.md`、`docs/合战场任务设计.md`、`docs/陆联任务设计.md`

### 项目配置
- 新增 `MATR/CLAUDE.md`（发布流程与路径陷阱说明）
- `pack.ps1` 保留 `MATR.dll` 拷贝（apphost 启动必须）

### 全任务枢纽超时重启（2026-07-18）
- 所有 `DetectWhereAmI` 枢纽及 `Expedition` 入口新增 `"timeout": 120000`（2 分钟）
- 超时后通过 `on_error` 触发 `RestartGameAction` 自定义动作：ADB 强杀 `com.youzu.djlw` → 等待 2 秒 → `monkey` 重启游戏
- 新增 C# 自定义动作 `RestartGameAction`（注册于 `MaaProcessor.cs`）
- 9 条流水线全部添加对应的 `X_RestartGame` 节点和 `on_error` 引用

### Pipeline 修复
- 习合 `M_ClickSelectAll`：删除 `timeout: 3000`，`post_delay` 改为 1000ms，`next` 加 `M_ExitNoMaterial`，`on_error` 改 `M_DetectWhereAmI`
- 习合 `M_CheckPos1~6`：默认 `next` 列表加 `M_ClickMixDirectN`（搓糖关闭不再死循环）
- 远征 `E_SelectTeam1~5`：移除 `target_offset`
- 刷花 `FB_FilterByHorse` → `FB_FilterByTroop`，target 改为 `[226, 514, 109, 26]`
- 陆联 `LR_ClickDifficulty`：默认 target 改为 `[1110, 319, 18, 18]`，移除 `target_offset`
- 陆联 `LR_选择难度`：四档难度 ROI 全部更新

### 文档
- `docs/复用节点清单.md`：新增枢纽超时重启配置说明，修正 6 处过期节点数据（`IsHome`、`IsGameIcon`、`IsGameUpdatePopup`、`IsInGameUpdatePopup`、`IsSwordDropColor`、`IsSwordSelect`）

### 发布陷阱
- `MFAAvalonia.Core.dll` 必须从 `_src/MFAAvalonia/bin/Release/net10.0/` 拷贝，`_src/bin/AnyCPU/Release/` 下的为旧缓存（文件大小不同）

## 日志、全局设置、换队长、远征间隔（2026-07-18）

### 日志优化
- 任务完成/失败日志带任务名称（`{任务名} 任务完成`）
- 队列全部完成后输出汇总：`累计 X 成功，Y 失败`，手动停止也输出
- 修复计数器：只统计用户勾选的核心任务，排除预脚本/后置脚本

### 全局设置标签页
- 新增「全局」标签页，与「常规」标签互斥切换
- 删除高级标签页（`AdvancedOptionSettings` → `GlobalOptionSettings`）
- `TaskQueueViewModel` 新增 `IsGlobal` 属性、`GlobalPanelCache` 缓存
- `TaskOptionGenerator` 新增 `GenerateGlobalPanelContent`
- `interface.json` 新增 `global_option` 数组，通过 `TaskLoader.InitializeGlobalSelectOptions` 加载
- 修复：`__GlobalOption__` 不再在任务列表中显示

### 全局选项
- **卡死等待时间**：input（秒），默认 600，`pipeline_override` 拼接 `{seconds}000` 设置所有枢纽的 `timeout`（毫秒）
- **换队长方式**：select（马匹筛选/刀装筛选/队内拖拽），默认马匹筛选
  - 刀装筛选：`pipeline_override` 修改所有任务 `ClickCaptain3` 的 target 为 `[220, 512, 107, 29]`
  - 队内拖拽：新增 `DragCaptainAction` 自定义动作（OCR 六位置疲劳值 → 长按 1s → 拖拽 500ms → 空槽位跳过）
  - 子选项「拖拽跳过位置」：checkbox 六位置，默认全部不勾选
- 合战场/陆联任务说明更新为「切换队长的方式可在全局设置中修改」

### 远征间隔修复
- `RefreshInterval` 项新增 `pipeline_override`：`"E_WaitRefresh": { "post_delay": "{seconds}000" }`，秒输入转为毫秒
- 修复前为硬编码 60000ms，GUI 输入 10 分钟无效

### 子页面间距优化
- 子选项区域 `WrapPanel` 顶部间距从 2px 调整为 -10px，标题从 4px 调整为 15px

### 文档修正
- `S_IsSwordDropColor` ROI 统一修正为 `[598, 543, 66, 1]`
- 远征/陆联/合战场/复用节点文档同步更新
- 优化规划更新：智能调度方案、全局设置标签页、队内拖拽方案

## 换队长方式全局选项（2026-07-18）

### 三种换队长模式
- **马匹筛选**（默认）：保持原逻辑不变
- **刀装筛选**：`pipeline_override` 修改所有任务 `ClickCaptain3` 的 target 为 `[220, 512, 107, 29]`
- **队内拖拽**：新增 `DragCaptainAction` 自定义动作
  - OCR 六个位置疲劳值（ROI 80×22，读取 `XX/100` 格式）
  - 找最低疲劳值所在位置（空槽位自动跳过）
  - 最低值在位置 2-6 → 长按 1s + 拖拽 500ms 到队长槽
  - 最低值在位置 1 → 跳过拖拽
  - OCR 纠错：B→8、O→0、S→5

### 拖拽跳过位置子选项
- 勾选「位置一」时自动跳过位置二，位置二成为新队长槽
- 状态持久化：`TaskLoader` 恢复 `SubOptions`，`PersistResourceOptionConfiguration` 直接从 `GlobalSelectOptions` 保存
- 修复：`__GlobalOption__` 资源项删除后全局选项无法保存的 bug

### 卡死重启全局开关
- 开关类型，默认开启：禁用所有 `FallbackWait` 节点
- 子选项：`目标应用`（包名，默认 `com.youzu.djlw`）+ `卡死等待时间`（秒，默认 120）
- `RestartGameAction` 改为动态读取包名

### Pipeline 修复
- `S_PostSortieHub`：加 `on_error → S_DetectWhereAmI`，`timeout: 30000`
- `S_DetectAfterSortieNow`：加 `on_error → S_DetectWhereAmI`
- `LRentaisen`/`Underground`：加 `DragCaptain` 节点
- `E_WaitRefresh.post_delay` 由 `RefreshInterval` pipeline_override 动态设置

### GUI
- 输入框 `MinWidth` 120 → 200

### 远征间隔修复
- `RefreshInterval` 项加 `pipeline_override`：`{seconds}000` 秒数拼毫秒，修复前 GUI 输入无效

### 任务队列回本丸（2026-07-18）
- 不同任务类型之间自动插入 GoHome 步骤，确保每个任务从本丸开始
- 回本丸任务从任务列表移除（隐藏），仅代码自动调用
- `S_ClickSortieNow`、`S_DetectAfterSortieNow`、`S_PostSortieHub` 加 `on_error` 兜底

### 选项配置
- 合战场 `repeatable: false, repeat_count: -1`（无限循环，不显示次数）
- 习合 `M_ClickSelectAll`：去 timeout、`post_delay: 1000`、`next: [M_CheckAndMix, M_ExitNoMaterial]`
- `M_CheckPos1~6`：默认 next 加 `M_ClickMixDirectN`
- 队内拖拽 pipeline_override：禁用旧刀剑选择链（`S_IsSwordSelect`/`S_DetectSortOrder` 等）

## v0.6.1-beta：陆联同步远征 & 后台计时器（2026-07-18）

### 陆联同步远征
- 新增 `LR_同步远征` 选项，陆联回本丸时自动跳转远征流水线派遣空闲队伍
- `MaaProcessor` 添加 `LRentaisen` 入口名，自动复用远征任务部队配置
- `LRentaisen.json` 新增 `LR_WaitRefresh` 等待节点
- `E_CheckTeamStatus` 点击"状"后经 `E_IsTeamStatusPanel` 验证面板已打开

### 远征后台计时器
- 全部忙线后启动倒计时（数值 = RefreshInterval），倒计时归零后回活动页时主动回本丸检查
- 新增 `ExpeditionTimerAction` / `ExpeditionTimerCheckAction` / `ExpeditionTimerRecognition` 自定义类
- `Expedition.json` 新增 `E_TimerStart` / `E_CheckTimerExpired` / `E_TimerReset` 节点
- 正常跳过走 `next`（不截图），过期回本丸走 `on_error`
- GUI 日志面板显示倒计时启动和到期提示

### GUI
- 任务选项 checkbox 改用自适应列宽 UniformGrid，随窗口宽度自动调整列数
- 陆联选项重排：选择部队→选择难度→同步远征→换队长→部队交替
- `E_ClickBackToHome` pre_delay 100→1000ms，`LR_WaitRefresh` 新增 pre_delay 3000ms

### 文档
- 远征任务设计文档完整更新，与实际 JSON 保持一致
- 新增同步远征集成说明和后台计时器章节
- 复用节点清单新增远征计时器节点

## v0.6.1：远征智能调度 & 合战场同步远征间隔 & 长期远征计划设计（2026-07-19 ~ 2026-07-20）

### 远征智能调度
- 新增全局 switch「远征智能调度」（默认关闭），派遣后 OCR 队伍面板剩余时间动态计时
- 取最早归队时间与远征间隔的较小值，+10 秒缓冲（后删除）
- 独立远征通过 `E_TrackerOCR` + `E_SmartWait` 替代固定 `post_delay`
- 同步远征在 `E_TimerStart` 内部调用 OCR 逻辑，无需额外 pipeline 节点
- 新增 `ExpeditionReturnTracker`（静态追踪器）、`ExpeditionTimeTracker`（OCR 扫描）、`SmartWaitAction`（动态等待）
- 修复 `IsSmartSchedulingEnabled()` switch 类型判断错误（`SelectedCases` → `Index`）
- 修复智能调度失败时队伍状态面板未关闭的问题

### 合战场同步远征间隔
- `S_同步远征` 接入后台计时器：`S_IsHome → E_CheckTimerExpired`，未过期跳过远征
- `E_CheckTimerExpired.next` 和 `E_TimerStart.next` 按任务区分，不再硬编码 LR
- `MaaProcessor` 计时器注入按入口名动态设置 `next` 目标

### 换队长优化
- 修复换队长复选框无法控制队内拖拽：`S_DragCaptain` 等默认 `enabled: false`，复选框统一控制三种模式
- 队内拖拽点击坐标从 OCR ROI 中心改为独立 `DragPoints`（`[477, y]`）

### 日志与 UI
- 远征计时日志统一「[远征计时]」前缀，精简内容
- 倒计时显示改为 `X分Xs` 格式
- 远征间隔选项独立为全局设置（后撤回保持原样）
- 「卡死重启」默认值改为关闭
- 游戏图标模板匹配阈值 0.95 → 0.88（全部 9 个 pipeline + 2 个文档）

### 文档
- 复用节点清单全面校对：新增换队长章节、重写远征共用节点（含智能调度）、修正 `DetectSortOrder` OCR 错误（"並替"→"降序"/"升序"）、添加目录
- 新增 `长期远征计划设计.md`：派遣前疲劳检查 + 自动 1-1 刷花，含完整流程、节点清单、特供换队长（喜爱筛选 + ColorMatch）、使用部队记录机制
- 删除过时文档：远征后台计时功能设计、标准化、ALAS 调度器分析
- `后续优化规划.md` 更新：远征智能调度/日志优化/全局设置标签页标记已完成
- v0.6.1 更新公告（docs + resource 双份）
- MATR 长期公告更新（去掉 beta、更新仓库地址和新功能）

## Expedition 管线死循环修复（2026-07-20）

### 问题排查
- **现象**：Sortie 任务中的 Expedition 节点持续运行约 2 小时 50 分钟（10217428ms），直到用户手动停止
- **根因**：远征途中触发内番完成对话画面，该画面的识别特征（ColorMatch 颜色条位置与范围）与刀剑掉落画面相同，但 Expedition 节点的 `next` 列表中缺少对应识别节点。全部识别失败后落入 `E_FallbackWait`（`DoNothing`），而其 `next` 指向 `Expedition`，形成死循环：识别 → 全失败 → Fallback → DoNothing → 再识别
- **判断依据**：
  - 内番完成对话画面与刀剑掉落画面共用同一套 ColorMatch 识别参数（ROI `[598, 543, 66, 1]`，颜色范围 upper `[96,56,10]` / lower `[88,48,2]`）
  - `Expedition` 的 next 列表仅 14 个节点，缺少该识别节点
  - 日志中 Expedition 循环内所有识别均 Failed，最终命中 `E_FallbackWait`

### 修复
- Expedition 节点 `next` 列表新增 `E_IsSwordDropColor`（位于 `E_IsInternalReport` 之后、`E_FallbackWait` 之前）
- 新增 `E_IsSwordDropColor` 节点定义：ColorMatch 识别刀剑掉落画面颜色条 `[598, 543, 66, 1]`，匹配后点击 `[1067, 558, 120, 79]` 关闭掉落画面，成功/失败均回 `Expedition`

### 已知遗留
- 该节点同时覆盖内番完成对话画面和刀剑掉落画面（两者 ColorMatch 识别特征相同），因此命名为 `E_IsSwordDropColor` 虽不精确但功能正确

## 长期远征计划刷花链路实现（2026-07-20 ~ 2026-07-21）

### 疲劳检测修复
- `GetThreshold()` 修复：疲劳阈值是"长期远征计划"的子选项，需穿透 `SubOptions` 查找，而非直接在 `GlobalSelectOptions` 中搜索
- 输入类型的值从 `Data["threshold"]` 读取，非 `SelectedCases[0]`
- `val == 0` 视为 OCR 误读跳过（如 `100/100` 误读为 `00/100`）
- `check_captain` 达标时输出 GUI 日志"[远征疲劳检测] 疲劳值恢复完成"

### 刷花链路
- `E_CheckFatigueN.on_error`：`E_FlowerBattleHub` → `E_FlowerNavigate`（先点目录回本丸，再进枢纽）
- `E_FlowerNavigate` 加 `post_delay: 1000`（等本丸加载）
- `E_FlowerClickSortieInMenu` 加 `post_delay: 1000`（等时代选择页加载）
- `E_FlowerClickFirstEra` / `E_FlowerClickRegion1_1` 阈值 0.98→0.95
- 新增 `E_FlowerConfirmEra`：点击"决定"按钮确认时代选择
- `E_FlowerBattleHub` 新增 4 个导航节点（14-17），完整覆盖 出阵→时代→地域→选队 链路
- 新增 `E_FlowerHomeBrightness` + `E_FlowerIsHome`（刷花专用本丸检测，与远征 `E_CheckHomeBrightness` 隔离）
- `E_FallbackWait.next`：`Expedition` → `E_FlowerBattleHub`（重试枢纽，不重开远征）
- `E_FlowerMarching` 补 `action: Click`；`E_FlowerFormation` 指定阵形点击位置 `[588, 404, 74, 46]`
- `E_UseTeamRecord_Step*` 全部加 `post_delay: 300`

### 解散换队长流程修正
- 流程顺序改为：选队 → 先解散换队长 → 再查疲劳 → < 阈值出阵 / >= 阈值回家
- `E_FlowerTeamSelect.next` → `E_FlowerDissolve`
- `E_FlowerCaptainFind.next` → `E_FlowerCheckCaptain`
- `E_FlowerCheckCaptain.on_error` → `E_FlowerSortie`

### 接口与 UI
- "长期远征计划"子页面添加使用说明（纯文本）
- "远征智能调度"添加功能说明
- 子页面内容上移 20px（`SubPageContent` Margin `-6`→`-26`）

### 文档
- `长期远征计划设计.md` 全面同步：枢纽表格、流程图、JSON 代码块、新增节点定义

## 远征刷花管线深度优化（2026-07-21）

### 死循环修复：拆分 Expedition/E_FlowerBattleHub 兜底节点
- **根因**：`E_FallbackWait` 的 next 同时被远征中枢和刷花中枢引用，远征识别失败后误跳入 `E_FlowerBattleHub`，刷花中枢无法匹配远征画面，无限循环
- **日志证据**：`maafw.log` 中 `E_FlowerBattleHub` 在约 64 秒内循环 45-50 轮，OCR 识别到"远征中（01"但 hub 全节点不匹配
- **修复**：
  - `E_FallbackWait` next 改为 `Expedition`（远征侧兜底回远征中枢）
  - 新增 `E_FlowerFallbackWait` next 指向 `E_FlowerBattleHub`（刷花侧兜底回刷花中枢）
  - `E_FlowerBattleHub` next 列表末尾 `E_FallbackWait` → `E_FlowerFallbackWait`

### 删除全部 OCR "战斗结果" 识别节点
- 删除 5 个文件共 7 个 `expected: "战斗结果"` 的 OCR 节点：
  - `Sortie.json`：`S_IsBattleResult_Title`
  - `Underground.json`：`U_IsBattleResult_Title`
  - `LRentaisen.json`：`LR_IsBattleResult_Title`
  - `EdoCastle.json`：`EC_IsBattleResult_Title`、`EC_PostBossBattleResult_Title`
  - `FlowerBrush.json`：`FB_IsBattleResult_Title`、`FB_PostBattleResult_Title`
- 同步清理所有中枢 next 列表中的引用

### 远征刷花换队长链路优化
- 新增 `E_FlowerCaptainClick3`（刀装筛选），插入 Click2 与 ClickFav 之间，target `[226, 514, 109, 26]`
- 换队长点击延迟统一缩短：`E_FlowerCaptainClick1/2/3/4` 的 `post_delay` 800ms → 500ms
- 链路变为：Click1（筛选）→ Click2（清残留）→ Click3（刀装）→ ClickFav（喜爱）→ Click4（樱吹雪）→ Click5（确认）

### 刷花主任务优化：升序后颜色匹配找空闲刀剑
- `FB_ConfirmSortAsc`：删除 click action（仅 OCR 识别升序），next 改为 `["FB_CaptainFind", "FB_CaptainSwipe"]`
- 新增 `FB_CaptainFind`：ColorMatch 找空闲刀剑（ROI `[1224, 137, 11, 553]`，颜色 `[203,105,105]` ±3），命中点击 → `FB_ClickSortieNow`
- 新增 `FB_CaptainSwipe`：`PageScrollAndHoldAction` 滑动翻页 → 回到 `FB_CaptainFind`
- 与远征刷花 `E_FlowerCaptainFind/Swipe` 逻辑一致，仅在 next 目标上不同（刷花跳即刻出阵，远征跳疲劳检测）

### 文档
- `刷花任务设计.md`：同步 FB_CaptainFind/Swipe 节点、修正"马"→"刀装"、删除战斗结果节点引用
- `长期远征计划设计.md`：新增 Click3 节点、更新延迟值、E_FallbackWait → E_FlowerFallbackWait

## pipeline_override 合并顺序修复（2026-07-22）

### 现象
用户设置远征「部队一→休息」后，合战场同步远征仍然进入队伍一的远征流程，并在 `E_SelectMap1`（`ExpeditionMapSelectAction`）处因休息检测返回 `false`，随后 fallback 回主循环重新检查，形成无限循环。日志中 `E_CheckTeam1` 仍然执行且 OCR 识别到"待机中"，说明 `enabled: false` 未生效。

### 根因
`MaaToken.Merge()` 的实现仅为 `Tokens.Add(token)`——将每次合并追加为独立的数组元素，不做深度合并。序列化后同个节点 `E_CheckTeam1` 的 `enabled` 字段出现在两个不同数组元素中：

- index 10（`S_同步远征` option 的 PipelineOverride）：`{"E_CheckTeam1": {"enabled": true}}`
- index 11（远征「部队一→休息」option 的 PipelineOverride）：`{"E_CheckTeam1": {"enabled": false}}`

MaaFramework 的 pipeline_override 数组处理中，`enabled: true` 的优先级异常盖过了 `enabled: false`。

### 修复
将 `MaaToken.Merge()` 改为真正的深度合并：所有覆盖折叠到首个字典中，同一节点的同名属性后续值覆盖先前值。修复后无论合并顺序如何，最后合并的值必定生效。

### 影响范围
- 所有通过 `MaaToken.Merge()` 构建管线参数的流程，包括全局选项、资源选项、控制器选项、任务选项、同步远征
- 修改文件：`_src/MFAAvalonia/Extensions/MaaFW/MaaToken.cs`

### 补充（2026-07-22）：Merge 修复后问题依旧
Merge 逻辑本身无误，但问题仍然复现。日志显示最终 `pipeline_override` 中 `E_CheckTeam1.enabled` 仍为 `true`。

进一步排查发现：**远征任务「部队一→休息」的保存状态未被正确加载**。`CreateNodeAndParam` 中同步远征块从 `InstanceConfiguration` 读取的远征任务 `expTask.Option` 里，部队一的 `Index` 并非 0（休息），导致 `ProcessOptions` 永远走不到休息 case 的 `PipelineOverride`，`enabled: false` 从未被合并。

重启 MATR 后手动将部队一切换为 1-1 再切回休息即可恢复正常，说明写入路径正确但加载路径存在陈旧状态问题。**根因应在 `MaaInterfaceSelectOption.Index` 的持久化或反序列化环节**——旧版本保存的数据可能缺少 `index` 字段，或默认值与 UI 显示不一致。

## E_FlowerBattleHub next 列表 Flower 化（2026-07-22）

### 背景
`E_FlowerBattleHub` 的 next 列表中闪退恢复、远征结果、刀剑掉落节点直接复用了远征中枢的原节点（如 `E_IsGameIcon`、`E_IsSwordDropColor` 等）。这些原节点的 next/on_error 指向 `Expedition`，命中后跳回远征中枢而非刷花中枢，与死循环修复（拆分兜底节点）的设计意图不一致。

### 修改
- **新增 9 个 Flower 专用副本节点**：复制原节点内容，`next`/`on_error` 中的 `"Expedition"` 全部替换为 `"E_FlowerBattleHub"`：
  - `E_FlowerIsGameIcon`、`E_FlowerIsLoginButton`、`E_FlowerIsGameUpdatePopup`、`E_FlowerIsInGameUpdatePopup`、`E_FlowerIsInternalReport`（闪退恢复）
  - `E_FlowerClickExpeditionReturn_Exp`、`E_FlowerClickExpeditionReturn_Title`（远征结果）
  - `E_FlowerIsSwordDropColor`（刀剑掉落）
  - `E_FlowerRestartGame`（重启游戏）
- **`E_FlowerBattleHub.next`**：8 个原节点替换为对应的 `E_Flower*` 版本。`E_IsAnnouncementPopup` 和 `E_IsLoginReward` 保留原样（无 Flower 版本，其闭环链路不涉及 `Expedition`）
- **`E_FlowerBattleHub.on_error`**：`"E_RestartGame"` → `"E_FlowerRestartGame"`
- **删除多余节点**：`E_FlowerWaitRefresh`（刷花流程不需要等待刷新）、`E_FlowerCheckTeamStatus`（刷花过程不打开状态面板）、`E_FlowerSmartWait`（刷花流程不需要智能等待）、`E_FlowerPostClickDirectoryHub`（刷花用 `E_FlowerNavigate` 直接导航，不走目录验证中转）

### 影响范围
- 修改文件：`assets/resource/base/pipeline/Expedition.json`、`docs/长期远征计划设计.md`

## 启动路径改名 & 重伤处理——停止重设计（2026-07-22）

### GUI 本地化
- 设置-启动设置：「模拟器路径」→「游戏路径」
- 四语言全覆盖：简体中文（游戏路径）、繁體中文（遊戲路徑）、English（Game Path）、日本語（ゲームパス）
- 文件：`Strings.resx`、`Strings.zh-Hant.resx`、`Strings.en-US.resx`、`Strings.ja-JP.resx`

### 重伤处理——停止：出战前流程重设计

**问题**：旧停止模式仅记录日志后流水线静默结束，存在三个缺陷：
1. 重伤弹窗检测到后未关闭，画面卡在弹窗上
2. 未返回本丸，刀男留在战场画面
3. 无任何通知，用户不知道任务已停止

**新流程**（出战前）：

```
重伤弹窗 → 点击关闭 → 记录日志 → 目录 → 确认菜单打开 → 点击本丸
  → 确认本丸画面（模板匹配"本丸.png"）
    → 是本丸：终止当前任务 + Toast 通知
    → 不是本丸：点击 [185,22,15,18] 兜底 → 重新确认本丸
```

**新增 7 个节点**（Sortie / Underground，后者 `U_` 前缀）：
- `S_NavigateToHomeFromStop`：导航枢纽，next 含 `S_DetectWhereAmI` 兜底
- `S_ClickMenuStop`：OCR 识别"目录" → 点击，`on_error → S_DetectWhereAmI`
- `S_ClickHomeStop`：模板匹配"菜单目录.png"确认菜单已打开 → 点击本丸按钮，`on_error → S_ClickMenuStop`
- `S_VerifyHomeScreen`：模板匹配"本丸.png"确认回到本丸，`on_error → S_ClickHomeFallback`
- `S_ClickHomeFallback`：点击 [185,22,15,18] 关闭可能的遮罩 → 重新 `S_VerifyHomeScreen`
- `S_TerminateOnDamage`：调用 `StopOnDamageAction` 自定义动作

**修改 3 个节点**（Sortie / Underground）：
- `S_StopOnDamagePopup`：加 `action: Click [788, 465, 1, 1]` 关闭弹窗
- `S_LogStopOnDamage`：message 简化，new next → `S_NavigateToHomeFromStop`
- `S_LogStopOnDamageText`：同上

**新增 C# 自定义动作**：`resource/base/custom/StopOnDamageAction.cs`
- 获取当前任务名（`ViewModel.CurrentTaskName`）和下一任务名（`TaskQueue.FirstOrDefault()`）
- 有下一任务：`[重伤检测] 检测到刀剑男士重伤，{当前任务} 任务终止，开始{下一任务}`
- 无下一任务：`[重伤检测] 检测到刀剑男士重伤，{当前任务} 任务终止，所有任务运行完毕`
- `ToastHelper.Warn("重伤检测", message, duration: 10)` 弹出应用内通知
- `ObservableQueue<T>` 新增 `FirstOrDefault()` 方法

**未改动**：`S_IsStopOnDamage`（修刀画面检测）、`S_IsMarchingDamage`（行军中重伤）维持现状待定。

### 文档
- `docs/合战场任务设计.md`：停止模式章节重写，新增全部节点 JSON 定义和流程图
- `docs/复用节点清单.md`：更新 StopOnDamagePopup 节点定义、新增 7 个停止节点、更新日志动作表格和复用检查清单

### 远征智能等待（SmartWaitAction）修复

**问题 1：GUI 日志面板无远征计时输出**
- `SmartWaitAction` 只调了 `LoggerHelper.Info()`（写 Serilog 文件），没有 `AddLog()`（GUI 面板）
- 等待期间完全静默，用户不知道倒计时状态
- **修复**：新增 `Log()` 方法同时写 Serilog + GUI 面板；输出开始消息、倒计时结束消息、手动停止消息

**问题 2：等待时间硬编码 600 秒**
- `SmartWaitAction.GetRefreshIntervalSeconds()` 从 `GlobalSelectOptions` 找 `RefreshInterval`，但它是远征任务选项不是全局选项，永远找不到，掉到硬编码默认 600 秒
- **修复**：改为从 `custom_action_param["interval"]` 读取，与 `ExpeditionTimerAction` 一致

**问题 3：独立远征任务未注入 interval**
- `MaaProcessor.CreateNodeAndParam` 只在合战场/地下城/陆联**同步远征**块内向 `E_SmartWait` 注入 `interval`
- 独立远征任务（`entry="Expedition"`）不走该分支，`pipeline_override` 中只有 `{"enabled":true}`，没有 `custom_action_param`
- **修复**：在 `CreateNodeAndParam` 末尾新增独立远征任务的 interval 注入逻辑

**问题 4：停止通知误报下一任务为"回本丸"**
- `StopOnDamageAction` 用 `TaskQueue.FirstOrDefault()` 获取下一任务名，但任务间自动插入的回本丸在队列头部
- **修复**：加过滤 `t => t.Name != "回本丸"` 跳过中间任务

### 时间格式优化
- `SmartWaitAction` 新增 `FormatSeconds()`，纯秒数 → `X分Xs` 格式

### 影响文件
- `_src/MFAAvalonia/Extensions/MaaFW/Custom/SmartWaitAction.cs`：重写日志输出 + interval 读取路径
- `_src/MFAAvalonia/Extensions/MaaFW/MaaProcessor.cs`：独立远征任务注入 `E_SmartWait.interval`
- `resource/base/custom/StopOnDamageAction.cs`：`FirstOrDefault` 过滤回本丸

### 远征智能等待 00:00:00 归队修复

**问题**：队伍剩余时间显示 `00:00:00`（已完成但未领取）时，`ParseRemainingSeconds` 返回 0，但 `0 > 0` 为 false，`ScanAndStore` 走 `Reset()` 清空追踪器。`SmartWaitAction` 掉到全间隔等待（如 50 分钟），队伍迟迟无法派遣。

**修复**：
- `ExpeditionTimeTracker`：移除 `> 0` 条件，0 秒也正常存入 `ExpeditionReturnTracker`
- `SmartWaitAction`：新增 `remainingSeconds == 0` 分支，`waitSeconds = 0`，不等直接返回让中枢检测归队弹窗（日志 `[远征计时] 检测到队伍已归队`）

### 王点匹配效率优化

**问题**：`S_CheckBossBeforeMarch` 的 next 列表 30+ boss 模板，每轮检测 ~1.5s。`S_选择时代` 只改了点击坐标，没有按时代过滤 boss 节点。

**方案**：在 `MaaProcessor.CreateNodeAndParam` 中读取 `S_选择时代` 的 `Index`，动态注入非本时代 boss 节点的 `enabled: false`。

- 39 个 boss/道中节点按时代前缀映射，只保留当前时代 4~8 个
- 行军检测从 ~1.5s 降至 ~0.2s（热缓存）
- 不改 `interface.json`，纯 C# 动态注入
- 设计文档：`docs/王点匹配效率优化.md`

### 行军检测加速

**问题**：点击战斗结果画面后，下一个画面就是行军，但需要等 `S_DetectWhereAmI` 中枢轮询 25 个节点才能命中 `S_IsMarching`，每轮 ~460ms，延迟 1-5 秒。

**修复**：`S_HasExpeditionReturn_Exp`、`S_IsBattleResult_Exp`、`S_IsSwordDropColor` 三个节点的 next 加入 `S_IsMarching` 作为首位。点击关闭战斗结果后优先检查行军，画面一出即刻命中，延迟从 1.5-5s 降至 0.5-0.6s。不影响 `S_DetectWhereAmI` 兜底。

### MaaFramework 更新至 v5.12.2

- 从 GitHub Releases 下载 `MAA-win-x86_64-v5.12.2.zip`，直接替换 `runtimes/win-x64/native/` 下 13 个原生 DLL + 2 个 .node 文件
- C# 绑定层保持 NuGet `Maa.Framework 5.8.0`（NuGet 未发布 5.12），编译验证 ABI 兼容（0 错误）
- 主要收获：v5.12.1 MuMu 模拟器搜索修复、v5.12.2 Batch OCR 缓存冲突修复
- 新增 `docs/MaaFramework更新准备.md`

## 卡死重启与全流水线 on_error 补全（2026-07-23）

### 远征收益数据修正
- 修正 10 条远征收益记录，木炭/钢材/冷却材/砥石列的值错位或对调
- 效率表改为绿色四档热力图：深绿（90%+）/ 中绿（70-90%）/ 浅绿（40-70%）/ 无色（后40%）

### 计时器检查逻辑修正
- `E_CheckTimerExpired` 从 `LR_IsEventPage` 移至 `LR_IsTeamSelect`，避免过场动画中计时器到期导致 `E_GoHome` 失败
- `LR_EnterEvent` next 首位加 `LR_IsTeamSelect`，优先检测部队选择页
- `LR_ClickTeam` post_delay 从 2000ms 降至 800ms

### 重伤节点重构
- `S_LogMarchingDamage2` → `S_LogMarchingDamage`
- `S_IsMarchingDamage1` → `S_IsMarchingDamage`
- `S_ConfirmReturnHome` 从 OCR 改为模板匹配 `合战场_确认返回本丸.png`
- 删除孤儿节点 `S_ConfirmCancelMarchingDamage2`、`S_HasExpeditionReturn_Title`
- 修复修刀链路的 `enabled: false` 遗漏：`S_ClickRepair`、`S_ConfirmRepair`、`S_ClickMenuFromRepair` 加入 interface.json 修刀选项
- 删除无识别节点的冗余 `on_error`，恢复纯路由节点的 `on_error`

### OCR 目录 → 模板匹配
- 全流水线 25 个 OCR "目录" 节点改为模板匹配 `目录.png`，ROI `[1194, 22, 65, 34]`
- 主枢纽扫描性能大幅提升（模板匹配 <5ms vs OCR 20-400ms）

### 删除江户城流水线
- 删除 `EdoCastle.json` 及相关 interface.json、README、docs 引用

### 新增部队记录页面检测
- Sortie、LRentaisen、Expedition 主枢纽首位新增 `*_IsTroopRecordPage`：模板匹配 `部队记录页面.png`，ROI `[577, 8, 130, 35]`，点击 `[63, 28, 17, 17]` 后退

### 卡死重启功能
- `RestartGameAction` 升级：从运行时配置读取 ADB 路径、序列号、MuMu 安装路径和实例编号
- 流程：杀 `MuMuPlayer.exe` → 等待重连 ADB（最长 60s）→ `monkey` 启动游戏
- GUI 开关 "卡死重启" 加入 global_option，子选项 "卡死等待时间"（默认 120s）和 "目标应用"（默认 `com.youzu.djlw`）
- 补全 `E_FlowerFallbackWait` 和 `FB_PostFallbackWait` 到禁用覆盖

### 全流水线 on_error 补全
- Sortie：14 个有识别节点 + 8 个路由节点
- Expedition：21 个有识别节点 + 14 个路由节点
- FlowerBrush：12 个有识别节点 + 9 个路由节点
- LRentaisen：5 个有识别节点 + 13 个路由节点
- Underground：14 个有识别节点 + 16 个路由节点
- Disassemble、Mix、GoHome 各少量路由节点
- S_DetectWhereAmI on_error → S_RestartGame（全扫描失败兜底）

### 其他
- 全部 JSON 格式化：roi/target/threshold 一行，action/next 短内容塌成一行
- CLAUDE.md 新增开发规范：禁止 target_offset、新增节点必须确认 on_error
- 新增 `docs/合战场_缺失on_error节点.md`
- 打包脚本加入 `DependencySetup_依赖库安装_win.bat`

## v0.7.0-alpha 发布（2026-07-24）

### 设计文档全面重写

- 远征、合战场、陆联、刷花四份设计文档全部基于当前代码重写，与实际 JSON 对齐
- 复用节点清单重写，补充全局选项涉及节点、跨流水线共用节点、新增流水线检查清单
- 新增多 next 短路风险排查文档、缺失 on_error 节点清单

### 卡死重启功能完善

- RestartGameAction 升级：从运行时配置读取 ADB 路径、序列号、MuMu 安装路径和实例编号
- 杀模拟器 → 等待重连 → 启动游戏全流程自动化

### 陆联「不购买门票」

- 新增复选框选项，勾选后令牌不足时跳过购买，直接结束任务
- LR_CloseTicketPopup 点击 [782,465] 先关闭弹窗再结束

### 节点重命名

- LR_ClickUndergroundInMenu → LR_ClickActivityInMenu
- LR_NavigateToUnderground → LR_NavigateToActivity

### 版本升级

- v0.6.1 → v0.7.0-alpha
- 发布 v0.7.0-alpha prerelease
- 新增 v0.7.0-alpha 更新公告（标注为开发者测试版本）

### 文档

- 更新 CLAUDE.md：术语规范，禁止 target_offset，每个新增节点必须确认 on_error

## 2026-07-24

### on_error 规范与补全

- 发现 `E_StartExpedition` 因缺失 `on_error` 导致远征任务超时失败（点击出阵后 OCR 始终未匹配"远征确认"弹窗，20s 超时直接终结任务）
- 根因澄清：`E_VerifyConfirmPopup` 有 `on_error`，但它在 `E_StartExpedition` 的 next 列表中从未匹配成功，其 `on_error` 从未激活。节点只有成为 cur_node 后执行失败才会触发自己的 on_error，在上级 next 列表中匹配失败不算
- 修正 CLAUDE.md 规范：纯 Click 节点非豁免条件，只要 next 指向了识别节点就必须加 on_error
- **Expedition.json**：`E_StartExpedition`、`E_UseTeamRecord_Step3_Rec1~5`、`E_FlowerCaptainClickSlot` 共 7 个节点补 on_error
- **Sortie.json**：`S_LoginRewardClick3`、`S_ClickHomeInMenu`（disabled）、`S_PreConfirmSupply`（disabled）补 on_error
- **LRentaisen.json**：`LR_CheckSwordDropAfterSpecial`（日志确认此节点 5s 超时导致任务失败）、`LR_ClickHomeInMenu`（disabled）补 on_error
- 新增 `docs/on_error使用规范.md`：两阶段原理、判断标准、多next短路说明、检查清单
- 删除过期文档：`docs/缺失on_error节点.md`、`docs/多next短路风险排查.md`、`docs/已隐藏任务.md`

### 巧克力/南瓜活动任务设计

- 新增 `docs/巧克力南瓜任务设计.md`：完整设计文档含主枢纽、活动进入链路、剪影更新链路（OCR "更新"）、部队选择与换队长、同步远征集成
- 节点前缀 `CP_`，模板 `巧克力南瓜.png`，流水线文件 `EventFarm.json`

### 日志清理优化

- `AppPaths.CleanupOldDebugLogs`：`Directory.EnumerateFiles` 补 `SearchOption.AllDirectories`，覆盖 `debug/on_error/` 等子目录下的截图
- 保留天数从 1 天改为 3 天
- 导出对话框时间范围移除"最近7天"选项，仅保留全部/24h/3天
- 日志轮转命名格式从 `yyyyMMdd_HHmmss` 统一为 MaaFramework 一致的 `yyyy.MM.dd-HH.mm.ss.fff`

### 远征排序与换队长

- `interface.json` 远征任务移至 task 数组末尾（合战场→陆联→刷花→刀解→习合→远征）
- 换队长方式默认值从"马匹筛选"改为"队内拖拽"
- `interface.json` 版本号修正：`0.7.0-alpha` → `v0.7.0-alpha`

### GoHome 管道修复

- `G_VerifyMenuDir`（TemplateMatch `菜单目录.png`）加入 `G_DetectWhereAmI` 主枢纽 next 列表，排在 OCR "目" 节点之前，避免 × 按钮干扰误识别

### 问题反馈问卷

- 问卷星问卷上线，收集版本/模拟器/分辨率/问题描述/复现频率/任务/设置/日志文件
- 日志导出步骤文档化：点击文件夹图标→勾选全部→保存→上传问卷

### 编译发布

- 全量编译 0 错误，打包 `MATR-v0.7.0-alpha.zip`，commit `494cdea`

## v0.7.0-alpha 优化中（2026-07-25）

### 远征智能调度关闭模式

- **设计文档**：`docs/远征智能调度_关闭模式设计.md`（v3）
- **需求**：智能调度关闭时，合战场每次回本丸直接检查远征（无计时），陆联选队时不主动回本丸
- **根因**：原设计中同步远征（task 层）的 pipeline_override 覆盖了智能调度（global 层）的同名字段，merge 顺序 `global < task` 导致 global 层修改不生效
- **解法**：不改 `interface.json` 结构，改为在自定义动作中按 `IsSmartSchedulingEnabled()` 判断分支

### 代码改动

- **`ExpeditionTimerAction.cs`**：`Run()` 中 `StartTimer` 和"倒计时开始"日志移入 `IsSmartSchedulingEnabled()` 分支；关闭时仅 `context.Click` 关闭面板
- **`ExpeditionTimerCheckAction.cs`**：`Run()` 输出"倒计时结束"日志前增加 `IsSmartSchedulingEnabled()` 判断，关闭时静默
- **`interface.json` — LR_同步远征**：移除 `LR_IsTeamSelect.next: E_CheckTimerExpired`，入口不再绑定计时器
- **`interface.json` — 智能调度开启**：新增 `LR_IsTeamSelect.next: E_CheckTimerExpired`，为陆联注入计时链路

### 测试结果

- 合战场 + 智能调度关闭：每次回本丸检查远征，无计时器日志 ✓
- 合战场 + 智能调度开启：行为不变，OCR 扫描 + SmartWait ✓
- 陆联 + 智能调度关闭：选队时不主动回本丸 ✓
- 陆联 + 智能调度开启：计时器到期后主动回本丸检查 ✓
- 独立远征：不受影响 ✓

### S_IsAnnouncementPopup 超时修复

- `assets/resource/base/pipeline/Sortie.json`：`S_IsAnnouncementPopup.next` 追加 `S_DetectWhereAmI`，弹窗不存在时回退到主识别枢纽，不再因 `reco_timeout` 超时导致任务失败

### interface.json 格式化

- 多行纯数字 `target` 数组压缩为单行格式，减少约 1285 行，commit `64df48f`

### pipeline_override 默认值未生效修复

- **现象**：远征「部队一→休息」设置后，合战场同步远征仍进入部队一的远征流程，`E_CheckTeam1` 未正确禁用。触发不稳定。
- **根因**：`ProcessOptions` 处理 select/switch 类型时，仅当 `selectOption.Index` 非 null 才合并 pipeline_override。用户从未主动更改过选项时 `Index` 保持 null，`default_case` 的 override 被跳过。
- **修复**：`Index` 为 null 时从 `interfaceOption.DefaultCase` 反查正确的 case index，确保默认值的 pipeline_override 始终生效。
- **涉及文件**：`MaaProcessor.cs` ProcessOptions select/switch 分支

### 修行书信识别

- 8 个任务主枢纽（Sortie/LRentaisen/Underground/FlowerBrush/Disassemble/Mix/GoHome/Expedition）+ `E_FlowerBattleHub` 新增 `IsTrainingLetter` 节点
- TemplateMatch `修行书信.png`，ROI `[1037,438,206,76]`，threshold 0.95，green_mask，命中后点击关闭回枢纽
- commit `43d8ca6`

### 打包发布

- 版本号 `v0.7.0-alpha`，空白目录解压测试正常

### 补充刀装流程断链修复（2026-07-27）

- **现象**：合战场勾选补充刀装后，刀装不足弹窗被检测到（`S_CheckEquipmentPopup`），但点击关闭后流程在 `S_PreCheckUseRecord` 处断链
- **根因**：`S_补充刀装` 的 pipeline_override 启用了 `S_PreCheckUseRecord` 但未启用 `S_PreCheckClickRecordN`（记录槽点击）和 `S_PreCheckUseRecordBtn`（使用记录按钮）。`S_选择部队` 各 case 只设置了 `next` 路由但未启用节点本身
- **修复**：
  - `S_选择部队` 5 个 case 各追加 `S_PreCheckClickRecordN.enabled: true`
  - `S_补充刀装` case 追加 `S_PreCheckUseRecordBtn.enabled: true`
  - `S_PreConfirmSupply` 加 OCR "部队记录" 验证，防止错误页面误触发
  - `S_StopOnEquipmentPopup` action 从 DoNothing 改为 Click，关闭弹窗再结束任务
- **附带清理**：删除 `S_IsTroopDepleted`/`S_RefillTroops`/`S_ConfirmRefill` 死代码（无任何选项启用）

### 全流水线登录奖励节点兜底（2026-07-26）

- 所有流水线 `IsLoginReward`（纯 Click 节点）的 `next` 补充 `DetectWhereAmI` 兜底，防止弹窗误触发或点击未关闭后卡死
- `Sortie.json`：`S_VerifyMenuOpened` 从 OCR 改为 TemplateMatch（菜单目录.png），提升菜单识别稳定性
- 数据查找表经验输入框 Maximum 从 7 位扩展为 9 位

### 合战场疲劳处理功能（2026-07-27）

- 新增 `S_疲劳处理` 下拉选项（无 / 刷花 / 停止），入口阈值 30
- 插入点：队内拖拽 `S_DragCaptain.next` 和点击换位 `S_ConfirmSortAsc.next` → `S_FatigueCheck`，由 `S_换队长` 的 pipeline_override 驱动
- 新增 57 个 `SF_` 前缀节点：主枢纽、弹窗恢复、菜单导航、时代/地域选择、部队选择、刀装弹窗检测、解散循环、疲劳检测、战斗循环、记录恢复、补充刀装
- `FatigueCheckAction.cs`：新增 `check_first` 模式，`reversed` 标志控制逻辑方向（SF_CheckFatigue 常态走 next 避免 on_error 截图）
- 刷花流程：`SF_Hub → SF_ClickMenu → 选时代→地域→1-1→选部队→解散至只剩队长→检测疲劳<100出阵1-1刷花→≥100记录恢复→导航回合战场`
- 补充刀装：`SF_CheckEquipmentPopup → SF_EqRefill_Step1→4 → SF_DetectWhereAmI`
- 涉及文件：`interface.json`、`Sortie.json`、`FatigueCheckAction.cs`、`合战场_疲劳处理_设计.md`
- commit `b231a4b`

### 疲劳处理测试修复（2026-07-27）

- **刷花子枢纽死循环**：`SF_DetectWhereAmI` 中含 `SF_ClickMenu`，OCR "目" 几乎所有页面都匹配导致无限循环。修复：从枢纽移除 `SF_ClickMenu`，改由 `SF_IsHome → SF_Hub → SF_ClickMenu` 触发，仅在确认本丸后导航
- **恢复链断链**：`SF_CheckFatigue` 达标后 `on_error → SF_UseRecord_Step1`，但 Step1 默认 disabled 未被 `S_疲劳处理` 启用。修复：刷花 case 启用 `SF_UseRecord_Step1` 和 `SF_EqRefill_Step1`
- **简化 enabled 控制**：`SF_UseRecord_Step3/4`、`SF_EqRefill_Step3/4`、`SF_NavigateBack` 去掉 `enabled: false`，默认常驻，仅由 Step1 入口控制
- **GUI 日志优化**：合战场疲劳处理日志前缀改为 `[合战场疲劳处理]`，OCR 重试间隔 200→100ms
- `SF_ClickRegion1_1` 点击坐标修正为 `[194, 213, 8, 1]`
- `S_疲劳处理` 添加描述"需与换队长同时开启方可生效"
- `S_ClickRegion.next` 首位加 `S_IsTeamSelect`，加快地域选择后进入部队选择
- 工具：`tools/compress_json.py` 压缩脚本（四元素数组 + 单键值对压缩为单行）
- commit `d70b2fd`，打包 `MATR-v0.7.0-alpha.zip`

### 疲劳处理路由修复（2026-07-27）

- **现象**：开启换队长 + 疲劳处理选"无"时，检测首位疲劳后任务失败
- **根因**：路由职责分裂——`S_换队长` 无条件将 `S_DragCaptain.next` 和 `S_ConfirmSortAsc.next` 改为 `S_FatigueCheck`，但 `S_疲劳处理` "无" 未启用 `S_FatigueCheck`。Disabled 节点被强制进入后，`return false` → `on_error → SF_Hub`（也 disabled）→ 失败
- **修复**：路由职责从 `S_换队长` 移到 `S_疲劳处理`
  - `S_换队长`：移除 `S_ConfirmSortAsc.next` 和 `S_DragCaptain.next` 覆盖
  - `S_疲劳处理` "刷花"/"停止"：新增 `S_ConfirmSortAsc.next` 和 `S_DragCaptain.next` → `S_FatigueCheck`
  - "无"时路由保持原始 `S_IsPreSortieConfirm`，完全不经过 `S_FatigueCheck`
- **设计文档同步**：`合战场_疲劳处理_设计.md` 路由表、插入点、修改清单更新

### FatigueCheckAction check_first 性能优化（2026-07-27）

- **现象**：刷花循环每轮 `SF_CheckFatigue` 耗时 ~2.7s，换队长入口 `S_FatigueCheck` 同样 ~2.7s
- **根因**：`check_first` 模式在进入自己的分支前，先执行了通用六位扫描（且用了错误的远征 ROI）+ retry 循环 + FindLowest + 日志，全部丢弃后再用正确的出阵 ROI 重新读
- **修复**：`check_first` 提前到 early return，仅 OCR 首位，跳过全部无用前置逻辑
- **日志优化**："首位疲劳低于阈值" → "首位疲劳低于30"

### SF_Hub / 卡死重启 覆盖补全（2026-07-27）

- **SF_Hub 无 on_error 兜底**：`SF_Hub.next` 仅 `SF_ClickMenu`，OCR "目" 失败时死循环 20s 超时。补 `on_error: ["SF_DetectWhereAmI"]`
- **卡死重启漏覆盖 SF 节点**：
  - `SF_FallbackWait` 未在 `卡死重启` pipeline_override 中禁用，补上
  - `SF_DetectWhereAmI` 未在 `卡死等待时间` pipeline_override 中设置 timeout，补上

### 道中/王点撤退节点补充（2026-07-27）

- 道中撤退新增 `S_MidRetreat_E4_R1`（4-1-1，ROI `[1080,242,10,15]`）和 `S_MidRetreat_E4_R2`（4-1-2，ROI `[1031,303,10,14]`）
- 王点撤退新增 `S_Boss_E5_R2`（5-2，ROI `[1080,242,10,15]`）和 `S_Boss_E6_R1_2`（6-1-2，ROI `[1103,224,11,16]`）
- 全部使用 TemplateMatch，threshold 0.98，`next → S_ClickRetreat`

### 重伤处理修刀并停止（2026-07-27）

- 原"修刀"选项改名为"修刀并继续"
- 新增"修刀并停止"选项：选刀后 OCR "修复"并点击，结束任务（不加速不回家）
- 新增 `S_ClickRepairandStop` 节点：OCR `[1159,588,83,44]` 匹配"修复" + Click，无 next 结束任务
- 修刀并停止通过 pipeline_override 覆盖 `S_CheckAccel.next → ["S_ClickRepairandStop"]` 改变路由
- 修刀并继续不改路由，默认走 `S_ClickAccel` 加速修复
- **注意**：终端节点（无 next）必须放在文件中其他有 next 节点之后，否则 MaaFramework 资源加载失败（`IMaaResource failed to load resources`）

### debug 日志容量清理优化（2026-07-27）

- `AppPaths.CleanupOldDebugLogs` 新增：debug 目录磁盘占用超 100MB 时，仅保留最新两条 `maafw.bak.*.log`
- 保留原有按日期清理（3 天）逻辑

## v0.7.0-beta.1：战术强化训练活动自动化（2026-07-31）

### 新活动流水线
- 新增 `TacticalTraining.json`（TT_ 前缀，57 节点）：战术强化训练（通称巧克力/南瓜）活动全自动出阵
- 活动页面识别：TemplateMatch `战术强化训练.png`，ROI [429,69,289,52]
- 剪影更新链路：`TT_CheckSilhouette`（OCR "更新"）→ `TT_IsSilhouettePopup` → `TT_ConfirmSilhouette`（ColorMatch 绿色按钮）→ 确认剪影更新
- 门票购买链路：`TT_TicketGate`（ColorMatch 灰色按钮）→ `TT_IsTicketReplenishPage`（OCR "更新令牌补充"）→ `TT_ConfirmTicketReplenish` → `TT_CloseTicketDialog`（3 连击关闭）
- 轮次完成判断：`TT_IsSwordDropColor` 命中后 `TT_CheckRoundComplete`（TemplateMatch 活动页面），标记一轮完成
- 换队长仅支持队内拖拽，`TT_DragCaptain` 由选项注入，不在流水线中常驻
- 部队选择支持五队全遍历回退，`TT_CaptainHub` 集成 ColorMatch 颜色检测判断部队可用性
- 导航回活动：`TT_IsHome → TT_NavigateToActivity → TT_ClickMenu → TT_PostClickMenuHub → TT_IsMenuDirectory → TT_ClickActivityInMenu`，与陆联一致

### GUI 选项
- 选择难度（初级/中级/上级，默认初级）
- 选择部队（一~五，默认一）
- 换队长（注入 `TT_DragCaptain`）
- 不购买门票（重路由 `TT_IsSilhouettePopup.next` 至 `TT_TerminateNoTicket`）

### 同步远征集成
- `TT_同步远征` pipeline_override，与陆联/合战场模式一致
- `MaaProcessor.cs` 新增 `TacticalTraining` 入口，自动合并远征任务队伍配置
- 远征智能调度注入 `TT_IsTeamSelect → E_CheckTimerExpired`
- 长期远征计划注入 `E_MarkTeamN → E_UseTeamRecordN`

### Pipeline 修复（多流水线）
- **远征** `E_UseTeamRecord_Step3_Rec1~5`：新增 `post_delay: 300`，next 回退到 Step2 重试，解决游戏卡顿导致确认弹窗未出现
- **合战场** `SF_UseRecord_Step3`：同样卡顿回退修复（next 自循环）
- **地下城** PreCheck 链路对齐合战场：新增 `U_PreCheckClickRecord1~5`、`U_PreCheckUseRecordBtn`，`U_PreCheckUseRecord` 改为纯路由，`U_PreConfirmSupply` 新增 OCR 识别

### 其他
- ColorMatch 格式修正：`TT_ConfirmSilhouette`、`TT_TicketGate`、`TT_CaptainHub` 从 `expected` 改为 `upper`/`lower` ±3
- 卡死重启从全局选项隐藏，功能保留
- 版本号升至 v0.7.0-beta.1
- 复用节点清单新增 TT_ 前缀
- 新增剪影识别小工具（SilhouetteView）和普图效率计算文档
- 战术强化训练设计文档替代旧巧克力南瓜设计文档

## v0.7.0：MFAAvalonia v2.13.0 升级与 4-1 道中撤退修复（2026-08-02）

### GUI 升级（MFAAvalonia v2.12.0 → v2.13.0）
- 同步上游 v2.13.0 全部源码更新（68 个文件），保留 MATR 七个自定义文件：
  - `TaskQueueView.axaml` / `.cs`：全选合并、布局调整、子页面导航
  - `TaskQueueViewModel.cs`：ToggleSelectAll、CloseSubPage、MuMu 自动修复
  - `TaskOptionGenerator.cs`：选项生成逻辑
  - `MaaProcessor.cs`：王点优化 + 道中撤退
  - `App.axaml.cs`：限锻/数据库 DI 注册
  - `AppPaths.cs`：CleanupOldDebugLogs
  - `RootViewContent.axaml`：限锻计算器 + 数据库侧边栏项
  - `EmulatorHelper.cs`：MuMu 模拟器支持
- 新增 `Markdown.Avalonia.Math` 子项目及 `SukiUI.Animations.FloatingTextExtensions`
- 适配上游 API 变更：`Win32ScreencapMethods` 复数形式、`AddTaskDialogViewModel` 三参数构造、`SetTasker` 无参化
- 修复 XAML 预编译失败：恢复 `AvaloniaResource` 通配符

### 框架升级（MaaFramework）
- `Maa.Framework` 5.8.0 → 5.10.0
- `Maa.Framework.Runtimes` 5.10.2 → 5.12.2
- 更新全部运行时 DLL（110 个），包括 `MaaFramework.Binding.dll`、`SukiUI.dll`、`Markdown.Avalonia.*.dll`

### 修复
- **4-1 道中撤退不生效**：`Sortie.json` 的 `S_CheckBossBeforeMarch.next` 补全 `S_MidRetreat_E4_R1`、`S_MidRetreat_E4_R2`；`MaaProcessor.cs` 王点优化 `bossNodePrefixes[3]` 添加 `S_MidRetreat_E4_` 前缀，`allBossNodes` 补全两个 E4 撤退节点
- **全局任务配置出现在任务列表**：`TaskLoader.cs` 移除 `CreateGlobalOptionItem` 到任务列表的添加逻辑，全局选项仅通过设置面板访问
- **侧边栏限锻计算器/数据库消失**：恢复 `RootViewContent.axaml` 中的两个 `SukiSideMenuItem`，恢复 `App.axaml.cs` 中的 DI 注册
- **初始化失败系列**：`AppPaths.cs` 恢复 `CleanupOldDebugLogs`；`TaskQueueView.axaml.cs` 恢复 MATR 版；运行时 DLL 全量同步避免类型加载异常

## v0.7.0 编译警告清零（2026-08-03）

### CS0618 过时 API（3 处）
- `ConnectSettingsUserControlModel.cs`：`Win32InputMethod.PostThreadMessage` 仍作为可选枚举值列出，`#pragma` 抑制（MaaFramework v5.9.0 废弃但需保留兼容）
- `TaskQueueViewModel.cs`：旧版 long 格式反序列化 switch 中 `PostThreadMessage` 同理抑制
- `VersionChecker.cs`：`ApplySecureUpdate` 调用点抑制，新的本地二合一更新链路未完成，旧方法仍在使用

### CS4014 缺少 await（4 处）
- `AboutUserControl.axaml.cs`：`CheckAnnouncement(true)` → `_ = CheckAnnouncement(true)`，event handler 无法 await
- `AgentHelper.cs`：三处 `TaskManager.RunTaskAsync` 加 `_ = ` 显式丢弃，均为后台 fire-and-forget 任务

### CS0067 未使用事件（1 处）
- `InstanceTabsControl.cs`：`SimpleActionCommand.CanExecuteChanged` 是 ICommand 接口必需，`#pragma` 抑制

### CS0414 死字段（1 处）
- `MaaProcessorManager.cs`：删除从未读取的 `_isLazyLoadingComplete` 字段及其 4 处赋值

## v0.7.0 on_error 全覆盖与 UI 修复（2026-08-04）

### on_error 全节点覆盖
- **631 个节点**补上 `on_error`，覆盖全部 9 个 pipeline 文件
- 统一指向所属枢纽（主枢纽 WhereAmI 或次枢纽 BattleHub），已有 `on_error` 的节点不动
- 枢纽映射：Sortie（`S_DetectWhereAmI` / `SF_DetectWhereAmI`）、Expedition（`Expedition` / `E_FlowerBattleHub`）、Disassemble（`D_DetectWhereAmI`）、GoHome（`G_DetectWhereAmI`）、Mix（`M_DetectWhereAmI`）、Underground（`U_DetectWhereAmI`）、TacticalTraining（`TT_DetectWhereAmI`）、LRentaisen（`LR_DetectWhereAmI`）、FlowerBrush（`FB_DetectWhereAmI`）
- 处理方式：正向 BFS 从枢纽遍历 `next` 链 + 反向追踪 + 命名前缀兜底，确保每个节点都有归属

### CLAUDE.md 规范修正
- 旧规则"已在主枢纽扫描链中就不需要 on_error"存在错误——实际上枢纽 chain 中的识别节点也可能超时失败
- 新规范：**每个节点都必须设置 `on_error`**，不写死指向目标，留判断空间

### 卡死重启 UI 修复
- `TT_FallbackWait` 补入"卡死重启" pipeline_override（此前唯一遗漏的 FallbackWait 节点）
- "卡死重启"加入 `global_option` 数组，GUI 全局选项列表可见

### 地下城远征智能调度优化
- `E_CheckTimerExpired.on_error` 从专用路径 `U_GoHomeForExpedition` 改为复用标准 `U_GoHomeAfterRound`
- `U_CheckRoundComplete.next` 精简为仅 `E_CheckTimerExpired`，分流完全由后者的 next/on_error 覆写处理
- 清理孤儿节点 `U_GoHomeForExpedition`，同步更新 `docs/复用节点清单.md`

### 任务列表复选框运行时禁用
- 问题：MFAAvalonia v2.13.0 升级后，任务运行期间复选框仍可点击勾选/取消
- 根因：`TaskQueueView.axaml` 第 1228 行 `IsEnabled` 仅绑定 `!IsResourceOptionItem`，未检查运行状态
- 修复：`DragItemViewModel` 新增 `IsCheckBoxEnabled` 属性，合并 `!IsResourceOptionItem && CurrentInstanceIdle`；构造函数订阅 `RootViewModel.PropertyChanged` 监听空闲状态变化；XAML 绑定切换到新属性

## v0.7.1 卡死重启 MuMu 12 兼容修复（2026-08-05）

### 问题
`RestartGameAction` 硬编码查找 `MuMuPlayer.exe`，MuMu Player 12 中该文件已移除，主程序改为 `nx_main/MuMuNxMain.exe`。导致模拟器重启被跳过，仅靠 ADB monkey 发送 intent 无法从卡死状态恢复，形成每 2 分钟一次的空转重启循环。

### 修复
`_src/MFAAvalonia/Extensions/MaaFW/Custom/RestartGameAction.cs`：

- **新增 `DetectMuMuEnvironment()`**：自动探测 MuMu 版本，MuMu 12+ 识别 `mumu-cli.exe`，旧版回退 `MuMuPlayer.exe`
- **MuMu 12+ 改用 CLI 重启**：`mumu-cli.exe control --vmindex <index> restart`，CLI 原生控制实例关闭+启动，无需杀进程，不会误弹出多开中心
- **旧版兼容保留**：`taskkill MuMuPlayer.exe` + `MuMuPlayer.exe -v <index>` 启动
- **新增 `am force-stop`**：在 monkey 启动前先强制杀死游戏进程，确保卡死的游戏被彻底终止而非仅仅带到前台

## v0.8.0 Mirror酱 接入与项目结构标准化（2026-08-05）

### Mirror酱 接入
- Mirror酱（MirrorChyan）提交 PR #4，MATR 获得专属 `res_id: MATR`
- `interface.json` 新增 `mirrorchyan_rid: "MATR"` 和 `mirrorchyan_multiplatform: true`
- 新增 `.github/workflows/mirrorchyan_release.yml`：Release 发布/编辑时自动上传到 Mirror酱
- 新增 `.github/workflows/mirrorchyan_release_note.yml`：release notes 自动同步
- 新增 `MirrorChyanUploadToken` Repository Secret
- README 新增 Mirror酱 下载渠道说明和致谢

### 项目结构对齐 MaaPracticeBoilerplate
- `interface.json` 和 `resource/` 移入 `assets/` 目录，对齐 MaaFramework 标准项目模板
- `AppPaths.cs` 路径常量同步更新（`ResourceDirectory`、`InterfaceJsonPath` 等）
- `SilhouetteViewModel` 修复硬编码路径，改用 `AppPaths.ResourceDirectory`
- `tools/pack.ps1` 适配新布局
- 新增 `.editorconfig`、`.prettierrc`、`.prettierignore`（VS Code 编辑器配置）
- 新增 `.github/workflows/check.yml`（PR 检查）
- 新增 `.github/PULL_REQUEST_TEMPLATE.md`、`.github/cliff.toml`（changelog 配置）
- `.vscode/settings.json` 配置 Prettier 为 JSON/Markdown 默认格式化器
- 删除根目录遗留 runtime 目录（`resource/`、`backup/`、`temp/`、`logs/`、`libs/`）
- CLAUDE.md、README.md 目录结构文档同步更新

### 设计文档全面重写
- **合战场**：补充 `SF_DetectWhereAmI` 副枢纽、疲劳处理、刀装破坏处理、同步远征计时
- **远征**：补充 `E_FlowerBattleHub` 副枢纽、计时器系统、五队串行流程、智能调度/长期远征计划
- **地下城**：完全重写（楼层选择、行军扁平化、动画跳过、连接中断处理）
- **战术强化训练**：全新文档（此前无设计文档）
- **刀解**：全新文档（筛选链路、解体循环、邮箱自动收取）
- **习合**：完全重写（搓糖机制、6 格轮询、刀种共享）
- **回到本丸**：全新文档
- **全局设置**：全新文档（换队长方式、远征智能调度、长期远征计划、卡死重启的 pipeline_override 详解）
- **数据工具**：全新文档（限锻计算、数据查找表）
- **复用节点清单**：更新全局选项节点表、修复 on_error 规则
- 陆联、刷花：on_error 覆盖修正
- 全部文档路径统一为 `assets/` 前缀

### Bug 修复
- `TT_DetectWhereAmI` 补入卡死等待时间 timeout 覆盖（此前遗漏，战术训练枢纽无法触发卡死重启）
- 陆联/地下城/刷花文档中过时的 on_error 规则修正（"next 列表中节点不设 on_error" → 统一指向主枢纽）

### 图标
- 更换 MATR 新图标（透明底 PNG → 多尺寸 ICO），含裁剪优化
- 更新 `resource/logo/`、`_src/MFAAvalonia/Assets/logo.ico`、`_src/MFAUpdater/logo.ico`
- README logo 引用路径更新

## v0.8.0-beta.1 发布准备（2026-08-06）

### 资源更新验证修复
- `ValidateExtractedResourcePackage` 增加 `assets/` 子目录回退检测，兼容新结构
- `IsDataRootRelativePath`（VersionChecker + PendingUpdateDeletionHelper）增加 `assets/resource/` 前缀
- `interface.json` 资源路径修正为 `{PROJECT_DIR}/assets/resource/base`
- `CheckInterface()` 默认生成路径同步更新
- pack.ps1 修复 Root 定位和 zip 内目录结构
- 解压测试通过，Mirror酱 更新可用

### 远征智能调度注入范围修复
- **问题**：不开启同步远征、仅开启远征智能调度时，未开同步远征的任务（如战术强化训练）仍会检查远征倒计时并回本丸查看远征状况
- **根因**：「远征智能调度」全局选项的 pipeline_override 被无条件合并到所有任务（`MergeGlobalOptionParams`），把 `TT_IsTeamSelect`/`LR_IsTeamSelect`/`U_CheckRoundComplete` 的 `next` 指向 `E_CheckTimerExpired`；而计时器仅由 `E_TimerStart` 启动（只在同步远征开启时注入），未启动时 `ExpeditionTimerRecognition.IsExpired()` 判定为已过期 → 走 `on_error` → `E_GoHome` 回本丸 → `E_SmartWait` → 跳转远征流程，任务被劫持
- **修复**：`MergeGlobalOptionParams` 增加注入范围过滤——「远征智能调度」仅对远征任务自身（`entry == "Expedition"`）与开启同步远征的任务生效，其余任务跳过该选项合并
- 已实机验证：开启/不开启同步远征两种组合行为均符合预期

### 打包体积优化
- `tools/pack.ps1` 打包时移除 `libs/` 中与 `win-x64/native/` 同名的原生库（libSkiaSharp 11M、av_libglesv2 5.2M、libHarfBuzzSharp 1.8M、uiohook 680K，共 18.7M，md5 已验证内容一致），运行时实际加载 native 目录下的同名文件
- zip 体积 114MB → 106MB

## v0.8.0 正式版：资源版本更新与仓库清理（2026-08-06）

### 资源版本更新
- `assets/interface.json`：`version` v0.8.0-beta.1 → v0.8.0，`custom_title` 同步
- 发布公告标题与内容更新为 v0.8.0

### 仓库清理（.gitignore 完善）
- `.gitignore` 新增：`/backup/`、`/temp/` 运行时目录（此前 CLAUDE.md 声明已排除但实际缺失）、`**/bin/` `**/obj/` 构建产物、SdkResolver 临时备份、IDE 个人设置、根目录发布产物、`/assets/config/` 本地运行配置
- 移除 git 跟踪共 358 个文件（磁盘全部保留，打包不受影响）：
  - 根目录发布产物（`matr.exe`、`MATR.dll` 等 5 个）与 `runtimes/`（343 个）：用户通过 GitHub Releases 下载 zip 分发，仓库内二进制无实际作用；上游 MFAAvalonia 同为纯源码仓库，仅跟踪依赖声明，依赖经 NuGet 自动还原
  - `.idea/`（7 个，含本机窗口布局）、`*.DotSettings.user`、`*.pubxml.user`：个人/机器相关 IDE 配置
  - `assets/config/maa_pi_config.json`：含本机 MuMu 路径，程序代码不读取
- 若后续有人加入开发：clone 后 `dotnet build` 即可编译，运行所需 `runtimes/` 需自行准备（从 Release zip 解压）

### 打包
- `MATR-v0.8.0.zip` 打包验证通过（101MB，523 条目，关键内容齐全）

## v0.8.1-alpha：移除按钮模板匹配、Pipeline 标准化与全局选项持久化修复（2026-08-06）

### 移除队员按钮检测改为模板匹配
- **SF_DissolveCheck / E_FlowerDissolveCheck / FB_DissolveCheck** 三个节点从 ColorMatch（竖条深色像素检测，ROI [1075, 211, 36, 420]，RGB 40–46）改为 TemplateMatch `Common/移除队员.png`
- 背景：原 ColorMatch 检测深灰 (43,43,43) 像素过于粗糙，滚动条、阴影等常见深色元素都会误触发
- 模板图为 26×26 深灰底浅色 X 图标（队员移除按钮，全不透明无绿色），ROI 调整为 [1074, 211, 37, 421]，threshold [0.98]
- **不设 green_mask**：模板无绿色，若开启 green_mask，浅灰 X 笔画 G 通道 243 会被误判为透明，只剩深灰方块可匹配，失去图案信息
- 点击位置 = 模板匹配框中心（X 按钮中心），随按钮在竖条内上下移动，精确跟随随机出现的移除按钮

### Pipeline 标准化脚本
- 新增 `tools/normalize_pipeline.py`：统一节点结构形态与键顺序
  - recognition / action 归一化为对象形态（字符串 `"OCR"`、扁平 `{"type","roi",...}` → `{"type", "param": {...}}`），节点层散落的 `target`、`custom_action`、`custom_action_param` 归入 action 对象
  - 顶层键标准顺序：`enabled, recognition, action, pre_delay, post_delay, timeout, next, on_error`（on_error 恒定最后）
  - recognition.param 键序：roi 恒第一，按识别类型固定（TemplateMatch / OCR / ColorMatch）
  - 输出格式复用 compress_json.py（2 空格 + 单行压缩规则），支持 `--dry-run`，幂等
- 共 **642 个节点**被调整（9 个 pipeline 文件），语义等价性校验通过（962 节点归一化前后叶值集合完全一致，无数据丢失）
- 修复过程中发现并处理旧式写法：字符串 action + 节点层 `target`（如 `E_LeaveTroopRecord`、`D_HasExpeditionReturn_Exp`），已归入 `action.param`

### 全局选项子选项持久化修复
- **问题**：全局设置齿轮子页面（子选项）中的设置无法保留，重启后回到默认值
- **根因**：`TaskLoader.cs` 的 `InitializeGlobalSelectOptions` 从配置恢复全局选项时只拷贝 `Name / Index / Data / SelectedCases`，**漏掉 `SubOptions`**；对照的 `InitializeResourceSelectOptions` 分支有 `CloneSubOptions`，全局分支缺失
- **证据**：配置文件 `config/instances/default.json` 中 `GlobalOptionItems` 已正确持久化 sub_options（疲劳阈值 91、卡死等待 120s 等），保存侧正常，仅加载侧丢失
- **修复**：全局选项恢复分支补上 `SubOptions = saved.SubOptions != null ? CloneSubOptions(saved.SubOptions) : null`
- 已实机验证：修改子选项 → 重启 → 值保留

### 版本与发布
- 资源版本号 v0.8.0 → **v0.8.1-alpha**（`interface.json` version 与 custom_title 同步）
- `dotnet publish` 发布并拷贝产物：`MATR.exe` / `MATR.dll` / `runtimes/libs/MFAAvalonia.Core.dll`

## v0.8.1-alpha：修复 4-1 道中撤退不生效（2026-08-07）

### 4-1 道中撤退修复
- **问题**：4-1 速刷委托符的道中撤退无法生效，勾选「道中撤退」后不触发
- **根因**：`S_MidRetreat_E4_R1` / `S_MidRetreat_E4_R2` 节点在 Sortie.json 中已定义且被 interface.json 正确启用，但未挂载到 `S_CheckBoss` 枢纽的 next 列表（同图王点节点与 E6/E8 道中节点均在列，唯独遗漏），节点不可达
- **修复**：`S_CheckBoss` next 列表补挂两个节点（紧跟同图王点节点，保持时代分组顺序）
- **节点命名统一**：`S_MidRetreat_E4_R1` / `S_MidRetreat_E4_R2` → `S_MidRetreat_E4_R1_1` / `S_MidRetreat_E4_R1_2`（4-1 图第一点位的两个变体，与 `S_Boss_E5_R3_1` 等多模板命名风格一致），interface.json 启用项同步更新
- 已实机验证：`S_CheckBoss` 列表正确加载新节点；未勾选选项时节点正常禁用，勾选后进入识别流程
- 版本号不变（v0.8.1-alpha）

## 日志切块与打包优化（2026-08-07）

### 背景
- MaaCore 日志（`debug/maafw.log`）运行期间持续追加不切块，单文件可达上百 MB，占磁盘且难以排查
- 原轮转机制仅在启动时执行（`AppPaths.CleanupOldDebugLogs`），运行数小时后单文件巨大

### 运行期切块（MaaLogRotator）
- 新增 `_src/MFAAvalonia/Helper/MaaLogRotator.cs`：后台每 30 秒轮询 `maafw.log` 大小，超过 20 MiB 执行切块
- 切块利用 MaaCore 官方 API `SetOption_LogDir` 切换日志目录：切换后旧文件句柄关闭 → 旧日志移动为 `maafw.bak.{时间戳}.log` → 切回原目录继续写
- 切换间隙的过渡日志直接追加合并进主 bak 末尾（时间顺序自然连续），不产生小碎文件
- 任务停止时（`MaaProcessor.ExecuteStopCore`）执行一次兜底切块
- 切回失败时记录当前日志目录，轮询持续追踪保持切块能力；并发与异常均有防护（SemaphoreSlim 互斥、全路径异常消化）

### 保留策略调整
- debug 目录磁盘占用超 **500MB** 才触发清理（原 100MB）
- 清理时保留最新 **10 条** bak 日志（原 2 条）与最新 **50 张** on_error 截图（原 5 张）
- 3 天时间保留机制不变

### 导出分卷
- 导出 zip 分卷阈值由 24.5MB 收紧为 **20 MiB**（`FileLogExporter.MaxArchiveVolumeBytes`），每卷不超过 20 MiB

### 验证
- 前置验证程序实证 MaaCore `SetOption_LogDir` 切换后旧文件可安全移动、新目录正常写入
- 实机验证：日志稳定按 20 MiB 切块、过渡日志正确合并、temp 零残留、多轮循环正常
- 导出验证：344MB 原文日志导出为 2 个分卷（19.4 MiB + 12.5 MiB），均不超 20 MiB

### 版本与发布
- 版本号不变（v0.8.1-alpha，本次为程序功能改动，资源未变）
- `dotnet publish` 发布并拷贝产物（`MATR.exe` / `MATR.dll` / `runtimes/libs/MFAAvalonia.Core.dll` 等）




## 枢纽重排与规范更新（2026-08-07）

### 枢纽 next 列表重排
- 调整全部主/次枢纽的 next 排列顺序：低频异常类 node 整体后移，高频识别 node 提前
- 涉及 **11 个枢纽**、9 个 pipeline 文件：
  - Sortie.json：`S_DetectWhereAmI`（主）、`SF_DetectWhereAmI`（道中次）
  - Expedition.json：`Expedition`（主）、`E_FlowerBattleHub`（刷花次）
  - Underground.json、FlowerBrush.json、GoHome.json、LRentaisen.json、Mix.json、Disassemble.json、TacticalTraining.json 各 1 个主枢纽
- **后移的三类低频 node**（相互间保持原相对顺序）：
  - 公告：`*_IsAnnouncementPopup`、`*_IsTrainingLetter`
  - 登录奖励：`*_IsLoginReward`
  - 闪退恢复：`*_IsGameIcon`、`*_IsLoginButton`、`*_IsGameUpdatePopup`、`*_IsInGameUpdatePopup`、`*_IsInternalReport`
  - 关联异常：`*_IsConnectionInterrupted` ×3（连接中断恢复）、`U_IsTutorial`（教程弹窗，与公告同类）
- `*_FallbackWait` 兜底保持列表最末（其后的 node 无法到达）
- 其余 node（行军、远征归来、战斗结果、掉落、编队/时代/地域选择、菜单目录等）保持原相对顺序前移
- **效果**：合战场掉落识别 `S_IsSwordDropColor` 由第 9 位提前至第 6 位，命中轮次不再先跑 2 个公告类模板匹配与 4 个 OCR；远征归来 OCR 全部置于首位
- 保留未动：`TT_IsSilhouettePopup`、`TT_IsTicketReplenishPage`、`LR_IsSupplyPopup`、`LR_IsTicketPopup`、`LR_IsReplenishDone`（功能弹窗，非三类）
- 语义校验：全部 JSON 解析通过，各枢纽 node 集合无增删，FallbackWait 均保持最后

### 掉落画面识别延迟调查
- **现象**：合战场运行中感知「识别到掉落画面很慢」
- **排查**：对照 `debug/maafw.log` 与 20 余段轮转日志
  - 色条 ColorMatch `[598,543,66,1]` 在历史日志中多次成功命中（count=66 全量匹配），配置本身无问题
  - 识别失败仅继续 next 列表下一项，不中断、不重试，循环结构无浪费
  - 识别轮次约 500ms（引擎 interval），ColorMatch 单次 0ms
  - 单段日志（20:48~20:49）全程 count=0 的原因：该段画面未出现刀剑掉落横幅，属正常画面状态
- **结论**：识别链路无优化空间，「慢」的感知主要来自游戏掉落横幅滑入动画（约 2-3 秒，不可加速）与引擎 500ms 轮询，横幅到位后最迟 1 秒内命中，已是引擎可达最快

### CLAUDE.md 规范更新
- 编码风格（JSON）新增：禁止使用中文「节点」二字，统一使用英文 node；层级描述使用 parent node / child node / sibling node
- CLAUDE.md 全量英文化（结构、路径、命令、协议术语不变，说明文字全部转英文）
- 该要求同时作为 AI 执行 CLAUDE.md 的检查点（对话、文档、注释一律使用 node）

### 版本与发布
- 未发版；资源版本号未变动（重排仅调整 next 顺序，是否递增待确认）
## 地下城刷花状态机与流水线优化（2026-08-08）

### UF_ 刷花状态机新增（57 node）
- 严格复刻 Sortie.json 的 SF_ 系列：核心链 26 node + 附属链 31 node（使用记录 8 / 装备补充 8 / 弹窗识别 5 / 图标与启动 5 / 主界面 2 / 远征归来与活动检测 3），合计 57 个 UF_ node 与 57 个 SF_ node 一一对应
- 引用严格复刻：UF_DetectWhereAmI.next 与 SF_DetectWhereAmI 前缀替换后逐项一致（含 UF_HasExpeditionReturn_Exp/Title、UF_IsSortieActivity）；出口例外 UF_NavigateBack.next → U_DetectWhereAmI（回到地下城主流程）
- 识别参数逐字段一致：recognition（ROI/模板/颜色/threshold）、action.param、timeout、enabled 与 SF_ 对应 node 完全一致（Task 7 全量比对 0 差异）
- 附加字段一致：26 个 post_wait_freezes、UF_DetectWhereAmI pre_delay 0 与 SF_ 对应 node 逐项相等
- 前缀无残留：Underground.json 全文无 SF_ 前缀引用

### 入口/出口链
- 入口：U_FatigueCheck（FatigueCheckAction，mode=check_first，threshold=30）检测疲劳；疲劳 <30 → on_error UF_Hub 进入刷花状态机；疲劳达标 → next U_IsPreSortieConfirm 直接出阵
- U_DragCaptain.next 默认恢复 [U_IsPreSortieConfirm]（与 S_DragCaptain 对齐，消除 disabled node 链路风险）；刷花/停止 case 经 interface.json override 覆盖为 [U_FatigueCheck]
- 刷花 case 补 U_ConfirmSortAsc.next → [U_FatigueCheck]：点击方式换队长（马匹/刀装筛选）路径同样接入疲劳检测
- 出口：刷花完成链 UF_UseRecord_Step4 → UF_NavigateBack → U_DetectWhereAmI 回到地下城主流程

### 选项接入
- U_疲劳处理-刷花：启用 U_FatigueCheck/U_FatigueDetect/UF_Hub/UF_UseRecord_Step1/UF_EqRefill_Step1 + U_ConfirmSortAsc/U_DragCaptain next 覆盖 → 部队选择页疲劳 <30 → UF 导航合战场 1-1 → 刷花循环（行军/战斗/掉落）→ 疲劳满 → 回地下城继续
- U_疲劳处理-停止：U_FatigueCheck enabled + on_error 覆盖为空（疲劳不达标不进入 UF_Hub，语义 = 疲劳时停止任务）；U_FatigueDetect 行军检测保持
- U_换队长：U_DragCaptain enabled；挂载由「换队长方式」全局选项的队内拖拽 case（覆盖 U_CaptainHub.next）承担，与 S 系同构
- U_选择部队-部队一至五：UF_ClickTeamN target 覆盖（对应部队坐标）+ UF_UseRecord_Step2_RecN/UF_EqRefill_Step2_RecN enabled（记录恢复与装备补充链接入选择部队流程）

### delay→freeze 迁移（86 node）
- pre_delay 清除 65 个（100×61、1000×2、500×2、200×1）；保留 pre_delay 0 ×5（U_DetectWhereAmI、U_CheckBeforeMarch、U_DetectAfterSortieNow、U_PostSortieHub、UF_DetectWhereAmI，与 S_DetectWhereAmI 模式一致）
- post_delay → post_wait_freezes 转换 12 个，值参照 Sortie.json 同构 node（U_CheckEquipmentPopup、U_ClickCaptain1-4/Slot/Descending、U_ClickMenuForRepair、U_ConfirmCancelPreDamage2、U_ConfirmRepair、U_EnterUnderground 等）
- post_delay 移除 8 个（S 同构无 freeze）：U_ClickFormation1、U_IsGameIcon、U_IsLoginButton、U_IsGameUpdatePopup、U_IsInGameUpdatePopup、U_NavigateToRepair、U_ConfirmReturnHome、U_NavigateToHomeFromStop
- 补 freeze 37 个（原无 post_delay、S 同构有 freeze）：U_IsAnnouncementPopup、U_IsLoginReward、U_LoginRewardClick2/3、U_ClickTeam、U_ClickSortieNow、U_ConfirmSortAsc、U_PreCheckClickRecord1-5 等，值与 S 同构 node 完全一致
- 有意保留 U_WaitRefresh post_delay 3000（S_WaitRefresh 亦保留）；UF_ 系列无任何 delay 残留
- 迁移后全文件残留：仅 5 个 pre_delay 0 + U_WaitRefresh post_delay 3000

### 冗余清理
- 删除 U_ConfirmCancelMarchingDamage2：v0.4.4（b23a472）曾由「重伤」识别 node 引用，重构后引用断开成为永久闲置（无任何内部/外部引用）；删除后无缺失引用，node 总数 214 → 213

### 全量语义校验（Task 7）
- SF_/UF_ 一一对应 57/57，无独有项；recognition/action.param/timeout/enabled 0 差异
- next/on_error 拓扑：SF_→UF_、S_→U_ 前缀替换后与 SF_ 对应 node 完全一致（57 node 0 差异）
- 引用完整性：Underground.json 213 node 的 next/on_error 0 缺失；interface.json 289 个 pipeline_override 引用全部有效（U_/UF_ 前缀 0 缺失）
- on_error 覆盖：识别 node 0 缺失（U_RestartGame/UF_RestartGame 为既有约定例外——最终兜底动作 node，无 recognition）
- target_offset：0 处；用词规范检查通过；JSON 有效；UTF-8 无 BOM、LF 换行

### 实机验证清单（待实机验证）
1. U_疲劳处理-刷花 + U_换队长 开启：部队选择页疲劳 <30 → UF 导航合战场 1-1 → 刷花循环（行军/战斗/掉落）→ 疲劳满 → 回地下城继续
2. U_疲劳处理-停止：疲劳 <30 → 回本丸停止（原行为不变）
3. U_刀装破坏处理各 case：停止/补充刀装/刀装保护/补充+保护 行为正确
4. 对照 debug/maafw.log：无短间隔重复点击、无卡死循环

### 版本
- 资源版本号 **v0.8.1-alpha → v0.9.0-alpha**（新增 UF_ 状态机 58 个 node，按 SemVer 规则为次版本号级别）

### 审查与测试安排
- 以 subagent-driven 方式执行：7 个任务逐任务审查通过（spec 合规 + 质量），1 轮 fix（刷花出口链节点启用 + U_选择部队 case 补全），最终 whole-branch 审查结论「可合并」，无 Critical 问题
- 实机测试待地下城活动开放后执行（当前活动未开放）：
  - 刷花模式（U_疲劳处理-刷花 + U_换队长）：疲劳 <30 → 合战场 1-1 刷花循环 → 疲劳满回地下城
  - 停止模式行为确认（疲劳 <30 时任务挂起语义，对齐 S 系，需验证无卡死）
  - 刀装破坏处理各 case（停止/补充刀装/刀装保护/补充+保护）

## v0.9.0-beta.2（2026-08-09）

### 卡死重启机制修复

#### 修复：RestartGameAction 重启动作崩溃中断
- 问题：adb 检查循环中 `WaitForExit(5000)` 超时后直接访问 `ExitCode` 抛 `InvalidOperationException`，导致整个重启流程中断，force-stop 游戏与 monkey 启动游戏均未执行，画面停留在桌面
- 新增 `WaitForAdbReady`：用 `HasExited` 判断后再读 ExitCode，超时继续重试（最多约 3.5 分钟），不再中断
- `RestartEmulatorViaCli`：读取 CLI 退出码与输出，超时先 Kill；CLI 重启失败自动回退旧版 taskkill + MuMuPlayer.exe 方式
- `DetectMuMuEnvironment`：CLI 环境下顺带探测 MuMuPlayer.exe，为回退做准备
- 新增 `RunAdbCommand`：force-stop / monkey 命令异常只记日志不中断流程

#### 修复：回本丸任务不应用全局选项 override
- 问题：任务队列插入的回本丸任务以 `"{}"` 参数启动，卡死重启禁用的兜底节点对其无效，卡死时 G_FallbackWait 无条件命中导致 G_DetectWhereAmI 的 2 分钟 timeout 永不触发，无限空转
- 新增 `BuildGoHomeParam`：回本丸任务合并全局选项 override（卡死重启等），以 Entry=GoHome 排除「远征智能调度」；G_FallbackWait 恢复启用，正常画面切换等待不受影响
- 生效后：卡死时回本丸 2 分钟超时 → G_RestartGame 自动重启游戏

#### 优化：合战场失败自动重跑（挂机不中断）
- 问题：合战场配置 `repeat_count: -1`（无限）因 `Repeatable=false` 被压成单次执行，任务失败即结束
- `CreateNodeAndParam`：`repeat_count < 0` 直接映射为无限循环（MFATask 转为 int.MaxValue），失败后自动重新提交任务，配合重启恢复实现挂机不中断
- 注意：合战场无限循环时，队列中排在后面的任务（如远征）不会执行，远征需依赖合战场「同步远征」

### 数据勘误

- 远征 4-4 单次木炭收益修正：250 → 225（用户游戏内实测）。佐证：合战场 4-3 实测数据按资源点 65 点整除性校验，修正后木炭产出 5005 = 65 × 77 恰好整除，原值不整除
- 涉及文件：`_src/MFAAvalonia/ViewModels/Pages/DataLookupModels.cs` 远征收益表

#### 优化：时代选择流程鲁棒性（修复刷花错误进入 6-1）

- 问题：时代选择点击后未验证是否生效，界面仍停留在错误时代时直接进入地域选择，导致刷花错误进入 6-1（而非 1-1）
- 时代点击节点增加 200ms 硬编码延迟：`S_ClickEra`（8 时代共用）、`SF_ClickFirstEra`、`UF_ClickFirstEra`（`FB_ClickFirstEra`、`E_FlowerClickFirstEra` 已有 100+500ms 延迟，未动）
- 新增时代验证机制：各任务 `XX_IsRegionSelect` 的 next 改为 `[XX_VerifyEra, XX_ClickBackToEra]`
  - `XX_VerifyEra`：OCR [831, 80, 26, 26] 识别地域选择页显示的时代序号，命中（时代正确）→ 继续点地域；未命中 → 走 `XX_ClickBackToEra` 点击返回按钮回时代选择页重选
  - `XX_ClickBackToEra`：纯点击 [127, 77, 26, 28]，next 回各任务检测枢纽
- 覆盖 5 条链路：合战场 `S_VerifyEra`（expected 由「选择时代」8 个 case 注入，时代五含 OCR 变体 "h"、时代一含 "l"/"1"）、合战场刷花 `SF_VerifyEra`、刷花 `FB_VerifyEra`、地下城刷花 `UF_VerifyEra`、远征刷花 `E_FlowerVerifyEra`（后四者固定去时代一，expected ["一", "l", "1"]）
- 实机验证生效

## v0.9.0 正式版：卡死重启机制全面完善（2026-08-11）

### 事故背景

- 2026-08-11 模拟器连续运行约 27 小时后安卓系统层冻结：游戏画面静止，`E_FlowerIsGameIcon` 持续命中（score 1.0），流水线以 ~350ms/轮无限循环约 20 分钟，枢纽 timeout 每次重新计时永不触发，直至手动停止
- 复盘发现卡死存在两种既有机制均无法覆盖的形态：
  1. **冻结但命中识别节点（循环形态）**：识别命中→执行→跳回枢纽→timeout 重置，枢纽 timeout 永不累计
  2. **模拟器无响应（挂起形态）**：截图请求挂起不返回，识别循环停摆；timeout 检查依赖识别循环执行而停摆，截图既不成功也不失败，ScreencapFailure 检测同样不计数

### 新增：画面冻结循环检测（LoopDetector）

- 新增 `_src/MFAAvalonia/Helper/LoopDetector.cs`：统计连续相同的动作键（节点名+动作类型+目标坐标）
- 连续 200 次相同动作 → 二次确认再累计 50 次（期间动作变化则放弃）→ 判定画面冻结；触发后静默、Reset 后恢复计数；Feed/Reset 加锁
- 接入 `MaaProcessor.HandleCallBack` 的 `Node.Action.Succeeded` 分支，仅「卡死重启」开启时生效；阈值硬编码 200/50，不做配置项

### 新增：模拟器无响应检测

- 判定条件：任务运行中 + 连续 120 秒无任何 Maa 回调 + 不在 SmartWait 等待窗口 → 判定模拟器无响应
- 新增 `SmartWaitTracker`：`SmartWaitAction` 等待开始/结束时记录窗口（try/finally 保证停止、异常路径也清除），排除远征等待的合法静默（最长 `min(归队剩余, RefreshInterval=3600s)`）
- `MaaProcessor.LastCallbackTime` 记录最后回调时间；`TaskQueueViewModel` 启动 10s 轮询检查循环，触发后复用自动恢复编排

### 新增：强制重启兜底

- 模拟器无响应时 mumu-cli 重启必然超时（依赖挂起的设备响应），旧版回退 `MuMuPlayer.exe` 在 MuMu 6.0 不存在
- `RestartGameAction` CLI 失败回退链扩展：旧版方式不可用 → `taskkill /F` 强杀 `MuMuNxDevice.exe`（无响应进程也能强杀）→ `mumu-cli control launch --vmindex {n}` 重启实例 → `WaitForAdbReady`
- `RestartEmulatorLegacy` 改为返回 bool 供回退链判断

### 修复

- 修复自动恢复完成后立即重复触发的问题（恢复风暴）：恢复流程开始时 `ResetLastCallbackTime`，恢复流程自身耗时（CLI 30s 超时等）不再被误判为无响应
- 修复模拟器无响应时无法自动重启模拟器的问题（强制重启兜底，见上）

### 自动恢复编排（统一复用）

- 触发后统一执行：停止任务 → 重启模拟器（CLI → 旧版 → 强杀三级回退）→ force-stop + monkey 重启游戏 → `ReconnectAsync` → `StartTask` 重新启动任务队列 → 复位检测
- 恢复语义：**整队重新开始**（非续跑），界面勾选的全部任务重新执行；对无限循环任务（合战场 `repeat_count: -1`）两语义等价
- 用户可见说明同步：interface.json「目标应用」选项说明、docs/全局设置设计.md「无响应检测」「恢复语义」小节

### 实机验证（挂起 MuMuNxDevice 进程）

- 挂起后约 100 秒无响应检测触发 → CLI 超时 30s → 强制路径强杀+重启 → adb 就绪 → 游戏重启 → 重连 → 合战场自动续跑，全程约 2.7 分钟无人工干预
- 验证过程中暴露并修复：挂起形态 timeout 不触发（timeout 检查依赖识别循环）、恢复风暴（LastCallbackTime 未重置）
- 正常挂机 30+ 分钟零误报；SmartWait 窗口排除生效

### 版本

- 资源版本号 **v0.9.0-beta.3 → v0.9.0**（正式版发布）：卡死重启完善与 beta 版本更新合并为 v0.9.0 更新公告，删除 3 个 beta 公告；JSON 资源压缩

## v0.9.1 正式版：修复远征派遣读取实例配置（2026-08-11）

### 修复

- 远征派遣自定义动作 ExpeditionMapSelectAction 原先从 appsettings.json 读取 Instances.LastActiveName（实例显示名）拼接实例配置文件路径，而实例配置文件实际按实例 UUID 命名，导致：
  - 多实例环境下读取失败后回退 default.json，按默认实例的远征地图派遣（如「配置 2」设置 1-2 实际派出 2-1）
  - 删除默认实例后回退落空，报「找不到实例配置文件」直接跳过派遣返回本丸
- 改为读取 MaaProcessorManager.Instance.Current.InstanceId（当前激活实例 UUID）定位 config/instances/{uuid}.json，与 InstanceConfiguration.GetConfigFilePath() 保持一致；UUID 缺失时回退 default.json

### 版本

- 资源版本号 **v0.9.0 → v0.9.1**（正式版发布）：修复远征派遣配置读取，同步更新公告与打包脚本版本

## v0.9.2 正式版：适配游戏更新时代/地域选择界面识别（2026-08-12）

### 修复

- 游戏 2026-08-12 更新后，时代选择/地域选择页面标题文字移除，原模板匹配识别失效：`S_IsEraSelect`/`SF_IsEraSelect`（模板「时代选择.png」，ROI [564,54,157,54]）、`S_IsRegionSelect`/`SF_IsRegionSelect`（模板「地域选择.png」，ROI [558,55,169,54]）
- 时代选择识别改为匹配时代列表左侧竖条标识区域：ROI [564,54,157,54] → [104,331,49,235]，模板图同步替换
- 地域选择识别改为匹配页面底部区域：ROI [558,55,169,54] → [848,607,257,63]，模板图同步替换
- 主枢纽（`S_`）与副枢纽（`SF_`）四处节点同步修改，实机验证通过

### 版本

- 资源版本号 **v0.9.1 → v0.9.2**（正式版发布）：适配时代选择/地域选择界面识别，同步更新公告与打包脚本版本

## v0.10.0-beta.1：合战场整合异去玩法（2026-08-12）

### 背景

- 游戏 2026-08-12 更新新增「异去」玩法（原合战场玩法改称「过去」），流程与过去几乎一致，但存在三处差异：时代选择界面新增「过去/异去」切换按钮且异去 tab 下页面识别特征不同（布局相同、视觉不同）；无王点前/道中撤退；有门票机制（出阵确认使用、不足弹窗）
- 方案：异去整合进合战场任务（GUI「过去/异去」下拉），pipeline 差异全部通过选项 override 表达，不复制节点

### GUI

- 合战场任务 option 置顶新增「过去/异去」select（默认「过去」）
- 「过去」case 子选项：S_王点前撤退、S_道中撤退（从顶层移入，旧配置勾选状态丢失需重新勾选，发布公告已说明）
- 「异去」case 子选项：异去_重复次数（input，默认 -1 无限）、异去_不购买门票（checkbox，照搬陆联语义）

### Pipeline

- 更名：`S_IsEraSelect` → `S_IsPastEraSelect`、`S_IsRegionSelect` → `S_IsPastRegionSelect`（仅更名，副枢纽 `SF_` 不改）
- 新增节点（常驻主枢纽检测链，无需模式开关）：`S_IsIsekaiEraSelect`（时代选择_异去.png，ROI [104,331,49,235]）、`S_IsIsekaiRegionSelect`（地域选择_异去.png，ROI [833,617,152,41]）、`S_ClickPastTab`/`S_ClickIsekaiTab`（tab 切换）、`S_IsIsekaiBattleEnd`（异去结算.png，阈值 0.8，命中 freeze 3000ms [414,226,423,315] 消化长结算动画，无 next = 本轮任务结束）
- 门票三节点：`S_IsIsekaiTicketConfirm`（OCR「出阵」→ 确认，挂 S_PostSortieHub、默认 disabled）、`S_IsIsekaiNoTicket`（OCR「归城提灯一补充」→ 补充/关闭）、`S_IsIsekaiConfirmPurchase`（OCR「确认」→ 购买），所有点击 freeze 300ms
- 两模式对称归位：时代/地域选择页面前后版识别互斥，无论游戏是否记忆 tab 都能自动归位；`S_VerifyEra` 异去模式 ROI [836,80,27,26]
- 行军：43 个 Boss/MidRetreat 节点默认禁用，一路打到底；打完回地域选择页面（非本丸），下一轮由队列按 Count 循环

### C# 联动

- `MaaProcessor.CreateNodeAndParam` 重复次数联动：「异去_重复次数」从「过去/异去」选项的 **SubOptions** 读取（子选项值不在顶层 Option 列表，初版从顶层找导致永远回退 -1 只跑 1 轮，实测后修复）；「过去」模式强制 -1；Count 三元增加 -1 直通分支（否则 `Repeatable=false` 会被折成单次）

### 实测

- 异去全流程通过：切 tab 归位、选时代地域（1-4）、门票确认、行军一路打到底、打完回地域选择页面、重复次数 2 跑两轮停止、结算 freeze 无多次命中

### 版本

- 资源版本号 **v0.9.2 → v0.10.0-beta.1**（预发布版）：合战场整合异去玩法，同步更新公告与打包脚本版本

## 子选项描述与子页面间距调整（2026-08-13）

### 任务描述

- 合战场顶层 description 移除「王点前撤退」「道中撤退」两段，改为挂载到「过去」case 子选项 `S_王点前撤退` / `S_道中撤退` 的 `description` 字段，在子选项 checkbox 下方显示
- 两段描述各自独立显示（王点前撤退段、道中撤退段），不合并

### 子页面间距

- 子选项 case 无 `DisplayName`（单选项场景：刀种筛选、卡死重启、长期远征计划、LR_部队交替）不再渲染空标题 TextBlock，消除多余占位空隙
- 移除间距负 margin 硬凑：`SubPageContent` 上拉 -20px 恢复为 8px、子选项 `WrapPanel` -10px 归零，标题边距统一 `(0,2,0,2)`
- 有标题页面标题距返回按钮 10px、标题与选项间距 6px；无标题页面内容距顶部 8px

## 卡死重启修复：画面冻结循环检测失效（2026-08-13）

### 现象

- 游戏卡死在本丸画面，MATR 形成「本丸识别 → 目录按钮点击 → 路由枢纽 → 亮度检查 → 本丸识别」四节点交替死循环（每 2.3s 点击一次右上角「目录」），持续 5 分钟以上，「卡死重启」未触发

### 根因（三层检测全部失效）

- Pipeline 识别超时（120s）：`E_FlowerNavigate` 每次 OCR 都命中「目录」，永不超时
- 循环卡死检测 LoopDetector 双重缺陷：
  - MaaFW 5.10 动作完成消息为 `Node.PipelineNode.Succeeded`（含 action_details），代码判断 `Node.Action.Succeeded`（旧协议名）→ `Feed()` 从未被调用，检测完全失效
  - 即便消息名修正，节点名取 `jObject["name"]`（流程上下文名，冻结循环中四节点轮流变化），动作键永不连续
- 无响应检测（120s 无回调）：死循环持续产生识别+点击回调，`LastCallbackTime` 一直刷新

### 修复

- `MaaProcessor.cs` 卡死检测事件：消息前缀改 `Node.PipelineNode.Succeeded`；节点名改取 `action_details.name`（实际执行动作的节点，冻结循环中恒定）；只统计 `Click`/`Swipe` 真实交互动作，排除 `DoNothing`（纯检测）与 `Custom`（智能等待等）——避免正常等待期持续 Custom 动作误触发
- `LoopDetector.cs`：阈值 `200+50` 降为 `80+20`（原阈值在 2.3s 慢循环下需 9.7 分钟，新阈值约 3.9 分钟，符合 70-400 秒设计意图）

### 验证

- 日志回放实测（本次卡死场景）：重启后段（09:11:16 起）修复后于 09:15:07 触发自动恢复；重启前段（09:10:26 卡死 49 秒后用户手动重启）未到阈值，符合预期

### 边界

- 冻结画面无任何点击/滑动（纯识别等待）时 LoopDetector 仍检测不到，需后续「截图画面不变」检测增强

## 5-2 王点前撤退修复（2026-08-13）

### 现象

- 5-2 王点前撤退从未生效：`S_Boss_E5_R2` 节点存在且「王点前撤退」override 已将其启用，但实战中 5-2 到达王点不撤退

### 根因

- `S_Boss_E5_R2` 未接入 `S_CheckBoss` 路由（`next` 列表缺该项），节点再启用也不会被检测
- 节点 ROI `[1080, 242, 10, 15]` 与实测王点位置 `[1115, 312, 9, 15]` 不符

### 修复（Sortie.json）

- `S_Boss_E5_R2` ROI 修正为 `[1115, 312, 9, 15]`，复用 `Sortie_boss/王点_5_2.png` 模板（9×15 尺寸吻合）
- 接入 `S_CheckBoss` 路由，插入 `S_Boss_E5_R1` 与 `S_Boss_E5_R3` 之间，补齐 E5_R1 → E5_R2 → E5_R3 检测顺序
- `interface.json` override 无需改动（原有 `enabled: True` 生效条件已满足）

## 地下城修复：重复次数、同步远征回本丸、活动页路由兜底（2026-08-13）

### 地下城重复次数（beta.2 起）

- `U_ContinueNextRound` 的 `next` 置空：不勾选「每轮回本丸」时，命中「传送凭证」（一轮完成）→ 点击「继续下一轮」进入下一层 → 任务完成 → 队列按重复次数 Count 循环（对齐 `TT_CheckRoundComplete` / `S_IsIsekaiBattleEnd` 模式）
- 勾选「每轮回本丸」时原有链路（回本丸 → 确认 → next 空 → 任务完成）本就生效，不受影响

### 地下城同步远征：计时器到期回本丸失效（beta.3）

- 现象：开启「同步远征」且未勾选「每轮回本丸」时，地下城任务连续失败（0 成功 5 失败队列停止）
- 根因链：
  - 计时器**从未启动**（`_nextCheckTime == null`）时 `ExpeditionTimerRecognition.IsExpired()` 按设计返回 true（"从未设置 → 应检查远征"）→ `E_CheckTimerExpired` 返回 false → 走 on_error
  - `U_同步远征` override 将 on_error 指向 `U_GoHomeAfterRound`，而该节点在不勾选「每轮回本丸」时 `disabled` → 跳转目标被禁用 → 任务失败
  - 旧版本未暴露：此前计时器已被远征任务启动（未过期走 next 正常）
- 修复：`U_ReturnHomeAfterMarchDamage` 改名 **`U_ReturnHomeAfterMarch`**（行军受伤回本丸节点复用，21 处引用同步），`U_同步远征` 的 `E_CheckTimerExpired` on_error 改指它并启用；回本丸后经 `U_IsHome → Expedition` 检查远征 → `E_TimerStart` 启动计时器 → 回地下城，不依赖「每轮回本丸」

### 地下城活动页路由兜底（beta.3）

- 现象：点击进入地下城后画面切到部队选择页时卡住（`U_VerifyEventPage` 反复失败）
- 修复：`U_IsEventPage` 的 `next` 增加 `U_DetectWhereAmI`：`U_VerifyEventPage` 验证失败（画面已变）时回定位枢纽重新路由，命中 `U_IsTeamSelect` 继续

### 版本

- 资源版本号 **v0.10.0-beta.2 → v0.10.0-beta.3**（预发布版）：同步更新公告与打包脚本版本

## 地下城修刀链路、重伤停止链路、轮次进度（2026-08-13，v0.10.0-beta.5）

### 地下城修刀卡死（beta.4 后续）

- 现象：重伤回城 → 菜单点「修复」→ 修复界面已打开，但 `U_VerifyRepairMenuClosed` 识别「菜单目录」模板 41 次全部失败（该模板/ROI 与实际画面不符，节点自引入以来从未命中过）→ 20 秒超时 → on_error 回 `U_DetectWhereAmI` → next 全失败 + `U_FallbackWait` 禁用 → 任务终止
- 修复：`U_ClickRepairInMenu` 的 next 增加 `U_SelectDamagedSword` 兜底（DirectHit，修复界面已打开时直接进选刀流程）

### 删除 U_DetectAfterSortieNow 路由（U/S）

- 现象：行军前（出阵确认/部队界面）误触发 `U_IsMarchingDamage` → 输出「撤退撤退」并回城
- 根因：`U_PostSortieHub` 将 `U_DetectAfterSortieNow` 作为兜底，其 next 的 `U_IsMarchingDamage` 用「重伤」模板匹配左侧队伍栏，行军前队伍里已有的重伤标记被误判为行军重伤
- 修复：删除该路由节点定义，`U_PostSortieHub` 及 `U_PreCheckTroopRecord`/`U_PreCheckUseRecord` 引用改指 `U_DetectWhereAmI`（S 版同构一并处理）

### 重伤「停止」处理不生效（U/S）

- 现象：选择「停止」后任务不终止，仍继续出阵循环
- 根因 1：`U_DecideDamage_Pre` 的 next `[U_NavigateToRepair, U_IsStopOnDamage]` 中 `U_NavigateToRepair` 是无条件命中（DirectHit），停止分支永远不可达；interface.json「停止」case 也缺失 `U_NavigateToRepair: {enabled: false}`
- 根因 2：`StopOnDamageAction.cs` 编译错误（`ObservableQueue<MFATask>` 无 `FirstOrDefault`，CS1061），action 从未注册成功，`U/S_TerminateOnDamage` 执行时报 "Action is null" → on_error 继续循环
- 修复：`StopOnDamageAction.cs` 改用 `TaskQueue.Any(...)` 判断下一任务；interface.json 补 `U/S_NavigateToRepair` 禁用；删除失效的 `U/S_IsStopOnDamage`（含 ROI y=-1 越界问题），`U/S_DecideDamage_Pre` next 改指 `U/S_LogStopOnDamageRepair`
- 停止链路简化：删除回本丸导航链 12 个节点（`NavigateToHomeFromStop`/`ClickMenuStop`/`ClickHomeStop`/`VerifyHomeScreen`/`ClickHomeFallback`/`TerminateOnDamage`），`U/S_LogStopOnDamage`/`LogStopOnDamageText` 改用 `StopOnDamageAction`（日志 + Toast 弹窗 + 任务终止）
- `U/S_IsStopOnDamage` 从 `U/S_DetectWhereAmI` 主枢纽移除，避免全局误触发

### 任务轮次进度报告

- 新增：有限重复次数（repeat_count > 0 且 > 1）的 MAAFW 任务，每轮结束后输出「任务完成: 任务名 进度 X/Y」（`MFATask.Run` 循环内，`await Action()` 成功后）
- 无限重复（-1）与单次任务不输出；失败轮次不输出
- 新增本地化 key `TaskRoundComplete`（简/繁/日三语）
- `MaaProcessor.cs`：任务成功时不再输出「任务名 任务完成」日志（与进度日志重复），失败仍输出「任务名 任务失败」

### 合战场「过去」模式固定 3 轮

- 原逻辑：选「过去」时强制 `-1`（无限循环，UI 重复次数被忽略）
- 修改：`MaaProcessor.cs` 联动逻辑，选「过去」时固定 `repeatCount = 3`（跑 3 轮后任务结束）；「异去」仍按子选项「异去_重复次数」

### 版本

- 资源版本号 **v0.10.0-beta.4 → v0.10.0-beta.5**（预发布版）：同步更新公告与打包脚本版本

## v0.10.0 正式版：合战场异去整合与地下城重伤链路完善（2026-08-14）

- beta.1 ~ beta.5 五轮公测期间无新增 bug 反馈，转为正式版
- 资源版本号 **v0.10.0-beta.5 → v0.10.0**（正式版发布）：beta 版本更新合并为 v0.10.0 更新公告，删除 5 个 beta 公告


## 记住上次模拟器匹配（2026-08-14）

### 背景

- 用户反馈：由 taptap 调用模拟器时，每次打开 MATR 都要手动输入 ADB 序列号（从 MAA 配置复制后改多开编号）才能连上
- 实测日志确认失效链路：模拟器未启动时「按指纹匹配到的设备数量:0」→ 自动检测结果为空 → `SetEmptyDeviceState` 清空列表 → 界面无设备，只能手动输入或等模拟器启动后手动刷新

### 实现（TaskQueueViewModel.cs）

- `UpdateDeviceList` 空结果分支新增兜底：`TryRestoreLastDeviceOnEmpty` 恢复上次使用的 ADB 设备（未连接、不触发连接），受「记住连接」开关控制，关闭时保持原有行为
- 新增 `StartDeviceWaitRetry`：兜底恢复后后台每 10 秒重试检测，模拟器就绪后自动选中设备并连接（按「刷新后尝试连接」设置，与手动刷新行为一致）；连接失败（模拟器未就绪等）继续下一轮重试；连接成功、任务运行或用户改选其他目标时退出
- 手动刷新命令取消后台重试，避免与用户操作冲突

### 调试记录

- 首版用 `ReferenceEquals` 判断用户是否改选设备，但连接流程会重建设备实例，导致重试循环 6ms 即误判退出 → 改为按 ADB 序列号+路径比较（容忍实例重建）
- 首版找到设备但连接失败后直接返回，模拟器 adb 可发现但系统未就绪时会留 30 秒空窗仍需手动刷新 → 改为连接失败后继续循环重试
- 实测通过：模拟器未启动打开 MATR → 兜底显示上次设备；启动模拟器后自动检测到设备并尝试连接，未就绪时失败后继续重试；用户手动重连正常
- 决策：重试间隔保持 10 秒（模拟器 adb 可发现到可连接窗口约 13 秒，用户手动重连可抢在下一轮之前，可接受）

### 版本

- 资源版本号 **v0.10.0 → v0.10.1-alpha.1**（内测版）：记住上次模拟器匹配功能，同步更新公告与打包脚本版本

## 后勤任务:远征 + 修刀整合(2026-08-14)

### 背景

- 用户反馈场景:修刀与内番视为「多 N 个位置的远征」,与远征合并为「后勤」任务
- 内番暂缓(无界面数据),先实现修刀

### 任务改名与同步后勤

- 远征任务改名「后勤」(entry 保持 Expedition,兼容已有配置)
- 新增「修刀」switch 选项(默认关闭),Yes case 挂子选项「修复工坊一~四」(checkbox,参照「长期远征计划」模式)
- 「同步远征」label 全部改为「同步后勤」(合战场/地下城/陆联/战术强化,内部 name 不变保配置兼容)
- 修复任务改名引发的联动:`ExpeditionMapSelectAction`、`DispatchLogAction` 按任务名「远征」查找配置失败导致全部判为休息 → 改用 entry 匹配

### 工坊检查链(Expedition.json)

- `E_ExpSubHub5` 全忙 → `E_WorkshopHub` → `E_CheckWorkshop1..4`(OCR「未使用」,ROI 由用户提供)→ 命中 → `E_ClickRepairButton`
- 检查链沿用 `E_ExpSubHub` 枢纽模式(disabled 只出现在 next[0],next[1] 均为 enabled 枢纽)
- 未开启任何工坊时检查链穿透,行为与纯远征一致
- 同步后勤场景:其他任务(地下城等)管线合并时额外合并后勤任务的「修刀」选项 override,使同步后勤跳转时修刀同样生效(MaaProcessor.cs)

### 修复流程

```
E_ClickRepairButton(模板 本丸修复.png + 点击 + post_wait_freezes)
  → E_VerifyRepairScreen(OCR 修复状况 [553,62,172,38],无 action)
    → E_SelectRepairSlot1..4(模板 Common/未使用修刀槽.png,按工坊启用,多候选 next 路由)
      → E_VerifyRepairSelect(OCR 修复刀剑男士 [195,129,151,34],仅确认)
        → E_FindRepairableSword(自定义动作选刀)
          → E_ConfirmRepair(OCR 修复 [1156,587,85,44] → 点击)
            → E_GoHome
```

### 选刀:颜色识别 + 长按滑动(新增 ClickTopRepairableSwordAction)

- 问题:修复工坊可能修到出阵任务(地下城等)正在使用的刀剑导致无法出阵
- 方案:在 [459,127,16,554] 扫描白色标记像素(可修复刀剑标记,出阵中的刀无标记),点最上方白色区域
- 标准 ColorMatch 只能返回聚合外接矩形,多白色区域时无法定位 → 自定义动作 GetImage + CopyPixels 逐像素扫描
- 视野内无白色标记 → 三段式长按滑动:按住 [732,638] 0.5s → 800ms 滑动到 [732,131] → 再按住 1s,最多 8 次
- 参数可经 custom_action_param 覆盖;日志仅写日志文件不输出 GUI
- 调试坑:内置自定义动作需在 MaaProcessor 手动注册,漏注册导致动作不执行直接 on_error 回家
- 实测通过:正确的槽位修了正确的刀(避开出阵中的刀),回本丸继续地下城任务

## 同步后勤缓存污染导致部队被误派循环(2026-08-15)

### 现象

- v0.10.1-alpha.1 内测反馈:同步后勤开启时,MATR 反复进入远征界面。第 5 队设置为「休息」仍被派遣,`E_SelectMap5`(ExpeditionMapSelectAction)读取配置发现休息返回 false,约 18 秒一轮无限循环(23 分钟产生 60+ 张 on_error 截图)
- 日志特征:`E_CheckTeam5` 正常执行且 OCR 识别到「有队员需要修复」(命中 expected 中的「队员」),说明 `enabled: false` override 未合并
- 「重启 MATR 后手动切换部队配置」临时有效,但再次界面操作后复发;与 7-22 记录的「陈旧状态」问题同症状

### 根因

`TaskItems` 被惰性 IEnumerable 污染:

- `TaskQueueView.SaveConfiguration` 与 `TaskQueueViewModel.ResetTasks` 调用 `SetValue(TaskItems, ...Select(...))` 时缺少 `.ToList()` 物化
- 惰性 IEnumerable 存入 `InstanceConfiguration._config` 后,`GetValue<List<MaaInterfaceTask>>` 类型转换失败返回**空列表**
- 同步后勤合并(MaaProcessor `CreateNodeAndParam`)从缓存读取 `expTask` 得到 null → 部队选项 override(含 `E_CheckTeam5: {"enabled": false}`)全部不合并 → 休息的部队被误判可派遣

### 触发链

1. 任何 UI 操作(任务勾选/添加/删除/选项修改)调用 `SaveConfiguration` → 缓存被污染
2. 此后任意一次启动任务,同步后勤合并读到空 TaskItems → 部队选项缺失
3. 重启后 `LoadTasks`(已正确 `.ToList()`)重建缓存 → 「重启后第一次正常」,再操作界面又复发
4. v0.10.0 时代配置稳定、日常启动不触发污染,故「看起来没问题」;alpha.1 升级伴随任务改名与新增修刀选项,界面操作频繁,必然触发

### 修复

- `TaskQueueView.axaml.cs`(SaveConfiguration):补 `.ToList()` 物化
- `TaskQueueViewModel.cs`(ResetTasks):补 `.ToList()` 物化
- `MaaProcessor.cs`(同步后勤合并):读取到空列表时 `InstanceConfiguration.ReloadFromDisk()` 重读磁盘兜底,防止漏网污染源

### 验证

- 修复后诊断日志:`[同步后勤] savedTasks=6 expTask=后勤(entry=Expedition) 部队五Index=0`(修复前 `savedTasks=0 expTask=null`)
- `E_SelectMap5` 循环停止,第 5 队正常跳过,地下城 + 同步后勤稳定运行
- 排查过程中在同步后勤合并处保留了 Info 级诊断日志(打印 savedTasks/expTask/部队 Index),便于后续回归定位

### 版本

- 修复随 v0.10.1-alpha.1 内测补丁发布,未改资源版本号

## v0.10.1-beta.1 内测版:内番功能与全流水线优化(2026-08-15)

### 后勤新增内番功能

- 内番视为「多 N 个位置的远征」:五队全忙且工坊检查完成后,状况面板 OCR「待机中」([915,297,296,43])命中 → 点「本丸内番」(模板 ROI [1220,315,40,106],面板不关闭)→ 今日内番表(OCR [554,7,177,42])点击开始 → 「内番设置确认」(OCR [541,55,196,41])点击确认 → E_GoHome
- 「内番」switch 选项(默认关闭),pipeline_override 控制 `E_NaibanHub.enabled`;「内番中(XX:YY:ZZ)」或识别失败 → E_AllTeamsBusy 不阻塞主循环
- 同步后勤合并补上内番开关:此前 `MaaProcessor.CreateNodeAndParam` 只同步部队/修刀/刷新间隔,其他任务执行后勤检查链时 E_NaibanHub 保持 disabled 被静默跳过(现象:内番待机中但不去指派)

### 远征派遣链优化

- `E_SelectTeamN`:点击后 200ms freeze,next 加自回(验证失败重试点队伍)
- `E_VerifyScreenN`:next 精简为 [E_SelectMapN]——无识别节点 DirectHit 必中,原 next[1] 的 E_DetectHome 永远不会轮到;验证失败时作为候选被跳过,由 E_SelectTeamN 自回承接
- `E_ConfirmDispatchN`:100ms 全屏 freeze,next [E_SelectTeamBtnN, E_VerifyScreenN] 双候选(部队选择弹窗未出现时回验证重走地图链)
- 时间控制统一 `post_wait_freezes`(移除 pre/post_delay 硬延迟):SelectMap/ClickSmallMap/StartExpedition 100ms、ConfirmExpedition 200ms、回本丸四节点 [0,631,137,89] 200ms

### post_wait_freezes 的 roi 字段无效

- MaaFW 源码确认:`WaitFreezesParam` 无 roi 字段,检测区域由 `target` 决定:true=Self(识别 box)、字符串=PreTask/Anchor、数组=Region
- 此前配置的 `"roi"` 全部被静默忽略——E_CheckTeamStatus 配置 [0,629,136,91] 实际检测的是 OCR「状」的 box [8,374,29,26]
- 全项目 23 处 `"roi"` → `"target"`(DirectHit 节点的 box 恰为全屏,原配置等效,纯 DirectHit 节点可不写)

### 重复路由与 DoNothing 清理

- 规则确认:MaaFW 插件「重复路由」校验正确——主枢纽已在 next 列表时,on_error 再指主枢纽为冗余(识别未命中走 next 候选兜底,on_error 是 action 异常路径)
- 删除 257 处重复路由 on_error(Expedition 17 + 各任务 *_DetectWhereAmI 240)+ 74 处 DoNothing action
- 教训:git checkout 回滚会还原未提交改动——批量修改前先提交,回滚前检查工作区状态

### 地下城流水线优化

- 节点合并:U_IsSortie+U_OnSortieScreen、U_IsEventPage+U_VerifyEventPage(同模板二次验证冗余)、U_NavigateToActivity+U_ClickMenu、U_ClickAccel+U_CheckAccel(纯路由中转)
- 删除 U_VerifyRepairMenuClosed/U_TryClickRepair 重试链、U_FindSlot1..3(OCR 找槽位)
- 重伤检测:删除文字识别节点(U_/S_ IsPreDamage2、StopOnDamageText 及其日志链、ConfirmCancelPreDamage2),仅保留模板识别(Common/重伤.png)
- U_SelectRepairSlot1..4 改为与 E_SelectRepairSlot 同款模板匹配(未使用修刀槽.png);U_SelectDamagedSword 增加 OCR「修复状况」确认
- 改名:U_NavigateToUnderground → U_NavigateToActivity、U_/S_ IsPreDamage1 → IsPreDamage、U_/S_ LogPreDamage1 → LogPreDamage
- interface.json override 同步:U_VerifyEventPage → U_IsEventPage(层数选择不启用)、U_CheckAccel → U_ClickAccel(修刀并停止)

### 修刀检查链优化

- E_ConfirmRepair.next 移除死路由 E_VerifyRepairSelect(E_GoHome DirectHit 必中,next[1] 永远轮不到)
- E_WorkshopHub 默认 enabled:false,「修刀」开关 Yes case override 启用——未开修刀时跳过 4 个 Hub 节点(~0.7s 开销)
- E_ExpSubHub5.next 加 E_NaibanHub 与 E_AllTeamsBusy 兜底:修刀关+内番关时直接关面板回本丸,不再空转

### 教训:改名需覆盖代码并重新发布

- U_NavigateToUnderground 改名后,MaaProcessor.cs 运行时注入的 E_TimerStart override 仍为旧名——代码改了但未重新 build+publish,运行旧 DLL 导致 PipelineChecker「Invalid next node name」校验失败,任务每轮秒败
- 排查特征:maafw.log 的 `failed to override_pipeline`;override 数组里带 `interval` 的 E_TimerStart 块是运行时生成,不在 interface.json 中
- 规则:改名/删除节点需覆盖三处(pipeline JSON、interface.json override、_src 代码字符串),改代码后必须重新发布

### 版本

- 资源版本 v0.10.1-alpha.2 → v0.10.1-beta.1,新增内番、远征/地下城/修刀优化、全流水线清理;删除 alpha 公告,公告改为 0.10.0 → 0.10.1-beta.1 全量变化

## 地下城返回识别与 GUI 轮次日志顺序修复（2026-08-16）

### 返回按钮识别与点击范围

- `U_ReturnHomeAfterMarch`、`U_GoHomeAfterRound`、`S_ClickRetreat` 增加 OCR「返回」识别，ROI 统一为 `[1164,407,88,40]`
- 三个 node 原点击位置均为单点 `[1208,449,1,1]`，统一扩大为按钮区域 `[1173,416,70,68]`，降低单点点击受画面缩放或坐标偏差影响的风险
- 两份 Pipeline JSON 均通过解析与格式检查

### GUI 轮次日志乱序

- 现象：地下城有限重复运行时，GUI 中「开始任务：地下城」与「任务完成：地下城 进度 X/Y」偶尔倒序，视觉上无法按轮次一一对应
- 日志证据：相邻两条记录仅相差数毫秒，部分轮次出现下一轮「开始任务」先于上一轮「任务完成」显示
- 根因：`MaaProcessor.AddLogByKey` 为每条本地化日志单独启动 `Task.Run`，多个线程池任务再分别向 GUI 线程投递，投递顺序不受调用顺序保证
- 修复：移除 `AddLogByKey` 外层多余的 `Task.Run`，直接调用 `DispatcherHelper.PostOnMainThread`；日志创建、集合写入和裁剪仍全部在 GUI 线程执行，不引入跨线程集合访问
- 普通 `AddLog` 原本已使用相同的直接投递模式；修改后同一任务循环产生的「开始 → 完成」日志按调用顺序进入 GUI 调度队列

### 构建、发布与实机验证

- `dotnet build _src/MFAAvalonia.sln` 完整构建成功：0 错误；保留既有依赖漏洞与运行时标识警告
- `dotnet publish _src/MFAAvalonia.Desktop -c Release` 发布成功
- 按发布规范复制 `MFAAvalonia.Core.dll`、`MATR.dll`、`MATR.exe` 到工作区目标位置，三个目标文件与发布源 SHA-256 一致；未同步到运行区
- 实机连续运行验证通过，地下城轮次日志显示顺序正常
- 本次未提升资源版本号

## 修刀选刀误选与连续滑动修复（2026-08-16）

### 现象与定位

- 用户在后勤中启用修刀，并通过地下城的同步后勤进入修刀流程；成功选择三号空工坊后，选刀动作错误点击了正在队伍中的刀剑
- `E_FindRepairableSword` 调用内置自定义动作 `ClickTopRepairableSwordAction`，原逻辑在 `[459,127,16,554]` 内从上向下扫描 RGB `[252,252,252]`～`[255,255,255]`，只要出现单个白色像素就视为可修复标记
- 12:43:54 实机诊断确认误命中位于屏幕坐标 `(460,471)`，首个匹配行仅有 4 个白色像素；结合诊断截图确认白色来自点击特效，并非刀剑的可修复状态标记
- 动作滑动原本由三次独立 `Swipe` 组成：起点原地 500ms、上滑 800ms、终点原地 1000ms。MaaFW 在每次 `Swipe` 结束后都会抬起触点，前后两次原地滑动因此被游戏识别为长按或点击，导致列表滑动过程中临时选中不符合要求的刀剑

### 颜色识别修复

- `ClickTopRepairableSwordAction` 改为只接受完整的 **15×7 连续纯白矩形**，不再以单个像素或零散白点作为命中依据
- 扫描顺序保持从上到下、从左到右；命中后点击白色矩形中心，确保优先选择当前画面最靠上的可修复刀剑
- `E_VerifyRepairSelect` 增加 200ms `post_wait_freezes`，检测区域为颜色识别长条 `[459,127,16,554]`，等待点击特效消失并确认选刀列表稳定
- `E_SelectRepairSlot1..4` 统一增加 200ms `post_wait_freezes`，检测区域为修复信息面板 `[114,123,316,240]`，避免工坊点击后的画面变化干扰后续识别
- 调试期间为每次扫描保存 `debug/repair_selection/repair_selection_*.png`，并记录像素格式、命中坐标、连续白色区域尺寸及原始 BGRA 数据

### 连续触摸滑动修复

- 将三段独立 `Swipe` 改为同一个触摸会话：`TouchDown(732,638)` → 按住 500ms → 20 步连续移动至 `(732,131)`，总计 800ms → 终点保持 1000ms → `TouchUp`
- `TouchUp` 放入 `finally`，即使任务停止或动作异常也会释放触点，避免触摸状态残留
- 不复用现有 `SmoothTouchMove`：该方法默认一步时移动坐标仍为起点，无法完成本次连续上滑要求

### 验证

- 13:12:21 实机任务中，`E_VerifyRepairSelect` 成功识别“修复刀剑男士”；局部 freeze 实际等待 212ms 并成功稳定
- 前两次扫描未发现 15×7 连续纯白区域，仅执行列表上滑；第三次扫描命中后点击 `(466,334)`，对应不在队伍中的「今剑」，未再误选队伍中的刀剑
- 修复滑动方式后再次实机验证通过，滑动过程不再因原地 `Swipe` 的抬起动作产生错误选择
- 临时用于停在选刀画面的 `E_FindRepairableSword.next: []` 已恢复为 `next: ["E_ConfirmRepair"]`，完整修刀流程重新接通
- 连续触摸回归测试通过；`dotnet build _src/MFAAvalonia.sln -c Release --no-restore` 完整构建成功：0 错误
- 已更新工作区 `runtimes/libs/MFAAvalonia.Core.dll` 并完成 SHA-256 一致性校验；未同步到测试运行区
- 资源版本号 **v0.10.1-beta.1 → v0.10.1-beta.2**

## GUI 下拉框按选项启用搜索（2026-08-16）

### 背景

- GUI 的可交互选项统一使用下拉框控件生成器，底层 `ComboBoxExtensions.CanSearch` 已支持搜索，但生成器此前固定调用 `SetCanSearch(comboBox, false)`，导致所有下拉框均不显示搜索框
- `interface.json` 中「层数选择」已经配置 `"is_searchable": true`，但 `MaaInterfaceOption` 缺少对应属性，反序列化时该字段被忽略，因此配置未生效
- 目标是让选项自行决定是否启用搜索：选项配置为 `true` 时显示搜索框，未配置或配置为 `false` 时保持普通下拉框

### 实现

- `MaaInterfaceOption` 新增 `IsSearchable` 属性，通过 `[JsonProperty("is_searchable")]` 读取选项配置；布尔值默认保持 `false`，不改变现有下拉框行为
- `TaskOptionGenerator.CreateComboBoxControl` 改为调用 `SetCanSearch(comboBox, interfaceOption.IsSearchable)`，搜索能力按单个选项生效
- 搜索字段继续使用 `DisplayName`，搜索框水印继续使用现有本地化 `Search` 文本
- 「层数选择」沿用现有 `"is_searchable": true` 配置，其他未声明该字段的下拉框不显示搜索框

### 构建、发布与验证

- `assets/interface.json` 解析检查通过，确认「层数选择」类型为 `select` 且 `is_searchable` 为 `true`
- `dotnet build _src/MFAAvalonia.sln` 完整构建成功：0 错误；保留既有依赖漏洞与运行时标识警告
- `dotnet publish _src/MFAAvalonia.Desktop -c Release` 发布成功
- 按发布规范复制 `MFAAvalonia.Core.dll`、`MATR.dll`、`MATR.exe` 到工作区目标位置，三个目标文件与发布源 SHA-256 一致；未同步到测试运行区
- 本次未提升资源版本号

## 远征计时重复触发与内番对话流程修复（2026-08-16）

### 地下城返回本丸时重复检查远征计时

- 现象：测试运行中，远征倒计时结束日志会在相隔数秒后连续输出两次；同一轮返回流程重复进入远征计时检查
- 原因：确认返回本丸后页面仍处于切换阶段，流程过早进入下一轮画面识别，使返回链与远征入口链发生重叠
- 修复：为 `U_ConfirmReturnHome` 增加 200ms `post_wait_freezes`，等待返回后的画面稳定，再继续后续识别
- 资源版本号 **v0.10.1-beta.2 → v0.10.1-beta.3**，并同步更新 `interface.json`、`tools/pack.ps1` 和测试版更新公告

### 内番完成后的不定次数对话

- 原流程中，`E_NaibanVerifyConfirm.next` 将无识别字段的 `E_GoHome` 放在候选列表内，导致后续候选永远无法命中；同时确认内番后实际会出现次数不固定的内番对话，不能立即执行返回本丸
- 新增 `E_NaibanDialogue`，识别依据和点击区域与 `E_IsSwordDropColor` 保持一致，用于识别并点击内番对话
- `E_NaibanVerifyConfirm.next` 调整为先识别 `E_NaibanDialogue`，确认画面未消失时通过 `E_NaibanVerifyConfirm` 自循环再次点击
- `E_NaibanDialogue.next` 设置为 `["E_NaibanDialogue", "E_GoHome"]`：存在对话时持续循环；对话全部结束后，才进入返回本丸流程
- `E_NaibanVerifyConfirm` 与 `E_NaibanDialogue` 均补充 `on_error: ["E_GoHome"]` 作为异常退出路径
- Pipeline JSON 解析、node 引用、识别参数和动作参数一致性检查通过；尚待测试运行区完成实际流程验证

## Issue 模板重构与仓库清理（2026-08-16）

### Issue 模板重构

- `bug_report.yml` 重构为结构化表单：新增「问题类型」「发生频率」下拉、「问题概述」「复现步骤」「预期结果」「实际结果」文本域，均必填
- 任务列表同步当前流水线（远征→后勤、新增地下城/战术强化），标题规范为 `[Bug]: ` 前缀

### 仓库清理

- `docs/` 整目录移出 git 追踪（本地保留），相关设计文档与 superpowers 计划/规格不再入库
- `star-history.png` 移至仓库根目录，README 引用同步更新，避免 docs 移出后 GitHub 页面星图断链
- `.claude/`（含嵌入的 gstack 技能仓库）与 `CLAUDE.md` 移出 git 追踪（本地保留）
- `.gitignore` 补充「误生成空文件」条目（`()`、`,+`、`0`，构建时 XamlCompiler 偶发产生）

## 自定编队功能（UI 部分）（2026-08-16）

### 背景

- 目标：为出阵任务提供「按预设精确编成指定部队（刀剑 + 刀装 + 马匹）」能力，避免手动换队
- 首版以独立特殊任务「自定编队」实现（任务队列 ＋添加任务 → 特殊任务区 🛡️），不做任务列表常驻项；游戏内编成执行流程留待下一阶段

### 设计决策

- 预设库管理收在任务设置页内，不做全局设置页入口
- 预设结构：名称（兼作备注用途）、目标部队（1-5）、1-6 号位（第 1 位为队长），每位 = 刀剑名 + 刀装名 + 马匹名（马匹可「无」）
- 预设行：互斥勾选（单选本次使用）+ 固定编号「预设N」+ 名称输入（留空显示占位）+ 齿轮（进入编辑）+ 下拉菜单（删除/复制/粘贴，粘贴为内存暂存副本覆盖目标）+ 底部加号新增（新增后自动进入编辑）
- 编辑界面（SubPage 覆盖层）：目标部队标签与下拉同一行，1-6 号位每行 = 编号 + 刀剑名输入 + 刀装下拉（不含「无」）+ 马匹下拉（含「无」）；高度字号保持主题默认，宽度自适应
- 刀装/马匹下拉候选为占位数据，待提供游戏内真实名称后替换

### 实现

- `Models/FormationPreset.cs`：`FormationPreset`（Id/Name/Team/Slots）与 `FormationSlot`（Sword/Equip/Horse），`EnsureSlots` 恒保 6 位
- `ConfigurationKeys.FormationPresets`：预设列表存 `config/instances/{id}.json`（实例级，所有任务共享）
- `AddTaskDialogViewModel`：注册特殊任务「FormationConfig」（🛡️），默认参数 `custom_action_param: { preset_id: 0 }`
- `TaskOptionGenerator`：新增 `AddFormationOptions` / `RenderFormationPresets` / `OpenFormationEditor`，面板含开关去掉（任务勾选即启用）、预设行管理、复制暂存字段、SubPage 编辑
- `FormationEditorViewModel` / `FormationEditorView`：预设编辑覆盖层，保存写回预设对象并落盘
- 构建发布通过，工作区根目录产物已更新；未提升资源版本号（资源未变）

### 待办

- 游戏内编成执行流程（pipeline + 自定义 Action：回本丸 → 菜单 → 编成 → 选部队 → 逐位配置刀剑/刀装/马匹），刀剑查找采用 OCR 滚动扫描，不预检、失败即停并提示可能部分变更
- 刀装/马匹真实名称候选列表待用户提供

## v0.10.1 正式版发布，自定编队执行流程完成（2026-08-17）

### 自定编队（游戏内执行）

- 设计定稿并全部落地：主枢纽 `FC_DetectWhereAmI`（18 入口）收敛任意画面 → 选队 → 空队检查（颜色命中跳过解散/清刀装）→ 卸装备（可选）→ 解散 → 1-6 槽位编入（排序/筛选/滚动找刀）→ 装备状态机（判断位/导航/刀装 0-3 槽/马匹）→ 保存记录（可选）→ 回本丸结束
- 数据落盘：`SwordTypeMap.json`（129 刀名 → 刀种，兼作刀剑名搜索候选）；刀装缩写切分规则（轻步/重步/精锐/轻骑/重骑/铳/弓/投石/枪/盾，无分隔连写最长匹配，切分词直接 OCR 匹配）；马匹 11 种
- pipeline：`FormationConfig.json` 125 节点（`FC_` 前缀）；8 个编译侧自定义 action（ConfigAction/FindSword/FilterClick/EquipTeam/RecordSlot/EquipSelect/HorseSelect/EquipStateMachine）+ `FormationScan` 扫描辅助（OCR 0.85 阈值、滑动、双击、确定确认）
- `MaaProcessor`：注册 8 个 action；`CreateNodeAndParam` 特判按预设生成 override（选队坐标、卸装备/保存开关路由、空刀名槽位 disabled + 链重接、team 注入）
- 实测要点：OCR 识别误差（「升序」→「升户」）用单字 expected 解决；空队用颜色检查跳过；装备空槽用「装备为空.png」模板匹配重装；刀装/马匹选择 = 点击 + OCR「确定」（8 次重试）；状态机导航后等画面切换；点击延迟档位 post_delay 200→500、500→800
- 实测通过后已从「添加任务 → 特殊任务」列表隐藏（不提供给用户），`SpecialActionNames` 保留兼容已添加任务

### 卡死恢复断点继续

- `MaaProcessor` 跟踪任务队列执行下标（`_nextTaskIndex`），任务完成时推进；`StartTask`/`AddCoreTasksAsync` 支持起始下标
- 自动恢复流程（`TaskQueueViewModel.RunAutoRecoverAsync`）重连后 `SetResumeStartIndex(NextTaskIndex)`，从断点任务继续，已完成任务不重跑；中途卡死的任务从头重跑

### 其他

- 选项改名：陆联/战术强化「不购买门票」→「不买门票」、四任务「换队长」→「更换队长」、地下城「每轮回本丸」→「每轮回程」（仅 label 与描述，配置键不动）
- 修复异去「重复次数」、卡死重启「卡死等待时间」的文字与输入框不同行（`AddResponsiveBehavior` 禁用窄屏换行）
- 版本升 v0.10.1 正式版（pack.ps1 / interface.json version+custom_title / 公告四处同步），打包 `MATR-v0.10.1.zip`
- 地下城任务描述补充「一圈判定」说明（因远征倒计时结束/刀剑重伤/刀装破坏中途回本丸不计为完成一圈）

## v0.10.2-beta.4 部队颜色验证与地下城断点恢复（2026-08-19）

### 部队选中状态识别

- 部队选中状态增加浅色与深色两种 `ColorMatch` 验证 node，两个 node 使用相同 ROI，分别覆盖 `[186,186,186]~[192,192,192]` 与 `[91,91,91]~[97,97,97]`。
- FC、自定义编队、出阵、地下城、刷花、陆联和战术强化训练的部队验证流程全部同步调整。
- `interface.json` 中所有任务 override 同步覆盖两种颜色验证 node，消除旧 node 名称导致的未知 override 报错。

### 地下城重复任务断点恢复

- 修复颜色验证未命中后反复点击部队、触发卡死恢复时错误推进任务下标的问题。
- 只有 MaaFW 任务真正成功后才推进恢复断点；卡死任务会在模拟器重启、重连后重新执行当前任务。
- 记录每个重复任务已完成的轮次，恢复时仅执行剩余轮次，避免 999 次地下城任务执行几十次后直接结束或重置。
- 修复恢复下标超过任务数量时生成空队列的问题。

### 发布与验证

- 资源版本更新为 `v0.10.2-beta.4`，同步更新 `interface.json`、`tools/pack.ps1` 和版本公告。
- `MFAAvalonia` 编译与桌面发布成功，0 个错误。
- 已生成发布包：`MATR-v0.10.2-beta.4.zip`。

## v0.11.0-beta.1 本丸页面与工作记录首版（2026-08-20）

### 本丸页面

- 侧边栏新增「本丸」入口，位置调整到限锻计算之前。
- 本丸内部固定提供三个页签：仓库、工作记录、刀帐；不支持用户拖动调整顺序。
- 仓库和刀帐页面暂显示「开发中」，工作记录页面进入首版可用状态。
- 进入本丸后不再重复显示「本丸」大标题，进入工作记录后也不重复显示「工作记录」标题。

### 工作记录解析

- 新增工作记录 ViewModel、运行记录模型和日志解析构建流程。
- 从 `debug/logs` 下的 `log-*.log` 读取识别记录，并按任务时间段重建工作记录。
- 记录列表按开始时间倒序排列，支持选中记录查看详情和点击「刷新记录」重新扫描日志。
- 页面显示相对数据路径 `debug/logs`，原始日志自动保留最近 30 天。

### 工作记录统计

- 任务概况卡片新增任务名称、运行状态、时间、耗时、出阵次数、行军次数和完成圈数。
- 刷花次数与提前结束次数仅在大于 0 时显示，避免空统计占用空间。
- 出阵收获卡片合并资源收获和刀剑掉落；资源以一排文本显示，刀剑掉落按刀种分组。
- 刀种使用加粗显示，资源与刀剑掉落采用不同的低饱和主题色。
- 后勤记录中将检查队伍状况与后勤刷花并排显示。
- 区分后勤刷花和出阵刷花：前者归入后勤记录，后者统计在任务概况中。
- 后勤记录、出阵收获和特殊情况均在没有内容时自动隐藏。

### 工作记录视觉调整

- 采用左右两列卡片布局：左侧记录列表，右侧当前记录详情。
- 左侧记录列表支持滚动，刷新按钮放置在记录列表卡片标题行右侧。
- 收紧卡片左右内边距、页面顶部间距和信息行与卡片之间的空白，使工作记录与其他本丸页签保持一致。
- 选中记录使用跟随用户自定义主题色的浅色背景和指示条，避免使用刺眼的高饱和深蓝色块。
- 统一同级文字字号，并通过深蓝灰、灰蓝、绿色等层级区分标题、正文、资源和刀剑掉落信息。

### 发布与文档

- 资源版本更新为 `v0.11.0-beta.1`，同步更新 `interface.json`、`tools/pack.ps1` 和版本公告。
- 新增 `0-v0.11.0-beta.1 更新公告.md`，删除旧版 `v0.10.3` 公告，保留长期公告。
- 更新本丸页面设计文档，补充当前页面结构、工作记录解析、视觉规范和本地数据原则。
- 桌面项目发布成功并完成开发区运行文件拷贝；构建 0 个错误，仅保留既有依赖安全警告。

## v0.11.0-beta.2 地下城层数 OCR 优化（2026-08-20）

### 地下城层数识别

- `DungeonFloorSelectAction` 增加 ColorMatch 颜色过滤，先在数字 ROI 内提取文字区域，再执行 OCR，减少背景和相邻内容干扰。
- 每一位数字必须连续 3 次识别结果一致后才确认，避免将「54」误识别为「51」后直接执行错误点击。
- 颜色匹配未命中时自动回退到原始 ROI，避免因界面颜色变化导致层数选择直接失败。
- 单次识别最多尝试 15 次，并在识别结果变化或无效时重置连续计数。

### 自定义动作运行时修复

- 修复 `DungeonFloorSelectAction` 动态编译缺少 `MaaFramework.Binding.Buffers` 引用的问题，避免自定义动作加载失败后卡在层数选择界面。

### 发布与验证

- 资源版本更新为 `v0.11.0-beta.2`，同步更新 `interface.json`、`tools/pack.ps1` 和版本公告。
- 项目编译通过，0 个错误。

## 小判箱识别与确认返回冻结修复（2026-08-20，随 v0.11.0-beta.2 下发）

### 小判箱掉落识别修复

- 地下城小判箱掉落弹窗动画约 10 秒，动画期间模板持续命中；旧逻辑每次命中都打日志并点击，一次掉落被打点多次（实测单次掉落产生 10 条日志，间隔 0.5~3 秒）。
- 确认点击无法关闭小判箱弹窗（弹窗动画播放完后自行消失），去掉节点上的点击动作。
- 新增 `KobanChestLogAction` 自定义动作：命中后打日志一次，然后轮询模板匹配等待图标消失（连续 4 秒未命中判定结束），总超时 15 秒兜底，避免主循环重复命中重复打点。
- `U_IsKobanChest` 节点 action 更换为 `KobanChestLogAction`，识别参数全部由 `custom_action_param` 提供。

### 确认返回本丸冻结

- 确认返回本丸点击后画面切换存在延迟，切换完成前主循环会再次命中同一重伤标记（实测两次命中 box 均为 260,414 附近、尺寸 37×17），重复执行整个撤退流程。
- 地下城 `U_ConfirmReturnHome` 与合战场 `S_ConfirmReturnHome` 增加点击位置冻结 500ms（区域 10×10，与点击区域一致），画面稳定后再回主循环。

### 工作记录特殊情况过滤

- 词表正则的前缀捕获组排除 `=` 与空白，`[cfg=Default]` 等上下文块不再被误捕获为前缀。
- 修复日志切块失败、跨平台数据传输失败、日志清理失败等系统 Warning 日志被当作特殊情况显示在工作记录中的问题；合法词表行（`[地下城]`、`[后勤]`、`[中断]` 等前缀）不受影响。

### 发布与验证

- 工作记录过滤涉及主程序代码（`WorkRecordBuilder.cs`），已重新发布桌面端并拷贝发布产物：MATR.dll/exe/deps.json/runtimeconfig.json/libloader 至根目录，`MFAAvalonia.Core.dll` 至 `runtimes/libs`（项目自身输出），`MaaAgentBinary` 同步更新。
- 两个 pipeline 的 JSON 压缩后校验通过，解决方案构建 0 个错误。
- 资源版本号保持不变，修复内容已追加至 `v0.11.0-beta.2` 更新公告。

## v0.11.0-beta.2 刀帐功能与回本丸流程完善（2026-08-20）

### 刀帐数据与页面

- 新增刀帐页面，展示序号、刀种、刀名、已拥有状态，以及中伤、真剑、内番、轻装四类立绘勾选列。
- 支持手动勾选、保存、撤销和清空；清空操作增加确认弹窗。
- 新增刀帐自动识别入口，要求用户从自己已拥有刀剑中序号最小的刀剑页面开始扫描；识别序号后自动点击翻页，直到回到起始序号。
- 自动识别使用序号 OCR、页面切换冻结和连续稳定识别，减少点击过快、页面切换延迟造成的漏识别。
- 同名刀的最后一个条目显示“·极”，髭切、膝丸等存在特化与极化多序号的刀剑按目录中的独立序号保留。

### 刀帐数据存储与发布边界

- `assets/resource/base/SwordBookCatalog.json` 保存随程序发布的静态刀剑目录、序号和刀种刀名信息。
- 用户的已拥有和立绘勾选状态通过配置键 `SwordBookEntries` 保存到运行时 `config/` 下的当前配置文件；默认配置为 `config/config.json`，自定义配置为 `config/mfa_*.json`。
- 自动识别的临时结果保存为 `config/swordbook_scan.json`，点击保存后才写入正式配置。
- `.gitignore` 明确排除上述个人配置，`tools/pack.ps1` 也明确清理发布临时目录中的配置路径，避免个人刀帐数据被提交或打入发布包。
- `SwordBookCatalog.json` 同时作为编队刀剑类型映射的数据源，删除重复维护的 `SwordTypeMap.json`；仅用于类型映射的刀剑通过 `typeOnly` 标记，不显示在刀帐列表中。

### 回本丸与工作记录修复

- 回本丸任务新增战斗结算页面的经验点击、刀剑掉落页面直接处理、返回本丸和确认返回路由；回本丸任务中的刀剑掉落仅点击处理，不产生刀剑掉落日志。
- 地下城完成一圈只由“完成一圈”日志词条统计，不再把出阵次数兜底成完成圈数，避免任务刚开始就显示完成一圈。
- 修正任务队列结束后的路由，所有次数结束后进入回本丸流程，不再错误点击下一次行军。

## v0.11.0-beta.3 工作记录与后勤记录优化（2026-08-21）

### 工作记录统计修复

- 将任务概况中的“提前结束次数”改为“返回本丸次数”，统一由地下城 `U_ConfirmReturnHome` 输出 `[地下城] 返回本丸` 日志统计。
- 正常完成一圈后的回本丸和中途撤退后的回本丸均计入返回本丸次数，撤退原因不再重复兜底计数。
- 保留刀装近破坏撤退等 Warning 词条的特殊情况记录，不因统计口径调整而丢失。
- 修复运行中的任务显示为“进行中”、正常任务切换后显示为“成功”的状态判断。
- 远征倒计时结束继续计入后勤记录，连续重复的小判箱掉落日志合并为一次掉落。

### 保存记录与合并

- 工作记录支持将单条或同名多条任务保存为本地工作数据记录，并通过自动追加编号处理重名。
- 已保存记录支持查看、合并、删除和清空；保存数据独立于 `debug/logs`，不会随日志自动清理。
- 合并记录保留任务名、累计时长、日期范围、统计数据、资源、刀剑掉落、后勤明细和特殊情况。
- 已保存记录列表压缩为记录名与累计时长，减少单条记录占用高度。

### 页面布局优化

- 后勤记录分为两组：倒计时结束、检查队伍状况和刷花为一组；派遣远征统计与详细派遣信息为一组。
- 统一后勤记录两组与出阵收获两组之间的间距。
- 合并记录的任务概况只显示累计耗时，不再显示无意义的 `00:00–00:00`。
- 压缩工作记录列表上下留白，在保留任务名、状态、时间和耗时的前提下显示更多记录。
- 移除工作记录页面顶部的数据存放位置与自动清理说明，减少无关信息占用。

### 本丸页签调整

- 将仓库、工作记录和刀帐三个页签整体居中显示。
- 统一调整页签标题字号，避免主题样式覆盖导致字号设置不生效。

### 发布与验证

- 资源版本更新为 `v0.11.0-beta.3`，同步更新 `interface.json`、`tools/pack.ps1` 和版本公告。
- 测试与桌面项目构建通过，0 个错误。

## v0.11.1-beta.1 仓库页面与资源趋势图（2026-08-21）

### 仓库页面

- 完成本丸仓库页面首版布局，顶部为核心资源编辑区，下方分为其他资源列表和核心资源变化图表。
- 核心资源分为木炭、玉钢、冷却材、砥石、委托符、加速符和小判；前四项使用图标，后三项使用文字标签。
- 根据资源上限调整数字输入框：木炭、玉钢、冷却材、砥石上限为 `9999999`，小判上限为 `99999999`，委托符和加速符上限为 `9999`。
- 核心资源输入项按可用宽度自适应排列，其他资源和图表区域分别独立滚动。
- 资源数字统一使用千位分隔显示，输入框文本左对齐，便于比较不同长度的数量。

### 自动识别流程

- 新增 `Warehouse.json` 仓库识别流程，从右上角目录开始，依次进入锻刀页面和所持道具页面。
- 锻刀页面读取木炭、玉钢、冷却材、砥石、委托符和加速符；所持道具页面读取小判及其他物品。
- 新增 `WarehouseReadResourceAction`，负责清理 OCR 数字中的逗号、单位和其他非数字字符，并写入自动识别草稿。
- 新增 `WarehouseScanItemsAction`，负责物品列表 OCR、保持按住状态的上滑、连续三次结果相同的结束判断，以及按游戏页面顺序写入物品。
- 对包含“月下梅树”的 OCR 结果进行名称归一化，统一保存为“锷·月下梅树透图碎片”。
- 自动识别结果先保存到 `config/warehouse_scan.json`，识别完成后由用户手动点击保存，避免半途中断污染正式数据。

### 数据模型与保存

- 新增 `WarehouseData`、`WarehouseResourceSnapshot` 和仓库数据编辑服务，分别保存当前核心资源、其他物品以及带时间的资源快照。
- 正式数据使用配置键 `WarehouseData` 保存；自动识别草稿、用户配置和其他个人数据均保持在运行时目录，不进入版本控制和发布包。
- 保存、撤销、清空操作统一由仓库 ViewModel 管理；清空仓库数据和清空图表历史均增加确认流程。
- 只有保存后的资源快照才会用于图表，自动识别完成不会直接刷新图表。

### 资源趋势图

- 每种核心资源单独显示一张纵向折线图，每个有效历史快照显示一个圆点。
- 修正横坐标算法：圆点不再按记录数量均匀分布，而是根据 `RecordedAt` 的实际时间间隔线性映射，时间间隔越长，点之间距离越大。
- 不同核心资源分别计算纵坐标范围，避免小判等大数值资源和委托符、加速符等小数值资源使用同一比例尺。
- 鼠标悬停圆点显示记录时间和数量；右键圆点可以只删除该资源对应时间点的记录。
- 图表区域提供刷新和清空按钮，删除或清空后仍需保存才会写入正式仓库数据。

### 验证

- 主程序编译通过，0 个错误。
- 发布产物已按开发目录规则复制到项目根目录和 `runtimes/libs`。
- 版本号保持为 `v0.11.1-beta.1`。

### 刀剑掉落打点优化(同日追加)

- `SwordDropLogAction` 新增纯色校验:命中 `IsSwordDropColor` 后先检查 `[53,257,54,32]` 区域,全部像素命中 `[248,244,230]±3` 时判定为非刀剑掉落画面(如内番完成对话),跳过 OCR 与打点,仍执行原点击。
- 新增 `check_roi` / `check_color` / `check_tolerance` 三个参数可覆盖缺省值;合战场、地下城、联队战、战术强化四个挂载点统一生效,无需改 pipeline。

## v0.11.1-beta.2 刀剑掉落识别修复(2026-08-21)

### 修复

- 修复内番结束对话画面被误识别为刀剑掉落的问题:`SwordDropLogAction` 命中后先检查 `[53,257,54,32]` 区域,全部像素命中 `[248,244,230]±3` 时判定为非掉落画面,跳过 OCR 与打点,仍执行原点击
- 校验参数 `check_roi` / `check_color` / `check_tolerance` 可覆盖缺省值,合战场/地下城/联队战/战术强化四个挂载点统一生效,无需改 pipeline
- 修复资源更新后在安装目录根生成无用 `interface.json` 的问题:移除 `VersionChecker` 更新流程中向根目录写入 interface.json 的遗留逻辑(上游机制,当前代码只写不读,属冗余文件);该修改在主程序代码,已重新发布并拷贝产物

### 导出日志后的反馈入口

- 日志导出成功提示改为持续显示的成功 Toast，不再只显示“日志导出成功”。
- Toast 中新增蓝色“去反馈bug”按钮，点击后使用系统默认浏览器打开问卷页面。
- 问卷地址复用长期公告中的“问题反馈与日志收集”链接。
- `ToastHelper` 与 `FileLogExporter` 已完成接入，Release 编译、桌面发布和产物拷贝均已验证通过。

## v0.11.1-beta.3 工作记录实例归属修复（2026-08-22）

### 日志读取

- 工作记录读取改为识别日志中的 `[inst=页签名/实例ID]`，使用实例 ID区分同一资源配置下的不同页签实例。
- 针对部分自定义 action 只有 `[cfg=配置名]`、没有 `inst` 的情况，读取脚本会按日志顺序记住每个 `cfg` 最近对应的实例 ID，并自动补全后续日志的实例归属。
- 不修改原有日志输出格式，兼容旧日志中没有实例上下文的记录。

### 页签名称与记录显示

- 工作记录界面根据实例 ID动态读取当前页签名称，因此页签重命名后刷新工作记录即可同步更新显示。
- 日志记录和保存记录内部仍使用实例 ID，不把页签显示名称作为合并依据。
- 当前不存在的历史页签会回退显示实例 ID，避免因页签关闭导致记录消失。

### 保存与合并验证

- 同一实例、不同时间段的同名任务可以正常合并，累计时长、资源和统计数据会累加。
- 同一实例、开始时间和结束时间完全相同的记录会被视为重复段，只计算一次。
- 不同实例即使时间完全相同，也不会被错误去重。
- 已通过实际 `log-20260822.log` 回归验证，并通过项目测试。

## v0.11.1-beta.4 后勤修刀与工作记录修复（2026-08-22）

### 工作记录

- 修复出阵任务产生的“补充刀装”日志被解析器忽略的问题，现在会显示在对应出阵记录的“特殊情况”中。
- 后勤任务产生的“补充刀装”仍归入后勤记录，避免与出阵任务混淆。

### 后勤修刀

- 修复后勤修刀确认后没有输出 `[后勤] 修刀` 日志的问题。
- 将修刀确认的点击与日志打点合并到 `E_ConfirmRepair`，避免页面切换导致独立打点 node 被跳过。

### 实时日志面板优化

- 新增 `GuiLogAction`，将需要显示给用户的行为日志单独输出到实时日志面板，不改变原有文件日志和工作记录解析逻辑。
- 为合战场、地下城和后勤任务补充重伤撤退、修刀、刀装补充、疲劳撤退、刀装破坏/近破坏撤退以及内番等明文日志。
- 统一实时日志格式，使相关日志与重伤撤退、修刀等既有日志保持一致，便于用户直接查看任务当前状态。

### 合战场出阵打点

- 新增 `S_SortieSuccess` 出阵成功 node，在确认进入合战场后通过顶部颜色区域识别实际出阵状态，再写入 `[合战场] 出阵` 日志。
- 移除出阵确认阶段的提前打点，避免点击确认后因流程中断造成错误或重复的出阵记录。
- 为 `S_PostSortieHub` 增加失败回退路径，确保出阵成功识别失败时仍能回到位置识别流程。

### 合战场撤退流程统一（2026-08-23）

- 合战场疲劳、刀装和重伤撤退统一复用 `S_ClickRetreat` 及其后续确认流程，删除重复的 `S_ReturnHomeFromMarching`。
- 统一撤退日志顺序：先记录隐式撤退原因，再显示 GUI 撤退原因，最后由 `S_ConfirmRetreat` 记录返回本丸。
- 调整重伤撤退日志链路，使其与疲劳、刀装撤退保持一致。

## 地下城层数识别与切换优化验证（2026-08-23）

### 实现调整

- 地下城层数 OCR 改用完整层数 ROI `[279,167,38,50]`，一次识别十位和个位，避免原先分开识别造成的额外等待。
- 完整层数 ROI 避开加号、减号按钮及点击特效，减少点击过程对 OCR 的干扰。
- 层数切换的连续点击间隔统一为 `500ms`，兼顾点击生效率与切换速度。
- 切换完成后要求 OCR 连续识别到目标层数 `3` 次，才判定层数选择成功。
- 删除用于测量点击间隔的临时测试 action、测试流水线和测试脚本，保留正式地下城层数选择流程。

### 实机验证

- `99 → 66`：成功，连续识别 3 次确认。
- `66 → 45`：成功，连续识别 3 次确认。
- `45 → 9`：成功，连续识别 3 次确认。
- `9 → 22`：已连续识别到 2 次目标值，随后由用户手动停止，未完成第 3 次确认，不属于识别失败。
- 最新日志中未出现“盲点未到位”或回退微调记录，层数切换过程稳定流畅。

### 结论

当前采用的完整层数 ROI、`500ms` 点击间隔和连续 `3` 次 OCR 确认组合已通过实机验证，暂不需要继续调整。

## v0.11.1 正式版整理（2026-08-23）

### 新增

- 完成本丸仓库页面：支持核心资源、其他物品、自动识别、手动编辑、保存、撤销和清空。
- 完成仓库自动识别流程：从目录进入锻刀页面读取核心资源，再进入所持道具页面读取小判和其他物品。
- 完成核心资源历史快照与独立折线图：保存后刷新显示，支持按实际时间间隔绘制、悬停查看和右键删除单点。
- 完成其他物品的手动新增、删除、改名、数量修改和拖动排序。
- 完成工作记录保存、重命名、合并及配置归属显示。

### 修复

- 修复工作记录刷新时部分日志无法读取、任务状态错误和配置归属丢失的问题。
- 修复相同配置、相同时间的日志重复计入，以及合并保存记录重复累加的问题。
- 修复仓库识别最后一页遗漏物品、到底后提前结束、数量未覆盖最新值和排序不稳定的问题。
- 修复物品名称 OCR 误识别导致同一物品被拆成多个条目的问题，并增加已保存名称匹配和受控相近字形匹配。
- 修复刀剑掉落名称中相近字形无法校验的问题。
- 修复刀帐自动识别不在具体刀剑页面时卡死或闪退的问题，改为安全失败并弹窗提示。

### 优化

- 优化工作记录中的配置名称、进行中/完成/手动停止状态和无法合并提示。
- 优化合战场、地下城和后勤的实时日志输出，补充撤退、修刀、补充刀装、疲劳处理和内番等明文行为日志，同时保持文件日志解析逻辑不变。
- 优化仓库物品列表的分段识别、滑动等待、到底判断和最后一页专用 OCR 区域。
- 优化核心资源图表的横纵轴比例，分别适配大数值资源和小数值道具，并确保首尾数据点可见。

### 验证

- 解决方案编译通过，0 个错误。
- 桌面程序已完成 Release 发布并复制到开发目录产物位置。
- 仓库自动识别和刀帐错误页面提示已完成实际运行验证。
- 正式资源版本更新为 `v0.11.1`，beta 公告不再作为正式版公告展示。

## 工作记录后勤归属与刀剑掉落校验修复（2026-08-24）

### 后勤词条归属

- 为运行记录增加“由停止状态明确结束”的解析期标记。出现“停止前状态”后，该记录不再接收后续关联词条，避免历史后勤任务吸收新一轮出阵期间产生的后勤、远征计时等日志。
- 未经“停止前状态”明确结束的任务记录仍可接收同配置的后续关联词条，保持并行任务与同步后勤场景的归属能力。
- 当没有可接收的独立后勤记录时，后勤词条回归当前出阵记录，使地下城、合战场等任务运行期间的同步后勤继续展示在该任务的“后勤记录”卡片中。
- 新增回归场景：先停止独立后勤任务，再开始地下城并产生派遣远征词条；验证该词条只归入新的地下城记录。

### 刀剑掉落名称校验

- `SwordDropLogAction` 在刀种与刀名校验时增加受控相近字形匹配，覆盖“堀/掘”“广/広/厂”“国/國”等常见 OCR 混淆。
- 仍要求刀种、刀名整体校验通过才写入掉落日志，无法确认的 OCR 结果不会计入工作记录。

### 验证

- 工作记录回归测试通过。
- 解决方案构建与桌面程序 Release 发布均通过，均为 0 个错误。

## v0.12.0-beta.1 一键日课测试版（2026-08-25）

### 日课

- 完成登录奖励、暖心礼包、合成、锻刀、演练和领取奖励的日课流程，并在各项目之间返回本丸。
- 锻刀支持收取已完成锻刀、刀位检查、许可名单刀解整理及 ALL 50 公式最多锻造 3 把。
- 演练支持指定部队、阵形和避战强敌；累计胜利 3 次或处理完 5 个对手位置后结束。
- 日课固定为单次执行，完成领取奖励并关闭界面后不再重新进入队列。

### 暂缓

- 日课检非违使识别尚不稳定，已从界面注册中移除；保留底层流程，后续稳定后再开放。

### 版本

- 资源版本从 `v0.12.0-alpha` 更新为 `v0.12.0-beta.1`，同步更新 `interface.json`、`pack.ps1` 和更新公告。

## v0.12.0-beta.2 后勤修刀与疲劳处理修复（2026-08-26）

### 后勤修刀

- 筛选完成后新增“没有”提示 OCR，命中 `[676,382,53,31]` 后直接判定无符合条件刀剑，不再执行像素扫描和上滑。
- 修刀列表上滑前后增加列表 OCR 比较；两次有效 OCR 文本相同时判定已到列表底部，停止继续上滑，OCR 无结果时保留最大滑动次数兜底。
- 无符合条件刀剑时记录 30 分钟修刀冷却，冷却期间跳过 `E_WorkshopHub`；冷却分支兼容内番启用和未启用两种后续路径。
- 修复同步后勤场景中修刀冷却分支直接进入禁用内番流程、导致地下城任务失败的问题。

### 疲劳处理

- 合战场与地下城出阵前首位疲劳值连续 OCR 失败时，不再将失败误判为低疲劳并进入刷花，改为继续出阵。
- 首位成功识别且低于阈值时仍保留原有刷花行为。

### 验证与版本

- 新增修刀冷却、疲劳识别失败和列表 OCR 比较的回归测试。
- 测试项目通过，解决方案构建通过，Release 发布产物已复制到开发目录。
- 资源版本从 `v0.12.0-beta.1` 更新为 `v0.12.0-beta.2`，同步更新 `interface.json`、`pack.ps1`、更新公告及公告序号。

## 工作记录任务名称与 Entry 日志兼容（2026-08-26）

### 任务显示名称

- 主任务显示名称调整为：一键日课、常驻作战、大阪挖地、海陆联队、战术强化、刷花回气、刀解清仓、习合搓糖、本丸后勤。
- 仅修改任务 `label`，保留原有 `name`、`entry`、选项名和 pipeline 入口，避免破坏已有任务队列与配置。

### 工作记录兼容

- 工作记录解析改为以 pipeline `Entry` 作为唯一稳定身份，不再依赖任务 `label` 或用户备注名。
- 工作记录业务日志归属优先按 pipeline `Entry` 判断，用户手动修改任务备注后仍可正确归入对应任务。
- 保存记录和保存记录分段新增 `Entry` 字段；合并记录优先按 `Entry` 判断，同一 pipeline 使用不同备注时仍可合并。
- 没有 `Entry` 的历史保存记录只按原始任务名称判断，不再维护显示名称别名。

### 日志协议

- 任务执行期间的日志上下文自动增加 `entry` 字段，同时保留原有中文业务前缀。
- 示例：`[entry=Underground] [地下城] 刀剑掉落 短刀 秋田藤四郎`。
- 工作记录解析兼容旧版无 `entry` 字段的日志，避免历史日志失效。

### 验证

- 新增任务改名、旧新名称并行运行、不同备注合并和 Entry 归属回归测试。
- 测试项目通过，`interface.json` 解析通过。
- 桌面程序 Release 发布产物已复制到开发目录；未同步测试运行目录。

## 刀剑掉落播报与结果动画识别（2026-08-27）

### 新增

- 全局设置末尾新增“刀剑掉落播报”开关，支持进入独立页面搜索刀剑名册并维护播报名单。
- 掉落识别成功且刀名命中播报名单时，弹出“获得 太刀「狮子王」”格式的桌面通知。
- 在 `[131,354,136,126]` 增加结果动画 OCR：识别为“初”时保存完整截图，并继续按普通刀剑掉落处理。
- “初”结果截图保存至 `debug/sword_drop/`，文件名包含时间、刀名和结果类型。

### 优化

- “特”与“极”结果统一视为特化类动画，不计入刀剑掉落、不截图、不播报，仅继续执行原有点击流程。
- 保留原有纯色校验，用于跳过非刀剑结果画面；普通掉落继续使用刀种、刀名校验和播报名单过滤。

### 修复

- 修复动态编译使用旧版 `MFAAvalonia.Core.dll` 导致 `SwordDropLogAction` 编译失败、启动时未注册自定义动作，进而出现 `Action is null` 和任务失败的问题。
- 重新完成 Release 发布，并将项目自身 Release Core DLL 同步至 `runtimes/libs/`，同时更新开发目录中的 `MATR.dll` 与 `MATR.exe`。

### 验证

- 播报名单匹配、开关过滤、通知文本和结果动画识别回归测试通过。
- 解决方案 Release 发布通过，0 个错误。
- 已核验 `runtimes/libs/MFAAvalonia.Core.dll` 包含 `GetAnimationKind` 和 `SwordDropAnimationKind`。

## 更新数据任务与任务顺序调整（2026-08-27）

### 更新数据任务

- 新增“更新数据”任务，支持选择识别仓库、刀帐或仓库+刀帐，并支持每次、每天、每周的触发间隔。
- 新增独立的更新数据主枢纽和刀帐识别枢纽，包含本丸确认、异常恢复、弹窗处理和回本丸流程。
- 仓库识别完成后自动保存正式数据；仓库单独识别成功回到本丸后直接结束，不再误进入刀帐识别流程。
- 刀帐识别完成后自动保存识别状态，并在当前已打开的刀帐页面中即时刷新，不需要重启程序才能看到结果。
- 修复刀帐识别数据已写入配置但当前页面仍显示旧状态的问题；保存完成后由数据服务通知刀帐页面重新读取正式数据。

### 任务配置

- 默认任务顺序调整为“更新数据”置顶，“一键日课”移动到“战术强化”后面。
- 异去模式的默认重复次数调整为 3 次。

### 验证

- 更新数据 JSON 和界面配置解析通过。
- 仓库单独识别回本丸后结束流程已完成实际运行验证。
- 解决方案构建通过，Release 发布产物已复制到开发目录。

## 刀帐识别状态与工作记录隔离（2026-08-27）

### 保存状态

- 刀帐页面的“保存”“撤销”命令增加未保存修改校验；重启或重新加载已保存数据后，按钮与仓库页面一致保持禁用。
- 草稿变更、保存、撤销、清空以及识别数据写入后的重新加载都会主动刷新 command 可执行状态，避免仅依赖界面绑定造成状态滞后。

### 工作记录

- 刀帐自动识别属于数据采集辅助任务，不纳入玩家任务统计。
- 工作记录解析器会忽略“刀帐自动识别”的开始标记及其后续日志，既不生成独立记录，也不将识别期间日志附加到上一条业务记录。

### 验证

- 新增刀帐自动识别工作记录隔离回归测试，验证上一条业务记录不受影响。
- 测试项目与解决方案构建均通过；Release 发布产物已复制到开发目录。

## 习合搓糖锁定目标专用链路（2026-08-27）

### 修复

- 修复搓糖开启时，锁定且达到乱舞等级上限的目标刀剑进入习合操作页后被误判为应退出，随后反复重新进入习合页面的问题。
- 列表页识别到锁定目标后，改为进入独立的习合操作循环，跳过目标乱舞等级限制；普通目标仍按搓糖规则跳过已达上限的目标刀剑。

### 验证

- 习合流水线 JSON 解析、路由引用和 `on_error` 配置检查通过。

## 江户潜入活动与刀解、习合流程优化（2026-08-27）

### 江户潜入

- 新增“江户潜入”活动任务，固定使用 E4（超难），支持选择部队、更换队长、不买门票、同步后勤和远征智能调度。
- 新增四种行动策略：直奔王点、保守、均衡、激进；根据旗帜位置、剩余行动次数、已探索点位类型和地图拓扑关系规划路线。
- 新增点位颜色识别与状态持久化，记录紫色、黄色和黑色点的类型，并根据未知点的等概率分布估算进王概率。
- 非激进策略在剩余一步且王点不可达时自动撤退；撤退会记录特殊情况并计入完成圈数。
- 特殊掉落、活动页面和行动选择页面之间增加连续处理链路；江户潜入的刀剑掉落与特殊掉落不进行播报。
- 旗帜位置识别使用各点位的专用模板匹配，阈值为 0.9；行动选择页面的剩余行动次数使用 OCR 识别。

### 刀解

- 优化页面按钮点击后的等待方式，使用局部画面冻结检测替代固定长延迟，减少页面切换等待和误操作。
- 筛选点击后的等待时间调整为 300ms，确认解体等关键操作继续等待画面稳定后再进入下一个流程。

### 习合

- 新增锁定目标专用处理链路，避免锁定且达到乱舞等级上限的目标被误判为无需处理。
- 优化锁定目标的全选、确认和循环操作，并补充对应页面的识别与回退路径。

### 验证

- 新增江户潜入行动规划器与点位状态相关回归测试。
- 测试项目和解决方案构建通过，Release 产物已发布并复制到开发目录。
- 已生成 `v0.12.0-beta.4` 资源包并推送至 GitHub。

## 自定编队部队记录模式（2026-08-27）

### 新增

- 自定编队预设编辑页新增“仅使用编队记录”和“仅记录编队”两个互斥复选框，位于原有两个开关下方并在同一行自适应排列。
- “仅使用编队记录”只对目标部队使用同编号的游戏部队记录，不读取预设成员、不解散部队。
- “仅记录编队”只将目标部队当前编成保存到同编号的游戏部队记录，不执行预设编成。
- “仅使用编队记录”可与“编成前卸下现有装备”同时开启：先解除目标部队装备，但不解散部队，再使用该部队记录。

### 修复

- 修复自定编队使用部队记录时始终点击部队 1 记录的问题，改为根据目标部队编号动态选择对应记录槽。
- 修复进入部队记录页面后“使用记录”识别阈值过高导致流程超时的问题。
- 修复自定义编队动作失败后仍被外层任务计为完成的问题。

### 流程调整

- 使用记录确认弹窗中点击 `[789,512]` 后，在该点击位置冻结 200ms，再点击左上角返回按钮，确保记录应用完成后再离开页面。

### 验证

- 部队记录模式的模型测试、FormationConfig JSON 校验和桌面项目构建通过。


## v0.12.3 跨平台发布与打包流程重构（2026-08-29）

### 新增

- 支持 Apple Silicon macOS 构建与运行（PR #9）：osx-arm64 发布、非 Windows 禁用 NetBeauty、MaaAgentBinary 路径回退查找、CJK 字体回退、新增 tools/pack_mac.sh 打包脚本。
- 本地一键打包脚本 tools/pack_all.ps1：一条命令同时产出 win-x64（框架依赖）与 macos-arm64（自包含）两个发布包。macOS 包在 Windows 本机交叉发布（dotnet publish -r osx-arm64），无需 macOS 环境。
- 「工作记录」页新增资源点奖励记录（木炭、玉钢等）。

### 修复

- 资源点奖励解析跨文件引用导致 action 不注册：custom 目录每个 .cs 文件独立编译成独立程序集，ResourcePointRewardParser 独立文件导致 ResourcePointLogAction 编译失败；解析逻辑合并回 ResourcePointLogAction.cs 同文件静态类（参照 SwordDropLogAction 内 SwordNameMatcher 先例），测试改为直接验证 WorkRecordBuilder 主链路。
- 调整 13 个 SwordDropColor 节点的掉落确认点击位置至 [947, 533, 67, 75]。
- 修复江户城中途撤退不计入完成一轮的问题。
- 修复远征刷花卡死的问题。
- 修复习合搓糖错误出现在工作记录里面的问题。

### 打包发布流程重构

- 移除 GitHub Actions 发布 workflow（此前反复失败的 CI 已清理），发布回归本地脚本。
- pack_win.ps1：增加 $ErrorActionPreference = 'Stop' 与发布产物存在性检查（杜绝打出残废包还报成功）；修复 libs 与根目录两处 MaaAgentBinary 拷贝冲突；托管库改为平铺拷贝到包根（标准 .NET 布局，规避 Main JIT 早于 PrivatePathHelper 注册的加载失败）。
- Desktop.csproj：全局禁用 NetBeauty（其 BeautyLibsDir 在 Windows 产生字面目录 .;/runtimes/libs，把托管库藏进非标准位置）；RemoveUnwantedFiles 改用 $(PublishDir)（原按 OutputPath 推算路径错位导致 pdb 残留）；libloader.dll 以 Reference + STARTUP_HOOKS 固化进发布产物，消除手工放置与手工改 deps.json/runtimeconfig.json 的步骤。
- libloader.dll 入库（发布必需组件，此前为本地手工放置、git 未跟踪）。
- 清理废弃文件：pack_mac.sh（被 ps1 取代）、test_macos_math_exclusion.ps1（调试脚本）、作废的 CI 计划文档、构建残留目录。

### 文档

- AGENTS.md / CLAUDE.md 同步发布流程变更：pack_all.ps1 -Version vX.Y.Z 为主流程，版本同步表改为 -Version 参数。
- 版本号更新为 v0.12.3，新增 0-v0.12.3 更新公告.md。

### 待排查：4-3 王点前撤离偶发进王点

- 用户反馈：开启 4-3 王点前撤离后，两天内实际进入王点 5 次（游戏道具计数）。
- 日志分析：S_Boss_E4_R3（王点图标模板识别）在行军界面切换后 130ms 内识别分数仅 0.12（阈值 0.98）失败，随后 S_PostClickMarching 点击行军进王点；界面稳定后同一模板分数 0.999 命中。识别失败无重试机制，疑似识别时机过早（图标渲染未完成）。
- 应用日志扫描（出阵轮次守恒：玉钢岔路 + 王点前撤退 + 异常撤退 = 出阵数）发现大量无标记轮次，与用户实际计数（5 次）不符，判断标准仍需校准。
- 状态：待复刻验证（本地复刻中），复现后从完整 maafw 分片定位。

## 2026-08-31

### 队伍点击卡顿修复

- 修复所有任务中队伍选择相关 `ClickTeam`、`ClickTeamN` 及同类队伍点击 node 点击后触发冻结等待、最长卡住约 20 秒的问题。
- 移除相关 `post_wait_freezes`，统一改为总点击 2 次，点击间隔 300ms。
- 未修改 `interface.json` 中的队伍坐标 override，仅调整各任务基础 pipeline 配置。

## 主页选项与默认布局调整（2026-09-01）

### 任务选项

- 将刀装破坏处理拆分为“补充刀装”和“刀装保护”两个独立复选框。
- 将疲劳处理中的重疲劳检测撤退拆分为独立的“疲劳撤退”复选框。
- 将“不使用自动行军”改名为“自动行军”，并反转逻辑：勾选后启用自动行军。
- 移除相关复选框的描述文字，将复选框集中排列在疲劳处理下拉框之后。

### 主页布局

- 调整默认主页卡片布局：任务设置占 6 行，任务说明占 2 行；实时视图占 3 行，日志占 5 行。
- 默认窗口尺寸调整为 `960×720`，保持 4:3 比例并满足主页内容的最小可读宽度。

### 配置与发布

- 确认用户调整后的窗口尺寸和卡片布局保存在 `config/config.json`。
- 确认卡片默认布局保存在 `assets/resource/mfa_layout.json`；仅删除 config 不会重置资源默认布局。
- 修复发布拷贝遗漏：根目录残留旧版 `MFAAvalonia.Core.dll` 会覆盖 `runtimes/libs` 中的新核心库，导致卡片布局已更新但窗口默认尺寸仍为旧值。
- 重新发布并同步根目录 `MATR.exe`、`MATR.dll` 及 `MFAAvalonia.Core.dll`，删除开发区旧配置后确认 `960×720` 默认窗口生效。

### 验证

- 选项配置测试通过。
- 桌面项目 Release 发布通过；默认布局 JSON 校验通过。

## 主页输入框布局修复（2026-09-01）

### 修复

- 文本输入框、数字输入框和队伍序列输入框保持标签与输入框同一行，并统一左对齐。
- 输入框根据当前内容自适应宽度，同时不超过设置面板实际可用宽度，避免长输入框伸入右侧滚动条区域。
- 修复自适应宽度实现使用布局更新回调导致的无限布局循环问题，改为仅在输入内容或面板尺寸变化时重新测量。
- 下拉框保持原有的单行布局，不参与输入框宽度调整。

### 配置与版本

- 资源版本和窗口标题计划更新为 `v0.12.8`；应用程序集版本保持原值不变。

### 验证

- 测试项目通过。
- 桌面项目构建通过，无编译错误。
- Release 发布产物已复制到开发目录。

## 任务专属换队长设置（2026-09-02）

### 功能调整

- 移除全局“换队长方式”及全局“拖拽跳过位置”设置。
- 常驻作战、地下城、海陆联队和江户潜入的“更换队长”改为固定使用队内拖拽；右侧齿轮进入子页面，可分别设置“跳过位置”。
- 战术强化继续使用原有队内拖拽换队长，不提供齿轮和跳过位置设置。
- 跳过位置按任务独立保存；加载旧配置时，会将原全局跳过位置迁移到四个任务的专属设置。

### 流程清理

- 移除马匹筛选、刀装筛选及其旧换队长流程、排序 node 和相关说明。
- 四个任务启用更换队长时，明确启用对应的拖拽 node；未启用时直接进入出阵确认。
- `DragCaptainAction` 改为仅读取当前任务注入的跳过位置参数，避免任务之间串用配置。

### 验证

- 配置 JSON 校验、测试项目和桌面端 Release 构建通过。
- 常驻作战、江户潜入已完成实机验证，确认会执行拖拽换队长。
- 地下城、海陆联队和战术强化因活动未开放，待开放后补充实机验证。

### 合并与恢复

- 实现已合并至 `main`：`b01c24c feat: scope captain settings to tasks`。
- 已清理隔离开发分支；如需回溯，可通过提交 `b01c24c` 查看或还原本次变更。

## 日课异常恢复与演练避战阈值（2026-09-02）

### 异常恢复

- 为一键日课的七个任务枢纽补充三类返回本丸后的恢复处理：公告弹窗、离队记录弹窗和网络请求超时提示。
- 三类处理均接入既有 `[JumpBack]` 候选链路，识别到对应画面后先完成关闭或确认，再回到日课任务流程。

### 演练避战设置

- “避战强敌”由固定复选框改为开关，默认关闭；开启后在同一设置页显示威胁度阈值。
- 威胁度阈值使用 1 到 6 的离散滑块，默认值为 6；对手威胁度达到或超过阈值时跳过该对手。
- 阈值说明为：对面每有一把丙子或极化刀剑，威胁度加一；威胁度达到阈值时视为强敌。
- 单输入滑块且配置了说明时，界面上方显示选项标题与说明，滑块独占下一行的可用宽度，数值固定显示在右端。
- 选项系统支持两种下级选项呈现方式：默认通过齿轮进入独立页面；设置 `inline_sub_options` 后直接在当前页面展开。
- 兼容旧版已勾选“避战强敌”的配置，加载后自动视为已开启，避免类型变更导致原有设置失效。

### 验证

- 新增日课恢复引用、演练阈值配置、阈值判断和全宽滑块布局测试。
- 测试项目通过，桌面端构建通过，Release 发布产物已同步至开发目录并完成哈希校验。
- 未操作测试运行目录。

## 补充刀装一键装备兜底（2026-09-02）

### 功能调整

- 合战场和地下城的“补充刀装”新增“记录不足时一键装备”子选项，默认关闭。
- 启用后仍优先使用游戏部队记录补充刀装；记录中的刀装储备无法补满时，自动进入游戏的一键装备流程补充剩余空位。
- 确认使用部队记录后等待 500ms，识别“记录确认”弹窗并在界面日志与运行日志中输出“记录中刀装不足”，再确认继续。
- 回到部队选择页面后打开一键装备入口；按“刀装不足”模板识别缺少刀装的刀剑，并点击同一行的一键装备按钮。
- 识别到“更换装备”页面后确认一键装备、返回部队选择页面并继续检查，直到没有缺装刀剑，再进入即刻出阵。

### 修复

- 修复 `EquipmentFallbackAction` 未注册到运行时资源的问题。此前 action 为空会导致流程跳过缺装检查并直接进入出阵确认。

### 验证

- 刀装不足模板、地下城等待值和运行时 action 注册校验通过。
- 桌面项目构建与 Release 发布通过，开发区根目录运行文件已同步并完成哈希校验。
- 一键装备兜底线路已完成实机验证。

## 后勤远征部队检查配置修复（2026-09-02）

### 问题定位

- 本地复现本丸后勤在部队一已设置远征地图、部队二至四已远征时，会跳过部队一并优先进入部队五的远征地图。
- 对应运行日志显示 `E_CheckTeam1` 在实际执行时为禁用状态；部队一的地图配置仅覆盖地图坐标，未改变检查开关。

### 修复

- 将五个部队的检查 node 基础值统一设为关闭，避免未配置远征地图的部队参与检查。
- 每个远征地图分支显式开启对应部队的检查 node；五个“休息”分支继续显式关闭对应检查。

### 验证

- 配置语义校验确认五个基础检查值均为关闭、100 个地图分支均显式开启、5 个休息分支均显式关闭。
- JSON 结构校验与 `git diff --check` 通过。

## 刀帐拥有状态筛选（2026-09-03）

### 功能调整

- 为“已拥有”列增加“全部 / 已拥有 / 未拥有”筛选。
- 为“中伤、真剑、内番、轻装”四个立绘列分别增加同样的拥有状态筛选。
- 五个筛选条件相互独立并支持组合使用，例如筛选未拥有本体但已拥有轻装的刀剑。
- 筛选只改变界面展示，完整刀帐数据仍用于保存、撤销、清空和自动识别，避免筛选后遗漏未显示记录。

### 实现

- 新增 `SwordBookFilterMatcher`，统一处理全部、已拥有和未拥有三种状态匹配。
- 新增 `VisibleEntries` 作为筛选后的展示集合，原 `Entries` 保持完整数据集合。
- 刀帐表头改为列名加下拉框布局，默认所有筛选均为“全部”。

### 验证与发布

- 筛选逻辑测试断言通过。
- 测试项目构建通过，0 个错误。
- 桌面项目构建和 Release 发布通过，0 个编译错误。
- `MFAAvalonia.Core.dll`、`MATR.dll`、`MATR.exe` 已拷贝至开发区根目录。
- 未操作测试运行目录。

## 任务队列正常完成状态修复（2026-09-05）

### 问题定位

- 测试区最后一次“更新数据 + 一键日课”实际全部执行成功，但工作记录显示为“手动停止”。
- 日志显示一键日课于 11:12:06 开始，11:13:23 正常完成；外层任务却在 11:12:07 提前输出“任务执行完毕”并进入停止流程。
- 原因是 `StartTask` 将异步 lambda 传给了带 `CancellationToken` 的 `TaskManager.RunTaskAsync(Action, ...)` 重载，异步 lambda 被当作 `async void` 执行，第一次 `await` 后外层就误判任务已完成。

### 修复

- 调整 `MaaProcessor.StartTask` 的调用，使其使用能够等待 `Func<Task>` 的重载，确保 `ExecuteTasks` 完整返回后才执行停止流程。
- 新增回归测试，防止任务队列再次绑定到 `Action` 重载并提前停止。
- 将该改动补充至 `docs/upstream-customizations.md` 和 `docs/upstream-customizations.json` 的任务队列定制项。

### 验证

- 回归测试通过：`FormationPreset 测试通过。`
- 桌面项目构建通过：0 个错误。
- 构建中的 NuGet 漏洞、弃用及旧运行时标识符提示均为项目已有警告。
- 未操作测试运行目录。

## 自定编队普通任务恢复（2026-09-05）

### 问题定位

- MFAA 2.16.1 升级后，上游移除了 MATR 原有的自定编队特殊任务实现，导致任务列表中没有可用的自定编队入口。
- 首次恢复为普通任务后，预设管理器被显示为“请选择预设”的跳转按钮，与原有直接在任务设置中管理预设的使用方式不一致。

### 修复

- 将“自定编队”注册为 `FormationConfig` 普通任务，不重新加入上游特殊任务列表。
- 通过任务专属 `FC_选择预设` 保存本次使用的编队预设；运行前由 `MaaProcessor` 注入编队、刀装、马匹和游戏部队记录相关参数。
- 任务设置中恢复内联预设管理器，支持选择、新增、编辑、复制、粘贴和删除预设；一键日课的预设部队继续共用同一套预设。
- 在定制台账和 `AGENTS.md` 中记录该约束，后续上游升级不得将其退回为特殊任务或仅保留 pipeline 文件。

### 验证

- 新增界面回归测试：普通任务必须直接渲染预设管理器，不能退化为打开选择子页的按钮。
- 回归测试与桌面端 Release 构建通过，0 个错误。
- 已重新发布并同步 Windows x64 产物至开发区根目录；实际启动确认内联预设管理界面可用。
- 预设管理区域字体大小与旧版略有差异，已确认不影响使用，本次不调整。
- 未操作测试运行目录。

## 导出日志反馈入口与资源兜底路径修复（2026-09-05）

### 修复

- 恢复导出日志成功后的“去反馈bug”问卷按钮；链接仅在用户主动点击后交由浏览器打开，不属于遥测采集。
- 修复默认 interface 的资源路径，统一使用 `{PROJECT_DIR}/assets/resource/base`，避免双击根目录 `MATR.exe` 时生成不需要的 `resource/base/pipeline/sample.json`。
- 将两项约束补入上游定制台账，后续 MFAA 升级时必须保留。

### 验证

- 新增回归测试，覆盖导出成功问卷入口及默认 interface 资源路径。
- 测试项目和桌面端 Release 构建通过，0 个错误。
- 未操作测试运行目录。

## 卡死重启与日课异常恢复修复（2026-09-05）

### 问题定位

- 开发区日志显示，一键日课在 `DT_DetectWhereAmI` 超时后进入 `DT_PrepareHub`，随后在 `DT_LoginRewardHub` 超时；该 hub 没有配置 `on_error`，因此 MaaFW 直接结束 `DailyTask`，没有进入 `DT_RestartGame`。
- 日志显示常驻作战恢复时模拟器已经成功重启并完成 ADB 重连，但使用包名启动游戏时返回 `unable to resolve Intent`。原流程将这次游戏启动失败直接作为恢复失败，随后任务截图超时。
- 部分模拟器没有 `monkey` 命令，不能依赖 `monkey -p` 启动应用；不同模拟器对包名默认启动 Intent 的解析也不完全一致。

### 修复

- 卡死重启开启时，为一键日课的各 hub 补充 `on_error: ["DT_RestartGame"]`，使 hub 超时或识别失败可以真正进入游戏重启流程。
- 将日课的 `DT_DetectWhereAmI` 纳入“卡死等待时间”全局覆盖，避免继续使用固定 10 秒超时。
- 游戏重启时先通过 `cmd package resolve-activity` 解析目标应用的实际启动 Activity，再使用 `am start -n` 启动；解析失败时保留包名 Intent 作为兼容兜底。
- 区分模拟器重启失败与游戏启动失败：模拟器重启失败记录错误并结束恢复；模拟器重启成功但游戏启动失败时记录警告，并放行回日课或常驻作战的主枢纽识别流程继续尝试进入游戏。
- 资源版本更新为 `v0.12.10-beta.1`，与现有更新公告保持一致。

### 验证

- 使用 MuMu 开发区设备实际解析到 `com.youzu.djlw/org.cocos2dx.lua.AppActivity`。
- 回归测试通过：`FormationPreset 测试通过。`
- 桌面端构建和 Windows x64 Release 发布通过，0 个错误。
- Release 产物已同步至开发区根目录并完成 DLL 哈希校验。
- 用户已在开发区确认日课卡死重启行为可用。
- 未操作测试运行目录。

## 任务队列回本丸策略优化（2026-09-05）

### 问题

- MFAA 特殊任务由 `Entry` 标识提供，包括倒计时、定时等待、通知、自定义程序、结束进程、电脑操作、Webhook 和切换实例。
- 任务队列在普通任务之间需要自动插入“回本丸”，但特殊任务不操作游戏画面，执行前不需要返回本丸。
- MFAA v2.16.1 升级过程中原有的任务间自动回本丸逻辑被覆盖，导致普通任务之间也不再插入回本丸。

### 优化

- 恢复普通任务之间自动插入“回本丸”的逻辑，最后一个任务后不重复插入。
- 根据下一个任务的 `Entry` 判断是否为 MFAA 特殊任务；特殊任务前跳过回本丸，普通任务接在特殊任务后时仍在其前方回本丸。
- 将特殊任务 `Entry` 集合集中到任务队列策略中，任务添加界面与队列插入逻辑共用同一判定来源。

### 验证

- 新增回归测试，覆盖普通任务、倒计时、Webhook 和无下一个任务等判定场景。
- 测试项目通过，桌面项目 Windows x64 Release 发布通过，0 个编译错误。
- 发布产物已同步至开发区根目录，并由用户实际启动验证任务流程。
- 未操作测试运行目录。

## 核心资源变化图表增强（2026-09-05）

### 新增

- 核心资源变化图表支持查看最近 24 小时、7 天和 30 天的数据，默认显示最近 7 天。
- 新增 X 轴时间刻度：24 小时使用时分格式，7 天和 30 天使用月日格式。
- 图表使用底部实线和内部虚线辅助线，不绘制上、左、右外边框。
- 鼠标悬停在具体数据点时显示记录时间、当前资源值和相对上一条有效记录的变动量；资源增加显示绿色，减少显示红色。

### 验证

- 新增时间范围筛选、时间轴标签和悬停提示格式测试。
- 测试项目通过，桌面端 Windows x64 Release 构建和发布通过，0 个错误。
- 最新发布产物已同步至开发区根目录并由用户实际启动验证。
- 未操作测试运行目录。

## 自定义动作动态编译与资源混用修复（2026-09-05）

### 问题定位

- 过去作战执行到 `S_IsSwordDropColor` 时，MaaFramework 报错 `Action is null`，任务随后从 `S_DetectWhereAmI` 和 `Sortie` 链路失败退出。
- 运行日志进一步显示，`SwordDropLogAction`、`MixGreedySelectionAction` 和 `NewMixTargetSelectionAction` 动态编译失败，原因是 `IMaaContext.GetImage()` 返回 `IMaaImageBuffer`，而 `MFAExtensions.ToBitmap` 只接受具体的 `MaaImageBuffer`。
- 自定义动作加载器使用全局缓存，并额外扫描安装目录资源路径。资源切换或资源更新后，可能继续复用上一套动作，或把不同资源版本的自定义脚本混合注册。

### 修复

- 将 `MFAExtensions.ToBitmap` 的参数类型改为 `IMaaImageBuffer`，覆盖所有通过 `IMaaContext.GetImage()` 获取截图的自定义动作。
- 将自定义动作缓存改为按绝对目录隔离，并为每个自定义目录独立维护文件监听器。
- 文件发生变化时只失效对应目录的缓存；强制重载时清空所有目录缓存。
- 自定义动作注册只使用当前资源声明的路径，并对路径标准化去重，不再额外扫描安装目录中的旧资源。

### 验证

- 桌面端 Debug 构建通过，0 个错误。
- Windows x64 Release 发布通过，0 个错误。
- Release 产物已同步至开发区根目录。
- 修复前日志中的 4 组 `IMaaImageBuffer` 转换编译错误已由统一接口签名覆盖；用户正在测试区持续运行，待实际运行日志进一步确认全部动作加载和任务执行结果。
- 未操作测试运行目录。

## 任务结束与刀剑掉落通知完善（2026-09-05）

### 优化

- 任务失败时发送系统通知，与任务完成通知保持一致；该行为已登记至 MFAAvalonia 定制台账。
- 刀剑掉落仅在既有“刀剑掉落播报”开关开启且命中用户维护的播报名单时，复用同一条通知文本发送桌面通知和已启用的外部通知渠道；未增加独立开关。

### 验证

- 回归测试覆盖任务失败系统通知和掉落外部通知调用，测试项目通过。
- 桌面端 Release 构建通过，0 个错误。

## 任务列表运行态布局优化（2026-09-05）

### 问题定位

- 上游运行耗时显示在任务名称与设置齿轮之间，窄窗口下会优先压缩中文任务名称。
- 将耗时移到第二行后，齿轮右侧仍保留旧布局遗留的固定列；移除后，状态图标虽然与齿轮处于同一列，截图中仍有视觉偏移。
- 原因是 Suki 的设置齿轮与刷新等状态图标使用不同的矢量几何，几何边界的视觉中心并不一致，单纯使用 Grid 居中无法对齐。

### 修复

- 任务项改为两行：第一行显示名称与设置齿轮，第二行显示运行耗时与状态图标。
- 移除状态图标迁移后遗留的固定空白列，避免缩窄页面时齿轮右侧保留无用空白。
- 齿轮与状态图标统一使用 24×24 布局边界、16×16 图标尺寸；状态图标组向右平移 3 个逻辑像素，补偿矢量图形的视觉偏移。

### 验证

- 新增回归检查，约束两行布局、无遗留固定列、统一图标边界和视觉偏移补偿。
- 测试项目通过，桌面端 Release 构建通过，0 个错误。
- Release 产物已同步至开发区根目录并完成哈希校验；用户已实际确认齿轮与状态图标对齐。

## 一键日课奖励领取与异常恢复完善（2026-09-05）

### 新增

- 一键日课的“领取奖励”支持分别选择日常、月常、主线与活动任务奖励；每个类别均按当前启用状态依次切换、领取并继续处理后续类别。
- 新增独立的“领取邮件”日课项目，位于领取奖励之后。采用复选框选择资源、刀装、货币、便利道具、其他物品与景趣六类邮件，默认勾选除刀装外的全部类别。
- 邮件领取支持从本丸或目录进入收件箱，按已启用类别设置筛选条件，领取可领取邮件、关闭相关界面，并输出完成日志。

### 优化

- 为日课各业务枢纽补充卡死重启后的原流程回归：重启游戏后会返回触发恢复的同一枢纽，而非重新从不确定位置继续。
- `DT_DetectWhereAmI` 增加目录按钮与菜单目录识别路径，用于在异常恢复后回到日课主流程。
- 邮箱筛选改为按用户实际启用的类别逐项点击，避免把未勾选的筛选项一并选入。

### 验证

- `interface.json` 与 `DailyTask.json` 已完成 JSON 解析及任务选项、流程引用的结构检查。
- 未操作测试运行目录；邮箱资源领取流程仍待实机验证。

## 异去重复次数升级回归修复（2026-09-06）

### 问题定位

- MFAAvalonia v2.16.1 升级覆盖了 `MaaProcessor.CreateNodeAndParam` 中 MATR 的异去轮次数处理。
- 合战场任务本身固定为 `repeatable: false`；新版通用计数逻辑因此无条件生成单次任务，忽略“过去/异去”下级 `异去_重复次数` 输入。

### 修复

- 恢复从“过去/异去”的下级选项读取 `repeat_count` 的逻辑。
- 异去的正整数轮次与 `-1` 无限循环直接传入队列，不再受合战场 `repeatable: false` 限制；过去模式维持既有三轮策略。

### 验证

- 新增回归检查，覆盖异去下级重复次数的读取路径。
- 新回归检查已通过，桌面端 Release 构建通过，0 个错误。
- 完整回归测试通过；桌面端 Release 构建通过，0 个错误。

## 轮次进度 GUI 输出升级回归修复（2026-09-06）

- 现象：异去按设置重复执行，但每圈完成后 GUI 日志不再输出“任务完成：异去作战 进度 X/Y”。
- 根因：MFAA v2.16.1 将任务 action 改为返回 `MaaJobStatus`，迁移时遗漏 `MFATask.Run` 循环中原有的 `TaskRoundComplete` 输出。
- 修复：在 `MaaAction` 成功后恢复有限重复任务的轮次输出；无限重复、单次任务及失败轮次保持不输出。

## v0.13.0-beta.7：客户端设置与唤醒本丸（2026-09-08）

### 全局设置

- 全局设置首位新增“客户端类型”：官服使用默认包名；选择其它客户端时显示包名输入框与获取说明。
- 将刀解/合成许可名单入口调整到客户端类型下方，集中客户端相关设置。
- 卡死重启界面移除自定义等待时间，默认固定为 120 秒；保留配置读取入口，便于开发测试覆盖该值。
- 旧版“卡死重启”中的目标应用包名会自动迁移到新的客户端类型配置。

### 唤醒本丸

- 新增并置顶“唤醒本丸”任务：关闭并启动游戏，依次处理经验、掉落、公告、修行书信、登录奖励、广告、更新与网络异常等启动弹窗，再进入本丸。
- 任务通过目录入口与本丸亮度、模板识别确认已回到本丸后结束。
- 该任务启动游戏时不记录为卡死重启，不向实时日志写入卡死恢复信息，也不会生成工作记录。

### 验证

- 测试项目通过，资源接口与 `WakeHome` 流程 JSON、引用关系检查通过。
- 桌面端 Windows x64 Release 发布通过，0 个错误。
- 未操作测试运行目录。

## 内番服与本丸刀帐联动（2026-09-09）

### 新增

- 后勤“内番”新增“联动本丸刀帐”子设置。识别到内番服立绘时，会直接更新已保存的刀帐数据并保存；无需先在刀帐页面手动保存未提交修改。
- 同名刀剑存在多个刀帐序号时，优先更新已拥有条目中序号最大的记录；没有已拥有同名条目时，更新最小序号记录，并同时登记“已拥有”和“内番服”。
- “联动本丸刀帐”下新增“自动刷取内番服”。开启后，自动移除两个饲马位置的既有安排，并优先安排已拥有但尚未拥有内番服的刀剑男士；候选不足或目标不可选时，以任意可用刀剑补位。
- 自动流程仅调整饲马位置。种地位置保留原安排及其特殊加成，切磋位置不参与内番服获取。
- 结算展示的内番服立绘组为随机结果，识别到的实际展示刀剑会同步至刀帐，不要求与本轮饲马安排的两把刀剑一致。

### 验证

- 自动流程实际读取到“厚藤四郎、小夜左文字”，完成刀种筛选、精确名称匹配、两位饲马安排及开始内番。
- 运行时动态编译并注册 `NaibanOutfitSelectionAction`、`NaibanFilterClickAction`、`NaibanFindSwordAction` 成功。
- 实际结算识别到“数珠丸恒次、大典太光世”，并成功同步两把刀剑的内番服至刀帐，符合随机展示规则。
- 测试项目、JSON 结构校验和桌面端 Windows x64 Release 发布通过；用户已在开发区完成实际运行验证，未操作测试运行目录。

## 提前结束当前任务首批接入（2026-09-09）

### 新增

- 新增资源侧 `CompleteCurrentTaskAction`：仅请求跳过当前队列项的剩余重复次数，不停止全局队列，不清空后续任务。
- 队列在当前 MaaFW 流程正常返回后消费提前结束请求；触发该请求的当轮不计入完成轮数，后续自动回本丸项及队列项照常执行。
- 提前结束会以 Warning 词表写入工作记录特殊情况，格式为 `[任务名] 任务结束 原因：原因`。

### 首批接入范围

- 仅接入常驻作战与江户潜入；其他任务和活动暂不改动，待活动再次开放或确认具体流程后再逐项接入。
- 常驻作战：重伤修刀后停止、重伤停止、刀装破坏停止、部队记录刀装不足停止、异去不购买门票，以及修刀流程兜底。
- 江户潜入：`EC_TerminateNoTicket` 在通行令牌不足且无法继续补充时提前结束；`EC_TerminateRound` 保持正常完成一圈，不接入结束动作。

### 后续

- 目标 PT 功能仍停留在设计阶段，待活动开放后补充实际 PT 识别、配置界面与活动页接入。
- 其余活动的疲劳、重伤、刀装和门票出口需按活动实际界面逐项确认，不进行批量替换。

### 验证

- `Sortie.json`、`EdoCastle.json` 和上游定制记录 JSON 已完成结构与路由校验。
- 桌面项目构建通过，0 个错误；未操作测试运行目录。

## 秘宝之里完善与目标 PT 功能（2026-09-10）

### 秘宝之里（花牌）任务

- 新增固定开启的自动行军链路：进入部队选择页时用橙色 ColorMatch 判定未开启状态，开启后经「委托」确认返回复核。该功能不提供界面选项。
- 接入远征智能调度：部队选择页挂入倒计时检查点，`E_CheckTimerExpired.next` 指向完整的自动行军链路而非单个条件 node，避免条件不命中时触发 `on_error` 回本丸形成循环。
- 特殊掉落改为 TemplateMatch `Activity/秘宝之里_达成报酬.png`，点掉后进入复核 hub，一圈结束时由 `HP_TerminateRound` 写入完成一圈记录。
- 出阵后枢纽重做为平级扫描，依次处理重伤修刀、刀装补充、门票确认与门票不足；这些 node 默认开启，不注册界面选项。
- 门票改为两级弹窗：第一级识别「通行令牌不足」，第二级识别「通行令牌」。勾选购买门票时第一级点击购买按钮并进入 `HP_ClickTicketConfirm` → `HP_IsConfirmPurchase` 完成购买。
- 移除已无用途的 `HP_EnterEvent`，难度确认后直接进入部队选择。

### 门票选项反转

- 联队战、江户潜入、战术强化、合战场异去与秘宝之里的门票选项由「不买门票」改为「购买门票」，默认不勾选。
- 未勾选时门票不足直接结束当前任务；勾选后自动购买。各任务的 `cases` 与 pipeline 默认值按此对调，不新增 enabled 开关。
- 修正联队战 `LR_IsSwordDropAfter` 缺失的识别参数，补齐为与 `LR_IsSwordDropColor` 一致的 ColorMatch 与掉落记录。

### 目标 PT 功能

- 新增 `ActivityGoalPtPlanner`：按计划日线性推算今日绝对目标，公式为向上取整(总目标 × 已过天数 ÷ 总天数)。计划日以本地时间每日 05:00 为界，早于开始日期不检查，达到截至日期后固定为总目标。
- 新增 `ActivityGoalPtSettings`：解析任务面板的目标 PT 配置。目标总 PT 非正数、日期缺失或开始晚于截至时一律返回禁用，不参与判定。
- 新增 `GoalPtCheckAction`：在活动页读取累计 PT，未达标返回 true 走 `next` 继续出阵，达标返回 false 走 `on_error` 由结束 node 跳过剩余轮数。PT 识别失败时返回 null，绝不判定达标。
- 新增日期选择控件：输入项支持 `control: "date"`，渲染为 Avalonia `CalendarDatePicker`，取值格式 `yyyy-MM-dd`。三条控件生成路径均已同步。
- 秘宝之里新增「达到目标PT后跳过任务」选项组，位于任务选项首位，默认关闭。开启后可设目标总 PT（默认 300000）与日期计划；日期计划开启后在同页面显示开始与截至日期。
- 日期控件不叠加选项级 header，避免与输入框自身的标签重复显示。
- 内联子选项补上说明渲染：此前 Toggle 与 ComboBox 两条内联路径只渲染子选项控件，选项级 description 不会显示；现按齿轮子页面的样式渲染为灰色多行文字。

### 验证

- 日期计算逻辑 18 条断言独立验证通过，覆盖 05:00 边界、逐日推进、向上取整、无效配置与识别失败不判定达标。
- 桌面项目 Windows x64 Release 发布通过；用户已在开发区完成实际运行验证，未操作测试运行目录。
## 合战场疲劳值 0 识别与刷花打点调整（2026-09-10）

### 疲劳值 0 识别

- `FatigueCheckAction` 四处解析由 `val > 0` 放宽为 `val >= 0`：游戏内疲劳值可以为 0（显示 `0/100`），此前被当作识别失败丢弃，首位读到 0 时会重试 10 次后放弃，低疲劳刀剑不进入刷花。
- `DragCaptainAction` 与 `check_all` 的空槽位判断不受影响：空槽位不显示 `xx/100`，不会被读成 0。
- 同步修正两处日志文案（`> 阈值?` 改为 `>= 阈值?`），与判定逻辑一致。

### 刷花打点调整

- `[合战场] 刷花` 打点由 `SF_Hub` 移至 `SF_ClickSortieNow`：识别「即刻」并点击出阵时统计，刷花次数计数更准确。
- `SF_ClickSortieNow` 使用内嵌 `LogAction` + 显式 `click` 参数，识别、等待与后续路由保持不变；`SF_Hub` 保留为纯路由 node。
- 同步更新 `docs/node命中日志打点设计.md` 的 node 映射表。

### 验证

- 桌面项目构建通过，0 个错误；用户待同步至测试区实机验证。
